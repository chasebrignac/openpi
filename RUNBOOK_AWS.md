# Manual AWS foundation runbook

This runbook records the foundation created for the AWS-only π0.5 reproduction. It is intentionally manual: no CloudFormation should replace it until two clean smoke replays finish without undocumented changes.

## Scope and invariants

- Account: `752160877725`
- Region: `us-east-2`
- Project tag: `Project=pi05-aws-repro`
- Capacity policy: On-Demand only
- Created by this foundation pass: S3, ECR, IAM/SSM identity, a no-ingress security group, CloudWatch Logs, SNS/SQS budget transport, and one AWS Budget
- Live manual resource: one On-Demand `g6e.4xlarge` workbench launched for the first replay; its exact instance, cost reservation, and independent stop schedule are recorded below.
- Not created: launch templates, Spot requests, Capacity Reservations, Capacity Blocks, datasets, or container images
- Projected-cost launch checks and the cross-month project ledger are the hard stop. AWS Budgets is a delayed, account-wide, monthly backstop.

The machine-readable record is `repro/aws-foundation.json`. Policy inputs are under `infra/manual/`.

## 1. Resolve identity and network before mutation

Run these checks and stop if the account or region differs:

```bash
aws sts get-caller-identity --output json
aws ec2 describe-vpcs \
  --region us-east-2 \
  --filters Name=is-default,Values=true \
  --query 'Vpcs[].{VpcId:VpcId,CidrBlock:CidrBlock,State:State}' \
  --output json
aws ec2 describe-subnets \
  --region us-east-2 \
  --filters Name=vpc-id,Values=vpc-0b8deb3b4af5473ff \
  --query 'Subnets[].{SubnetId:SubnetId,AZ:AvailabilityZone,Available:AvailableIpAddressCount,MapPublic:MapPublicIpOnLaunch}' \
  --output json
```

Resolved on 2026-08-03: default VPC `vpc-0b8deb3b4af5473ff`; subnets `subnet-02b3d2aff3f50bf4a` (`2a`), `subnet-0b49463b4ceee4d0f` (`2b`), and `subnet-06510c52d562c79fd` (`2c`).

Before every replay, inspect exact names. A nonempty result means skip the corresponding `create-*` command and reconcile it with the `put-*`, `set-*`, or verification commands below.

```bash
aws s3api list-buckets \
  --query "Buckets[?Name=='pi05-repro-752160877725-us-east-2']" \
  --output json
aws ecr describe-repositories \
  --region us-east-2 \
  --query "repositories[?repositoryName=='pi05-repro']" \
  --output json
aws iam list-roles \
  --query "Roles[?RoleName=='pi05-repro-ssm-instance-role']" \
  --output json
aws iam list-instance-profiles \
  --query "InstanceProfiles[?InstanceProfileName=='pi05-repro-ssm-instance-profile']" \
  --output json
aws ec2 describe-security-groups \
  --region us-east-2 \
  --filters Name=vpc-id,Values=vpc-0b8deb3b4af5473ff Name=group-name,Values=pi05-repro-workbench-no-ingress \
  --output json
aws logs describe-log-groups \
  --region us-east-2 \
  --log-group-name-prefix /pi05-repro/jobs \
  --output json
aws budgets describe-budgets \
  --account-id 752160877725 \
  --query "Budgets[?BudgetName=='pi05-repro-hard-cap']" \
  --output json
```

## 2. Create and harden versioned artifact storage

The create command was run once because the preflight result was empty:

```bash
aws s3api create-bucket \
  --bucket pi05-repro-752160877725-us-east-2 \
  --region us-east-2 \
  --create-bucket-configuration LocationConstraint=us-east-2 \
  --object-ownership BucketOwnerEnforced
```

Apply controls serially. Serial order matters immediately after bucket creation.

```bash
aws s3api put-public-access-block \
  --bucket pi05-repro-752160877725-us-east-2 \
  --region us-east-2 \
  --public-access-block-configuration file://infra/manual/s3-public-access-block.json
aws s3api put-bucket-versioning \
  --bucket pi05-repro-752160877725-us-east-2 \
  --region us-east-2 \
  --versioning-configuration Status=Enabled
aws s3api put-bucket-encryption \
  --bucket pi05-repro-752160877725-us-east-2 \
  --region us-east-2 \
  --server-side-encryption-configuration file://infra/manual/s3-encryption.json
aws s3api put-bucket-policy \
  --bucket pi05-repro-752160877725-us-east-2 \
  --region us-east-2 \
  --policy file://infra/manual/s3-tls-policy.json
aws s3api put-bucket-lifecycle-configuration \
  --bucket pi05-repro-752160877725-us-east-2 \
  --region us-east-2 \
  --lifecycle-configuration file://infra/manual/s3-lifecycle.json
aws s3api put-bucket-tagging \
  --bucket pi05-repro-752160877725-us-east-2 \
  --region us-east-2 \
  --tagging 'TagSet=[{Key=Project,Value=pi05-aws-repro},{Key=ManagedBy,Value=manual-runbook},{Key=Environment,Value=reproduction}]'
```

The lifecycle rule only aborts incomplete multipart uploads after seven days. It does not expire datasets, checkpoints, manifests, or object versions.

## 3. Create the immutable container repository

```bash
aws ecr create-repository \
  --region us-east-2 \
  --repository-name pi05-repro \
  --image-tag-mutability IMMUTABLE \
  --image-scanning-configuration scanOnPush=true \
  --encryption-configuration encryptionType=AES256 \
  --tags Key=Project,Value=pi05-aws-repro Key=ManagedBy,Value=manual-runbook
aws ecr put-lifecycle-policy \
  --region us-east-2 \
  --repository-name pi05-repro \
  --lifecycle-policy-text file://infra/manual/ecr-lifecycle.json
```

Only untagged intermediate images expire. Training and evaluation commands must use an immutable image digest.

## 4. Create the SSM instance identity

```bash
aws iam create-role \
  --role-name pi05-repro-ssm-instance-role \
  --assume-role-policy-document file://infra/manual/ec2-ssm-trust.json \
  --description 'SSM-only EC2 role for pi0.5 AWS reproduction' \
  --max-session-duration 43200 \
  --tags Key=Project,Value=pi05-aws-repro Key=ManagedBy,Value=manual-runbook
aws iam attach-role-policy \
  --role-name pi05-repro-ssm-instance-role \
  --policy-arn arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
aws iam put-role-policy \
  --role-name pi05-repro-ssm-instance-role \
  --policy-name pi05-repro-artifact-and-logging \
  --policy-document file://infra/manual/instance-foundation-policy.json
aws iam create-instance-profile \
  --instance-profile-name pi05-repro-ssm-instance-profile \
  --tags Key=Project,Value=pi05-aws-repro Key=ManagedBy,Value=manual-runbook
aws iam add-role-to-instance-profile \
  --instance-profile-name pi05-repro-ssm-instance-profile \
  --role-name pi05-repro-ssm-instance-role
```

The role has standard SSM agent permissions plus repository-scoped ECR push/pull, artifact-bucket read/write without object deletion, reproduction-log writes, and metrics only in the `Pi05Repro` namespace. It cannot administer infrastructure.

## 5. Create no-ingress networking and logs

```bash
aws ec2 create-security-group \
  --region us-east-2 \
  --vpc-id vpc-0b8deb3b4af5473ff \
  --group-name pi05-repro-workbench-no-ingress \
  --description 'No-ingress SSM access for pi0.5 AWS reproduction' \
  --tag-specifications 'ResourceType=security-group,Tags=[{Key=Project,Value=pi05-aws-repro},{Key=ManagedBy,Value=manual-runbook}]'
aws logs create-log-group \
  --region us-east-2 \
  --log-group-name /pi05-repro/jobs \
  --tags Project=pi05-aws-repro,ManagedBy=manual-runbook
aws logs put-retention-policy \
  --region us-east-2 \
  --log-group-name /pi05-repro/jobs \
  --retention-in-days 30
```

Do not authorize any ingress rules. The security group deliberately retains default outbound IPv4 access for SSM, ECR, S3, package, and source endpoints; there are no paid VPC endpoints or NAT gateways in this foundation.

## 6. Create email-free budget alert delivery

The SNS policy admits `budgets.amazonaws.com` only from account `752160877725`. The SQS queue provides an encrypted, 14-day durable sink, so budget alarms do not require an email address.

```bash
aws sns create-topic \
  --region us-east-2 \
  --name pi05-repro-budget-alerts \
  --tags Key=Project,Value=pi05-aws-repro Key=ManagedBy,Value=manual-runbook
aws sns set-topic-attributes \
  --region us-east-2 \
  --topic-arn arn:aws:sns:us-east-2:752160877725:pi05-repro-budget-alerts \
  --attribute-name Policy \
  --attribute-value file://infra/manual/budget-topic-policy.json
aws sqs create-queue \
  --region us-east-2 \
  --queue-name pi05-repro-budget-alerts \
  --attributes MessageRetentionPeriod=1209600,SqsManagedSseEnabled=true \
  --tags Project=pi05-aws-repro,ManagedBy=manual-runbook
aws sqs set-queue-attributes \
  --region us-east-2 \
  --queue-url https://sqs.us-east-2.amazonaws.com/752160877725/pi05-repro-budget-alerts \
  --attributes file://infra/manual/budget-queue-attributes.json
aws sns subscribe \
  --region us-east-2 \
  --topic-arn arn:aws:sns:us-east-2:752160877725:pi05-repro-budget-alerts \
  --protocol sqs \
  --notification-endpoint arn:aws:sqs:us-east-2:752160877725:pi05-repro-budget-alerts \
  --attributes RawMessageDelivery=true \
  --return-subscription-arn
aws budgets create-budget \
  --account-id 752160877725 \
  --budget file://infra/manual/budget.json \
  --notifications-with-subscribers file://infra/manual/budget-notifications.json
```

The delivery-path smoke test used these exact commands. The purge removed only the received synthetic test message.

```bash
aws sns publish \
  --region us-east-2 \
  --topic-arn arn:aws:sns:us-east-2:752160877725:pi05-repro-budget-alerts \
  --subject pi05-repro-foundation-test \
  --message 'Foundation alert path test; not a spend alarm.'
aws sqs receive-message \
  --region us-east-2 \
  --queue-url https://sqs.us-east-2.amazonaws.com/752160877725/pi05-repro-budget-alerts \
  --wait-time-seconds 5 \
  --max-number-of-messages 1 \
  --attribute-names All \
  --message-attribute-names All
aws sqs purge-queue \
  --region us-east-2 \
  --queue-url https://sqs.us-east-2.amazonaws.com/752160877725/pi05-repro-budget-alerts
```

The alarms are absolute actual-spend thresholds of `$1,500`, `$2,400`, and `$2,700`, plus a forecasted-spend threshold of `$3,000`. The budget is account-wide and monthly. At validation it showed `$327.962` actual and `$496.321` forecast account spend; these are not attributed to this empty foundation. Do not subtract them from the project ledger.

Activate the project tag for later Cost Explorer reconciliation:

```bash
aws ce update-cost-allocation-tags-status \
  --region us-east-1 \
  --cost-allocation-tags-status TagKey=Project,Status=Active
```

Poll the durable alert sink during active runs:

```bash
aws sqs receive-message \
  --region us-east-2 \
  --queue-url https://sqs.us-east-2.amazonaws.com/752160877725/pi05-repro-budget-alerts \
  --wait-time-seconds 20 \
  --max-number-of-messages 10 \
  --attribute-names All \
  --message-attribute-names All
```

## 7. Verification gates

Run all checks before any instance launch:

```bash
aws s3api get-bucket-versioning --bucket pi05-repro-752160877725-us-east-2 --region us-east-2
aws s3api get-bucket-encryption --bucket pi05-repro-752160877725-us-east-2 --region us-east-2
aws s3api get-public-access-block --bucket pi05-repro-752160877725-us-east-2 --region us-east-2
aws s3api get-bucket-ownership-controls --bucket pi05-repro-752160877725-us-east-2 --region us-east-2
aws ecr describe-repositories --region us-east-2 --repository-names pi05-repro --output json
aws iam get-instance-profile --instance-profile-name pi05-repro-ssm-instance-profile --output json
aws ec2 describe-security-groups --region us-east-2 --group-ids sg-05e45a674dacd5e01 --output json
aws logs describe-log-groups --region us-east-2 --log-group-name-prefix /pi05-repro/jobs --output json
aws budgets describe-budget --account-id 752160877725 --budget-name pi05-repro-hard-cap --output json
aws budgets describe-notifications-for-budget --account-id 752160877725 --budget-name pi05-repro-hard-cap --output json
aws sns list-subscriptions-by-topic --region us-east-2 --topic-arn arn:aws:sns:us-east-2:752160877725:pi05-repro-budget-alerts --output json
aws sqs get-queue-attributes --region us-east-2 --queue-url https://sqs.us-east-2.amazonaws.com/752160877725/pi05-repro-budget-alerts --attribute-names All --output json
aws iam simulate-principal-policy \
  --policy-source-arn arn:aws:iam::752160877725:role/pi05-repro-ssm-instance-role \
  --action-names s3:PutObject ecr:PutImage logs:PutLogEvents ec2:RunInstances \
  --resource-arns \
    arn:aws:s3:::pi05-repro-752160877725-us-east-2/test-manifest.json \
    arn:aws:ecr:us-east-2:752160877725:repository/pi05-repro \
    arn:aws:logs:us-east-2:752160877725:log-group:/pi05-repro/jobs:test \
  --output json
```

Accepted state on 2026-08-03:

- S3 versioning enabled, AES-256 default encryption, all four public-access blocks enabled, bucket-owner enforcement, and TLS-only bucket policy.
- ECR AES-256 encrypted, scan-on-push enabled, immutable tags.
- Instance profile contains exactly `pi05-repro-ssm-instance-role`; the role has the SSM managed policy and one scoped inline policy.
- Security group `sg-05e45a674dacd5e01` has zero ingress permissions.
- Log retention is 30 days.
- Four budget notifications are in `OK` state and delivered through SNS to encrypted SQS.
- SNS-to-SQS delivery was tested with the message `Foundation alert path test; not a spend alarm.` The single test message was purged after successful receipt.
- IAM simulation allowed artifact upload, ECR image publication, and job-log writes; it denied `ec2:RunInstances` and destructive artifact/repository/log actions.
- No Spot, reserved, blocked, or dedicated capacity was created. The later manual replay launched the one On-Demand workbench recorded below.

## 8. Plan and launch On-Demand instances

Use `scripts/repro_aws_launch.py` for every GPU launch. Omitting `--execute` is the default read-only mode: it verifies the STS account, pinned region, AMI, subnet, zero-ingress security group, SSM profile, and instance-type offering, then prints the exact cost and deadline plan. It does not reserve money or call `RunInstances`.

Every launch declares both a spend `--category` and an underlying
`--workload`. They are identical for normal runs. A bounded corrective retry
uses `--category corrective_run` but must retain the actual workload, such as
`--workload shallow_training`. The launcher applies the normal workload matrix
unless an exact corrective-only fallback is declared in source. The sole
current exception is one `g7e.4xlarge` for a single-process Shallow replay
after a failed two-GPU runtime gate; ordinary `shallow_training` launches
remain locked to one `g7e.12xlarge`.
AMI selection is workload-scoped and has no command-line override. Workload
`evaluation` deterministically selects Amazon AMI
`ami-06517bc7fad3c6a48`, exact name
`Deep Learning Base OSS Nvidia Driver GPU AMI (Ubuntu 22.04) 20260403`; every
other workload selects the pinned base AMI. Preflight queries the selected ID
with owner `amazon` and requires owner ID `898082745236` (plus owner alias when
returned), exact name, `available` state, `x86_64`, `Linux/UNIX`, HVM, and root
device `/dev/sda1`. The selected ID remains in the printed plan and launch
request.

```bash
python3 scripts/repro_aws_launch.py \
  --category workbench_setup \
  --workload workbench_setup \
  --instance-type g6e.4xlarge \
  --hours 8 \
  --label pi05-workbench
```

Worker launches require a command, preferably kept in a reviewed file so shell quoting is unambiguous:

```bash
# First render `/tmp/libero-shallow-2k-01.command.sh` from the immutable
# bootstrap/spec VersionIds exactly as shown in repro/WORKER_RUNBOOK.md.
python3 scripts/repro_aws_launch.py \
  --category shallow_training \
  --workload shallow_training \
  --instance-type g7e.12xlarge \
  --hours 12 \
  --label shallow-libero-pilot \
  --command-file /tmp/libero-shallow-2k-01.command.sh
```

Normal Shallow training is intentionally launch-guarded to `g7e.12xlarge`: its
two local GPUs match the documented `torchrun --nproc-per-node=2` contract. If the
first AZ reports insufficient capacity, rerender the same launch with
`--subnet-id subnet-0b49463b4ceee4d0f`, the other foundation-pinned subnet in
which preflight found G7e. If both pinned AZs are unavailable, record an AWS
capacity outage and retry later rather than launching a command that cannot
run.

The one-GPU fallback is not a capacity-outage substitution and never runs the
two-process command. A4 used it as a non-publishing methodology diagnostic:
that spec contains direct `python scripts/train_pytorch.py` with no `torchrun`,
resume, overwrite, or publication destination. A4's one-batch checkpoint is
diagnostic evidence only and must never be selected as a worker artifact or
resume target.

After A4, this policy authorizes exactly one fresh single-GPU 2k LIBERO pilot,
not a generic expansion of the fallback. Its preparation gate must bind all of
the following before publishing a new spec or reserving spend:

- A4 run manifest VersionId `JyxugbqH_T.B9biVyfcETR46Sa5VTJxG`, SHA-256
  `6b642cbd90aba982fb59e84a9db7e0fbfa3d976878d81c08da28eca5be81a12c`,
  reports `succeeded`, exit zero, the passing 300-step finite overfit gate, and
  no reusable `published_inputs`.
- A4 final-sync evidence VersionId `RmSnrJEqgm3wsXj6NFBYZa9hGv3Bd9Fd`,
  SHA-256
  `7dfcce4bfdc1f4230574eb14ceb776c5a97537a8ac4fb343fa2cf2b0baf2af21`,
  reports successful final sync and binds that exact manifest receipt.
- A4 instance `i-0a5a2502e114aaedc` is terminated, and no other training or
  diagnostic GPU process or active Shallow worker exists. Never overlap a
  promotion candidate with a CUDA/NCCL diagnostic.
- The 2k worker has a fresh run ID and fresh experiment ID, retains the exact
  accepted model source, image, LIBERO dataset, teachers, seed, config, and
  batch semantics, and invokes direct `python scripts/train_pytorch.py` with
  `--num-train-steps 2000 --save-interval 2000`. It contains no `torchrun`,
  `--resume`, `--overwrite`, or one-batch-overfit option.
- The 2k spec declares exactly one checkpoint output at
  `checkpoints/CONFIG/EXPERIMENT/2000` and exactly one matching
  `publish_destination` at `CONFIG/EXPERIMENT/2000`. No other output may be
  promoted as a worker input.

Launch that one pilot only as `corrective_run` plus `shallow_training` on one
On-Demand `g7e.4xlarge` with `CapacityReservationPreference=none`. Four
requested hours cover the measured approximately 13m24s launch-to-first-step,
approximately 5.4 seconds for each of 2,000 optimizer steps (three hours), and
the checkpoint/final-sync buffer; the launcher separately reserves its
0.25-hour boot-and-shutdown margin:

```bash
python3 scripts/repro_aws_launch.py \
  --category corrective_run \
  --workload shallow_training \
  --instance-type g7e.4xlarge \
  --hours 4 \
  --label shallow-libero-pilot-single-gpu-2k \
  --command-file /tmp/libero-shallow-pilot-single-gpu-2k.command.sh
```

One later single-GPU 2k-to-5k continuation is authorized only after the 2k
worker succeeds, final sync succeeds, its instance terminates, and its run
manifest contains exactly one immutable `published_inputs` descriptor. Copy
that descriptor unchanged into the fresh 5k run's `artifacts`; do not rebuild
it from an S3 prefix. The 5k run keeps the 2k experiment ID, uses a fresh run
ID, invokes direct Python with exactly one `--resume` and no `--overwrite`,
targets exactly `CONFIG/EXPERIMENT/2000`, and declares exactly one matching
published checkpoint at `CONFIG/EXPERIMENT/5000`. The worker must retrieve the
manifest and every checkpoint file by exact VersionId, reproduce every hash,
validate the schema-v2 resume fingerprint and complete optimizer/model state,
and restore the 2k checkpoint without re-uploading it. This authorization does
not admit additional pilots, sweeps, tracks, instance types, concurrent GPU
processes, or ordinary `shallow_training` launches on `g7e.4xlarge`; any later
continuation needs its own evidence-bound promotion decision.

The manual TensorRT replay is intentionally different from an ephemeral
training worker. It needs one bounded G7e session to survive a failed or
completed bootstrap while export, engine build, numerical validation, latency,
and compiled rollout commands are iterated over SSM on the same GPU. Plan it
with the explicit retention flag:

```bash
python3 scripts/repro_aws_launch.py \
  --category export_compile_quantize \
  --workload export_compile_quantize \
  --instance-type g7e.4xlarge \
  --hours 8 \
  --label pi05-trt-manual-01 \
  --scheduler-role-arn arn:aws:iam::752160877725:role/pi05-repro-scheduler-deadline-role \
  --command-file /tmp/pi05-trt-session-bootstrap.sh \
  --retain-after-command
```

`--retain-after-command` is rejected outside
`export_compile_quantize`. It only omits the job service's immediate
`ExecStopPost`; it does not broaden capacity or extend time. The instance
remains On-Demand, instance-initiated shutdown remains `terminate`, and both
the guest timer and independent EventBridge Scheduler still terminate it at
the fully reserved absolute deadline. Add `--execute` only after reviewing the
plan, command, ledger, and schedule role.

The bounded RoboLab camera smoke uses the pinned evaluation AMI and published
evaluator image through the reviewed worker command. Run this without
`--execute` first:

```bash
python3 scripts/repro_aws_launch.py \
  --category evaluation \
  --workload evaluation \
  --instance-type g6e.4xlarge \
  --hours 1 \
  --label robolab-r580-camera-smoke \
  --scheduler-role-arn arn:aws:iam::752160877725:role/pi05-repro-scheduler-deadline-role \
  --command-file repro/robolab-smoke-worker.sh
```

The first paid evaluation smoke completed successfully. Its immutable evidence
is recorded below; replay the same read-only plan before any later launch.

Review the plan, current `.repro/cost-ledger.json`, and the command file. Only then repeat the same invocation with the explicit mutation flag:

```bash
# Add this flag only after reviewing the read-only plan:
--execute
```

The execution path rechecks the ledger under a local file lock, reserves the projected instance cost before the paid API call, and adds 15 minutes of budget margin for boot and shutdown. It uses an idempotent EC2 client token, On-Demand capacity with `CapacityReservationPreference=none`, default tenancy, no key pair, the pinned no-ingress group and SSM profile, IMDSv2-only metadata with hop limit one, and an encrypted gp3 root volume. Ephemeral job containers run with networking disabled after their immutable inputs and image are staged, so they cannot reach IMDS or mutate AWS directly. The workbench defaults to 1 TiB and stops at its deadline; ephemeral workers default to 256 GiB and terminate. Both instance and volume receive `Project=pi05-aws-repro`, stage, and run-ID tags.

The cost guard withholds `$250` of the `$3,000` cap from EC2 reservations:
the full `$150` storage/log category plus `$100` headroom. This covers gp3,
S3, ECR, CloudWatch, and transfer charges that AWS reports only after a delay.
The live Ohio gp3 price recorded on 2026-08-03 is `$0.08/GB-month`. A stopped
workbench still accrues its 1 TiB gp3 charge, so stop is only a short pause;
terminate the workbench as soon as its durable state is verified in S3.

User data installs an absolute UTC systemd deadline before starting the supplied worker command. A definitive launch failure is reconciled by client token and cancels the reservation. If AWS cannot be queried after an ambiguous network failure, the reservation remains in `launch_unknown`; do not retry until this read-only query is conclusive:

```bash
aws ec2 describe-instances \
  --region us-east-2 \
  --filters Name=client-token,Values=RESERVATION_ID \
  --output json
```

Never manually change `launch_unknown` to `cancelled` merely because the CLI timed out. First establish that no instance, including a terminated one, has that token. Record reconciliation and any deadline/SSM issue in the manual iteration log.

### First paid manual replay

The first reviewed execution launched exactly one On-Demand workbench:

- Instance: `i-038112fe75c610517` (`g6e.4xlarge`, `us-east-2a`)
- Project reservation: `ae4bc2fd-6384-49d0-ac9a-fb5e5891ef2e`
- Reserved compute: `$18.7765` for 6.25 billed hours (six requested hours plus the launcher's margin)
- Root volume: encrypted 1 TiB gp3 `vol-0de00b022d7154c96`
- Original guest stop timer: `2026-08-04T05:26:34Z`; reviewed four-hour extension deadline: `2026-08-04T09:26:34Z`
- Independent schedule: `pi05-deadline-ae4bc2fd-6384-49d0-ac9a-fb5e5891ef2e`, updated to target `StopInstances` at the extension deadline
- Extension reservation: `6a2673de-4dd7-4ab8-91e4-90f7882c19fe`, `$12.01696` for four additional On-Demand hours on the same instance

The live preflight proved an L40S with 46,068 MiB, driver 595.71.05, Docker 29.6.2, SSM identity in account `752160877725`, bucket-encryption access, and the following storage layout. The DLAMI owns the instance-store initialization: `/dev/nvme1n1` is an LVM physical volume and `/dev/mapper/vg.01-lv_ephemeral` is already mounted read/write at `/opt/dlami/nvme`. Workers reuse that verified mount; they never format it. Every execution creates a new `pi05-runs/RUN_ID` subtree and refuses an existing run ID, so mount reuse never means output/control-state reuse.

### First paid evaluation smoke

The reviewed R580 camera-smoke execution launched one short-lived On-Demand
evaluator and terminated it after the worker exited:

- Instance: `i-011eb2c219aea0e3e` (`g6e.4xlarge`, `us-east-2a`)
- Project reservation: `96c5e984-1279-42c5-b795-ba7699683422`
- Reserved compute: `$3.7553` for 1.25 billed hours (one requested hour plus the launcher's margin)
- AMI: `ami-06517bc7fad3c6a48`; observed NVIDIA driver `580.126.09`
- Evaluator image: `752160877725.dkr.ecr.us-east-2.amazonaws.com/pi05-repro@sha256:2d17c15e62887c9fc8b4c41b7ee3d39c4c187348eb55b4273fd24e785a3325e7`
- Independent schedule: `pi05-deadline-96c5e984-1279-42c5-b795-ba7699683422`, targeting termination at `2026-08-04T01:52:03Z`
- Versioned evidence: `s3://pi05-repro-752160877725-us-east-2/manual-smoke/robolab/20260804T005329Z-i-011eb2c219aea0e3e.log`, VersionId `AIVks2vssJ5y8yT5.WDeKbiJA0rsvngM`, SHA-256 `dba12538bda077a51f2de816196dfa54d285da47a4fbb8f6cde1d998e6794c5d`

The worker verified the exact AMI, driver, image digest, source labels, Torch
and dependency versions, then ran the four upstream RoboLab suites
`test_isaaclab`, `test_registered_envs`, `test_tasks_valid`, and
`test_run_empty`. All 128 tests passed (72 test dots through 56%, followed by
56 more through 100% in the quiet pytest output), the log records
`smoke_exit_code=0`, and EC2 entered `shutting-down` immediately after
completion and was later verified `terminated`. This is the first clean
camera-enabled evaluator smoke, not a policy-quality evaluation.

## Manual iteration log

1. Immediately after S3 creation, parallel `PutBucketEncryption` and `PutBucketTagging` calls returned `OperationAborted` because other bucket control-plane changes were still in flight. Both were repeated serially and verified. Replays use serial S3 control changes.
2. The first inline instance policy allowed ECR pull only. Review showed that the workbench must publish the pinned image, so repository-scoped `InitiateLayerUpload`, `UploadLayerPart`, `CompleteLayerUpload`, and `PutImage` were added and the inline policy was reapplied.
3. The initial SNS topic had zero consumers. An SSE-SQS queue and scoped topic subscription were added so email-free alarms are durable and inspectable.
4. The `Project` cost-allocation tag existed but was inactive. It was activated successfully for later reconciliation; AWS may take time to populate tag-filtered cost data.
5. The launch wrapper was exercised in read-only plan mode against the live account for a one-hour `g6e.4xlarge` workbench. It resolved the pinned AMI root device, `us-east-2a` offering, zero-ingress group, exact SSM profile, 1 TiB encrypted gp3 volume, stop deadline, and a `$3.7553` reservation including the 15-minute margin. No reservation was written and no EC2 instance was launched.
6. Pre-launch review found that worker and staging boundary checks call `GetBucketEncryption`, but the first inline role policy omitted `s3:GetEncryptionConfiguration`. The bucket-scoped read permission was added, the live inline policy was reapplied, and policy simulation plus a role-credential smoke check were required before the first paid launch.
7. The pinned training base image is hosted in AWS's `763104351884/pytorch-training` ECR repository. The first role policy scoped all repository reads to the project ECR, so exact-repository pull-only permissions were added for that AWS DLC source; project ECR remains the only repository with push permissions.
8. Container access to the instance role was unnecessary after host-side staging and syncing. The launch template's IMDS hop limit was reduced from two to one and ephemeral containers were placed on Docker's `none` network, leaving loopback available for same-container evaluation while preventing metadata or external AWS access.
9. The installed AWS CLI rejects `--expected-bucket-owner` on high-level `aws s3 cp/sync` commands. Owner/account/region/encryption are verified first with owner-aware `s3api` calls; small control objects use `s3api put-object`, while large multipart syncs omit the unsupported flag and are followed by manifest/version/hash verification.
10. The first paid workbench showed that a guest-only systemd deadline cannot bound spend if cloud-init fails. A dedicated EventBridge Scheduler role and one-time external `StopInstances`/`TerminateInstances` schedule were added; launch is considered successful only after that external deadline exists, with the guest timer retained as defense in depth.
11. The first SSM diagnostic used `set -o pipefail`, but `AWS-RunShellScript` invoked `/bin/sh`, which does not support that Bash option. Subsequent SSM diagnostics either use POSIX shell syntax or invoke Bash explicitly.
12. The next diagnostic embedded Docker's Go-template braces through multiple JSON/shell quoting layers and produced an invalid `docker info --format` expression. The replay now uses plain `docker info` unless a command is supplied through a file.
13. Live `lsblk`, `findmnt`, and `df` evidence confirmed that the pinned DLAMI preformats its 600 GB instance store as LVM/ext4 and mounts it at `/opt/dlami/nvme`. Worker discovery was changed to accept only that specifically verified layout and to refuse reformatting any mounted or otherwise initialized device.
14. The first pinned Hugging Face staging environment installed `huggingface-hub==0.33.1` successfully, but that version did not provide the newer `hf` executable. No dataset payload had transferred. The command was corrected to call `huggingface_hub.snapshot_download` directly with the exact repository revision and local directory; replays use the Python API rather than assuming a CLI entry point.
15. A raw AWS DLC GPU smoke appeared to hang because the DLC defines `bash -m dockerd_entrypoint.sh` as its entrypoint; appending `python -c ...` did not override it. The diagnostic container was cancelled and exited cleanly. Replaying with `--entrypoint python` proved Torch 2.7.1+cu128, CUDA 12.8, L40S availability, and compute capability 8.9. The reproduction Dockerfiles deliberately set `ENTRYPOINT []`, so their normal commands do not inherit this behavior.
16. Parallel unauthenticated Xet downloads used 16 DROID workers plus 8 LIBERO workers and eventually received HTTP 429 responses from Hugging Face's per-file read-token endpoint. The partial snapshots remained resumable on persistent EBS. Replays serialize the two repositories with `max_workers=1` (increasing only after observed headroom), validate the final exact commit, and never delete a partial merely because a rate limit interrupted it.
17. To overlap public GCS teacher transfer with the final local review, the two staging scripts and reproduction config were uploaded as three AES256-encrypted, versioned manual-control objects before use. `repro_stage_data.py` is VersionId `zlXnKEjbimyEOR4FgN6bXeNj.OuuSPvN`, SHA-256 `a055f508787c010e97ba9a6abe535e84aa8a00a14214e9703553e3afd0930a0f`; `repro_stage_checkpoints.py` is VersionId `iEVUbEBJcbhJ5PriMEUhBxSHAMoZ7QWJ`, SHA-256 `c473c8e561587c297e735c07bc76870ce178ba009e1abb5dbeb3ee01c98614d4`; and `reproduction.json` is VersionId `D8H8r5a.eXlfv_YirtYC9EgHk5P4zx9P`, SHA-256 `ed9b3f81de4a93660b4d5bad5ef28571db8e8bd3e58e2e1b09bd9fbb6c905395`. The workbench verified all three before running the download action. These pre-commit control versions are bootstrap evidence, not the final source artifact; the eventual committed git bundle must reproduce or supersede them before training.
18. The pinned RoboLab Git checkout reached commit `0aef241fb088ca21bb4ebd24448940ed56620d17`, but the base AMI did not include the `git-lfs` executable needed for its large simulator assets. The same checkout was retained; `git-lfs` was installed from Ubuntu packages, `git lfs install --local` and `git lfs pull` were run in place, followed by exact-HEAD and `git lfs fsck` verification. No instance replacement or fresh clone was needed.
19. Both released teachers were downloaded from their pinned public GCS prefixes, checked against complete file inventories, and uploaded to versioned S3 before conversion. LIBERO contains 16 files and 12,439,085,481 bytes; its manifest SHA-256 is `9140fa118b1a2b627726519cb3d21a0a98f2b1b736b5909a49520fc75d8dd8ad`, its source revision is the inventory hash `b00d25ec1a1284656ccfd0cf00597fced40fa20c9c7c39ebfdf256db6e844fb7`, and its S3 manifest VersionId is `oX5OL_hTQDoYmYD7bTZ.sM7.4KxB5FX3`. The DROID joint-position teacher contains 26 files and 12,435,136,033 bytes; its manifest SHA-256 is `64e4082767ac652d35828f721ca0906bd9a97f78a769a4bf4f75b09837d5bf46`, its source revision is the inventory hash `6487c08461e26cac570a2781f477474e6573c7a6e0a4ba93a9f0efb146c2db5b`, and its S3 manifest VersionId is `etUbiXvb8B6C7ltGXEfmrB96kJxI18HC`. Replays must use the version-pinned manifests and must not substitute the generic DROID teacher for `pi05_droid_jointpos`.
20. RoboLab was built from commit `0aef241fb088ca21bb4ebd24448940ed56620d17` with client commit `aa6420561529593114160d05e5ad155792b272f3` on the resolved Isaac Lab base `sha256:b4d8e96cbfb9a6c40067bec6cc5ee180e36d4c0164b25f7215c5f47e31897b94`. The first runtime check incorrectly queried `importlib.metadata` for release versions: the image's internal `isaaclab` Python distribution reports `0.44.8`, and bundled Isaac Sim has no `isaacsim` distribution metadata. The replay gate now reads `/workspace/isaaclab/VERSION` (`2.2.0`) and `/workspace/isaaclab/_isaac_sim/VERSION` (`5.0.0-rc.45+release.23960.184afb15.gl`) directly before running the GPU task tests. The image was retained and the corrected check was rerun; no instance replacement was needed.

21. A pre-launch artifact trace found that the first Shallow worker example staged only the dataset even though its config also reads the released JAX normalization assets and converted PyTorch teacher. Staging uploads now emit copy-ready `worker_artifact` objects, the example includes all three inputs, and converted teachers receive distinct content/provenance revisions and S3 prefixes.
22. Numeric training checkpoints were durably uploaded but originally had no manifest in the schema enforced for later worker inputs. Declared checkpoint/artifact outputs can now set `publish_destination`; only after their S3 receipts exist does the worker upload a worker-input manifest and record a complete copy-ready descriptor in `run-manifest.json.published_inputs`. This is the required Shallow-to-SnapFlow and model-to-evaluation handoff.
23. Container review found that the editable project install resolved normal dependencies independently of `uv.lock`, and that generated `data`, `checkpoints`, `runs`, calibration, and engine directories could enter a later Docker context. The training Dockerfile now exports from the frozen lock before installing the local packages without dependency resolution, and `.dockerignore` excludes those generated trees. Replays require a clean checkout before assigning the OCI source-revision label.
24. Early manual downloads used `/opt/pi05`, while the reviewed container and worker interface consistently uses `/mnt/openpi`. After verifying `/mnt/openpi` did not exist and `/opt/pi05` was on the persistent 1 TiB root volume, the workbench added the single compatibility symlink `/mnt/openpi -> /opt/pi05`. Container bind sources and manual commands now use `/mnt/openpi`; ephemeral workers create that path directly and do not need the symlink.
25. The base AMI `ami-01901bc01d5d9bb55` exposes NVIDIA driver `595.71.05`. An untouched Isaac Lab 2.2.0 base reproduced the NVIDIA-known Isaac Sim RTX startup crash with `enable_cameras=True`, while otherwise identical non-camera startup succeeded. This isolates the failure to the camera/RTX driver path rather than the RoboLab or policy changes. Evaluation launches are therefore workload-pinned to Amazon AMI `ami-06517bc7fad3c6a48`, whose AWS release notes specify Ubuntu 22.04, NVIDIA driver `580.126.09`, and G6e/G7e support.
26. RoboLab's editable install upgraded `typing-extensions` to 4.16.0 and selected `typeguard` 4.6.0. Isaac Lab's bundled Torch 2.7.0 could no longer import `torch._dynamo`, while downgrading only `typing-extensions` made the typeguard pytest plugin fail. The same source checkout and base layers were retained; `typeguard==4.4.2` and `typing_extensions==4.12.2` were installed together without dependency resolution, and the Docker build now proves both import paths. The corrected immutable image digest is `sha256:2d17c15e62887c9fc8b4c41b7ee3d39c4c187348eb55b4273fd24e785a3325e7`.
27. After publishing that image, a host-side `ecr:DescribeImages` verification failed because the scoped instance role supported push/pull but omitted this read-only inventory action. `ecr:DescribeImages` was added only for the project repository, the live inline policy was reapplied, IAM simulation returned `allowed`, and the same workbench verified the immutable digest. No instance or image rebuild was needed.
28. The first paid R580 evaluator used reservation `96c5e984-1279-42c5-b795-ba7699683422` and instance `i-011eb2c219aea0e3e`. It observed driver `580.126.09`, pulled the exact evaluator digest, passed all 128 tests across the four camera/task smoke suites, uploaded its AES-256 encrypted versioned log with `smoke_exit_code=0`, and entered termination immediately. The external deadline remained independent protection but did not have to fire. The log VersionId and hash are in the evaluation-smoke section above.
29. The minimal Hugging Face staging virtual environment did not include the `polars` dependency needed for the DROID parquet-schema gate. The workbench retained the completed snapshot and installed the lock-pinned `polars==1.30.0` wheel into that environment. Replays use the committed reproduction image, whose frozen lock already includes this version; a separate ad hoc validation environment must install the same pin before running `repro_stage_data.py validate`.
30. The first real MolmoAct2 validation correctly stopped before hashing because the fixture-derived gate expected the two video feature names to be physical parquet columns. Inspection of the exact pinned snapshot showed the non-video policy inputs in `data/chunk-000/file-000.parquet`, while LeRobot v3 stores the declared video features under `videos/{video_key}/chunk-{chunk_index:03d}/file-{file_index:03d}.mp4`. Iteration 2 (SHA-256 `34dd04cbf38d43c9c8ff89e04182e84d64733fcba8733a1bdf97fd6945bb00f6`, VersionId `jm0L2TIJ4vT6Jjdxw5EyGkm6eOvUryFu`) was superseded when review found that subtree presence alone did not prove exact episode-derived media coverage; its in-progress hash was cancelled before it emitted a manifest and the object version is tagged `Status=superseded`. An earlier byte-identical VersionId `qauY1079BttAHh1T9ETK563Jr0UB4oFa` accidentally carried placeholder SHA metadata, was never executed, and is tagged `Status=invalid`. The accepted iteration 3 derives every expected parquet and MP4 path from the pinned episode metadata, rejects missing/orphan/empty media, and pins the observed 518 exterior-left and 316 wrist-left files. Its script SHA-256 is `f09196890691a877a66091d2ba298c9a2d3f702ca84dba0f56d5e5be9fec07a3`, staged at `manual/staging/iteration-03/scripts/repro_stage_data.py`, VersionId `.o4gDDzbSV3HiEMDWHZz3f0Mt6ZIgblC`. Replays use that approved version or the superseding committed source.
31. The workbench's 1 TiB gp3 root volume was provisioned at the 125 MB/s baseline, making each full 259 GB integrity pass approximately 35 minutes from bytes divided by throughput. The same attached volume `vol-0de00b022d7154c96` was increased in place to 500 MB/s at `2026-08-04T01:29:43Z`; no data, filesystem, instance, IOPS, or volume size changed. The maximum incremental provisioned-throughput cost is about `$15` for an entire month and remains inside the `$150` non-compute reserve; the volume is terminated with the workbench after durable upload rather than left at the higher setting.
32. The serialized exact-revision LIBERO resume completed without another rate limit. Validation found 1,693 episodes, 273,465 frames, 40 tasks, 1,699 payload files, and exactly 34,938,927,454 payload bytes. The upload revalidated and rehashed the local tree, synchronized all 1,699 files under the immutable revision prefix, and produced manifest SHA-256 `a195fab74ceb43dcc50438f3401f8174a8c477f90f47385ec806048c04537f5f` at S3 VersionId `30NitaqVRt8BpxA4TAzzxG27icirmyLF`. A version-specific `GetObject` reproduced that hash and source metadata. This is the copy-ready LIBERO dataset input for worker specs.
33. Accepted DROID iteration 3 validated 74,604 episodes, 17,758,044 frames, 49,623 tasks, 50 parquet shards, 834 episode-referenced MP4 files, 1,414 total payload files, and exactly 258,712,592,431 payload bytes. It then rehashed and synchronized the same retained tree to the immutable revision prefix. The final manifest SHA-256 is `04fd47ec9acc3d211e3ed16e3f44b7f6c73c7b82ca22a98da017f37493854326`, VersionId `T0MmQScu_e_I_.n824j5XWXHFzUr6n8_`. A version-specific encrypted `GetObject` reproduced the hash and source revision; an independent S3 listing reproduced all 1,414 payload objects and the exact byte total. This is the copy-ready DROID dataset input for worker specs.
34. The existing workbench pulled the pinned `nvcr.io/nvidia/tensorrt@sha256:7cd94ee931d2b5b85ad1c5af723d485b2625f6ce167e1e4abe577850b96ceac3` base without starting another instance. A network-disabled GPU smoke observed TensorRT `11.0.0.114`, an L40S, host driver `595.71.05`, and NVIDIA's CUDA 13.3 forward-compatibility user-mode driver `610.43.02`. The first inspection command lost Docker Go-template quoting through SSM; the retained image was inspected with `docker image inspect | jq` instead. A second smoke proved that TensorRT 11's `trtexec --version` prints its version but exits 1 with `Model missing or format not recognized`; the compiler Dockerfiles now use the TensorRT Python version assertion, `command -v trtexec`, and the successful `trtexec --help` build signature. No image or instance restart was needed.
35. The same retained image was tagged once as `tensorrt-base-26.06-7cd94ee931d2` and pushed to the immutable project ECR. Docker reported that NVIDIA digest `sha256:7cd94ee931d2b5b85ad1c5af723d485b2625f6ce167e1e4abe577850b96ceac3` is a multi-platform index and published only the locally resolved amd64 child, yielding account-local digest `sha256:2a5a0a9a32ec5ddc1c384c15ddcf3b89ddc4f8647e7ee7ae708d844210183a1e` and 6,244,295,363 compressed bytes. A fresh pull by that ECR digest, with networking disabled in the container, reproduced TensorRT `11.0.0.114`, the successful `trtexec` build banner, and CUDA forward compatibility. Compiler builds use this mirror digest, never the tag or external NGC reference.
36. Source review and the retained TensorRT base inspection consumed most of the original six-hour workbench window, so a bounded four-hour extension was reserved as `6a2673de-4dd7-4ab8-91e4-90f7882c19fe` for `$12.01696`; no instance was launched. The existing EventBridge Scheduler deadline was updated and read back as enabled at `2026-08-04T09:26:34Z`, with `ActionAfterCompletion=DELETE`, ten retries, and the same `StopInstances` target. The first guest-timer edit replaced the spaces in the systemd calendar value with periods and systemd correctly refused the invalid timer; the same instance was retained, the exact `OnCalendar=2026-08-04 09:26:34 UTC` line was restored, `daemon-reload` was run, and the active timer was verified to trigger at the same deadline. EC2 still reports instance-initiated shutdown behavior `stop`.
37. The final pre-bundle adversarial review found unsafe experiment-name checkpoint deletion, stale NVMe run reuse, container-visible worker control directories, a non-create-once compiled publisher using an unsupported high-level S3 owner flag, missing clean-checkout evidence, and a bare-host DROID compiled path. The reviewed source now rejects checkpoint escapes/symlink ancestors, uses unique fresh experiment and NVMe run IDs, mounts only payload roots, creates host evidence without overwrite, conditionally publishes compiled files with exact empty/history/version gates, binds the protected clean Git identity, and wraps every DROID phase in its digest-pinned v3 runtime. These were source corrections only; no AWS object or capacity mutation occurred.
38. The payload-only mount assumption was exercised on the retained workbench with the already-local account-mirrored TensorRT image. A UID-1000 network-disabled GPU container wrote through `/output/artifacts` while `/output/.ready` was absent from its namespace; the host read the expected probe and removed the exact temporary directory with empty-directory checks. The smoke printed `payload-only-mount-smoke=passed`; no instance was launched or replaced.
39. Pre-build inspection on the same workbench confirmed the mirrored TensorRT parent is Ubuntu 24.04/Python 3.12.3/TensorRT 11.0.0.114, the training DLC is Ubuntu 22.04/Python 3.12.10/Torch 2.7.1+cu128, the LIBERO apt dependencies exist, and ONNX Runtime GPU 1.28 installs in the actual DLC with the CUDA provider exposed. The compiler publication smoke now requires a no-CPU-fallback ONNX CUDA Add plus a one-iteration `trtexec` engine/inference before push, rather than accepting imports alone.
40. Reviewed source commit `81a359322e73e6f5765dcc705c06bc9b5111e9ce` was bundled with complete history into 939,155 bytes, SHA-256 `89b719d74e8457c68785dee090262a95aea460b68c6cb5132aa8f156c5cc0d3a`, and conditionally uploaded once to `s3://pi05-repro-752160877725-us-east-2/source/openpi.bundle`, VersionId `v83TLOupHVE3l6V_qFowJguvVEkQ0WB5`. A version-specific download reproduced the hash and the key had exactly one version with no delete marker. The first two remote verification attempts used `git bundle verify` outside a Git repository (first from the SSM working directory, then against `/opt/pi05/manual-repo`, which was not a repository) and failed before cloning. The retained workbench initialized an empty bare verification repository, verified the same bundle as complete, checked its HEAD, cloned it, checked out the exact detached commit, and proved the checkout clean.
41. The first committed LIBERO v2 policy-image build reached the frozen dependency install and stopped before image creation or ECR publication: OpenPI's lock selects simulator-only `gym-aloha==0.1.1`, whose `mujoco==2.3.7` dependency has no CPython 3.12 Linux wheel, so its source build demanded an unset `MUJOCO_PATH`. The training DLC is intentionally Python 3.12, while simulation is already isolated in the separately pinned LIBERO and RoboLab images. Replays use uv's exact-lock `--prune gym-aloha` operation, which was checked to remove only that unused simulator branch (nine packages, no additions or replacements), assert that Gym-Aloha and MuJoCo are absent, import both required distillation configs, and label the simulator runtime `external`; the frozen policy/training dependency set remains unchanged otherwise.
42. Pre-execution review found that the worker bootstrap repeated the out-of-repository `git bundle verify` error and that five TensorRT runbook label checks put backslash-escaped quotes inside single-quoted Docker Go templates. The bootstrap now creates a fresh root-owned bare verification repository, verifies the bundle there, checks bundle HEAD against the signed worker spec before cloning, and refuses stale reuse. The compiler commands now pass literal valid Go templates. These corrections were committed before any worker or compiler image publication.
43. The first source object was safely create-once, but its generic `source/openpi.bundle` key could not represent a corrected commit without either overwriting the current object or accumulating ambiguous versions. The repeatable source publisher now derives `source/openpi-$SOURCE_COMMIT.bundle`, requires empty version/delete history, uses `If-None-Match: *`, verifies the exact returned VersionId and metadata, requires exactly one final version with no delete marker, and round-trips that version's SHA-256. Worker specs copy the printed commit-specific URI and VersionId together.
44. The corrected policy-image build passed the frozen dependency, tokenizer, source-install, config-import, and version gates, then exposed an AWS DLC ABI mismatch at the former TorchCodec smoke. The pinned DLC's CPython 3.12.10 is a static build (`Py_ENABLE_SHARED=0`) with only `libpython3.12.a`; lock-pinned TorchCodec 0.4.0 attempts to load `libpython3.12.so.1.0` when using the available FFmpeg 4 libraries. Both exact LeRobot revisions support an explicit PyAV backend. Training now passes `video_backend="pyav"` rather than allowing package presence to select a broken default, the image retains its otherwise exact locked graph, and build plus digest smokes generate and decode a real local video through the LeRobot PyAV path. The OCI decoder label and worker identity gate bind that choice; later pilot throughput will measure whether it is an actual input bottleneck before any broader runtime change.
45. A pre-compiler runbook audit found that the combined TensorRT policy section required post-push GPU/import smokes but specified them only in prose and resolved only the DROID digest in its executable block. Before compiler execution, the runbook was completed with digest resolution for both tracks, exact architecture/RepoDigest/provenance/toolchain/runtime label gates, and network-disabled digest smokes for Torch CUDA, no-fallback ONNX Runtime CUDA, TensorRT builder creation, tokenizer/config loading, and the policy/WebSocket protocol stack. The LIBERO combined image remains only an intermediate parent; final LIBERO export and evaluation still require the later evaluator digest.
46. The first live v2 GPU smoke passed Torch CUDA, JAX CUDA, the generated PyAV decode, and runtime imports, then correctly failed the no-CPU-fallback ONNX Runtime gate before ECR publication. PyPI's ONNX Runtime GPU 1.28 wheel requires CUDA 13 (`libcublasLt.so.13`), while the training DLC is pinned to CUDA 12.8. The official ONNX Runtime CUDA matrix states that PyPI 1.27 and newer default to CUDA 13, while 1.26.x through 1.21.x use CUDA 12.8. The same built image was retained and a temporary container replaced only ORT with 1.26.0; the complete PyAV/Torch/JAX/no-fallback ORT CUDA smoke then passed on the L40S. The policy Dockerfile now pins 1.26.0 and binds it in OCI and worker identity; the TensorRT compiler's separate CUDA 13.3/ORT 1.24.2 environment is unchanged. Reference: https://onnxruntime.ai/docs/execution-providers/CUDA-ExecutionProvider.html#requirements
47. Source commit `9eaede81032b8cbeb2c2a8844c2386e4798fc352` produced both policy images without launching another instance. The DROID v3 build completed under SSM command `47a95ddc-641f-41b8-840a-c0123e3629b7` in 3m02s and passed the real PyAV decode plus exact LeRobot 0.4.3 dependency gate. Before publication, LIBERO v2 and DROID v3 each passed OCI-label checks and a network-disabled L40S smoke covering Torch CUDA 12.8, JAX CUDA, PyAV, the runtime-specific LeRobot import, and an ONNX CUDA Add with CPU fallback disabled. ECR returned `ImageNotFound` for both commit-qualified tags immediately before the one permitted push. The immutable LIBERO digest is `sha256:ed15a0c3bdb75180a8cc2a92f9b5f7231c7e868b299e8b244ed4c8d8b2899228` (13,130,271,069 compressed bytes); the DROID digest is `sha256:39b408afa5b489b11196754ef135caf38e5d0e73b0d63cee50e7e4f3f844668e` (13,228,434,802 bytes). SSM command `d40405f8-dd14-44d4-bf7c-0aaa4a7fd9b4` explicitly pulled both registry digests, verified their RepoDigests and complete provenance labels, and repeated both network-disabled GPU smokes successfully. These artifacts remain evidence for commit `9eaede8`; if a later source-contract fix changes the OCI revision, publish new commit-qualified tags rather than relabeling or overwriting them.
48. A read-only state audit found that the local conservative cost ledger included the four-hour workbench extension while the versioned S3 ledger still contained only the first two reservations. The two remote entries were byte-for-byte identical to the first two local entries, so the complete three-entry local document was published with `If-Match` against ETag `7aeef0a6f60687ac3bee45c2ec3dbb5c`. The accepted AES256/SHA256 object is VersionId `yH285_YCNG4Ia78TW3q1tOFE.57p97lR`, SHA-256 `b8bb0171b1d473cd47c74379e0a7084d7e86c4ea7fa42a1e25263e0807065d8c`, and contains three reservations totaling `$34.54876`; a current-object round trip reproduced the hash and version history contained no delete marker. Both independent workbench deadline layers stop rather than terminate the instance. After all required state is durable in S3, explicitly terminate `i-038112fe75c610517`; otherwise its stopped 1 TiB gp3 volume at 500 MB/s continues to accrue roughly `$3.19/day`.
49. The pre-conversion launch audit found that the documented `g7e.4xlarge` Shallow fallback could not run the two-process DDP command and that the generic corrective budget category could bypass a category-only hardware guard. The launcher now separates required workload identity from spend category, applies both hardware matrices, scopes AMI selection and lifecycle to the workload, and rejects Shallow on a one-GPU G7e even when corrective funds are used. The supported capacity fallback is the same `g7e.12xlarge` in the alternate pinned AZ. The audit also found that eager-base latency depended on fixed inputs emitted only by export; official base latency is now deferred to the retained G7e session, where exact commands run all five stages for both tracks and both summarizers on one instance.
50. Before teacher conversion, adversarial output/publication review found overwriteable framework reports, a non-create-once converted-checkpoint sync, nondeterministic converted manifests, unversioned converted payload retrieval, a source-mutation window during multipart upload, indiscriminate composite-manifest metrics ingestion, and missing top-level worker metrics/cost. The corrected comparison exclusively creates both evidence files before promotion. The converted publisher now uses a deterministic manifest, claim-first/manifest-last create-once protocol, per-part and streamed whole-file SHA-256 checks, stable file-identity checks, conditional completion, exact partial recovery, version-specific round trips, durable receipts, and mandatory per-payload VersionIds in worker artifacts. Workers stage those versions individually, reject converted artifacts without pins, ingest only command-bound schema-v1 metrics, publish hash-covered training diagnostics, and record projected cost without inventing actual billing. Corrective retained sessions are authorized by the resolved export workload rather than the spend category. No checkpoint conversion or training ran while these contracts were changing. The final integrated checks passed 361 script tests, 18 data-loader tests, Ruff lint/format, Python compilation, JSON parsing, shell syntax, and `git diff --check`.
51. Reviewed source commit `229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee` was bundled with complete history into 976,233 bytes, SHA-256 `bb5a5efa2d914de5ac223a9bf251082f7de03fd2c973d19117e92d708fb854be`, and conditionally created at `s3://pi05-repro-752160877725-us-east-2/source/openpi-229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee.bundle`, VersionId `HY8r1VZTuShbxIAknhQgVyM6pVnWm9uk`. Version-specific download reproduced the hash, and the key has exactly one version with no delete marker. SSM command `6d0628cf-62c1-4574-9ced-a8b4b8c4c50f` verified the complete bundle in a new bare repository, cloned it to `/opt/pi05/source/openpi-229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee`, checked out the exact detached commit, and proved the checkout clean. The first local preflight used zsh's read-only `history` variable and stopped before any upload; the identical command was retained and replayed under Bash with a task-specific variable name. Replays use Bash and the committed publisher command.
52. The retained workbench built the final 229c policy images without launching capacity. LIBERO build command `08954174-2c86-42dd-b538-42669defff0c` produced local image ID `sha256:d76e6d73fca409e998304a6a8997f80fab1252fe0301d667a072f99dd6624f24`; DROID build command `abf33b96-ff56-4edd-9b4f-4bb371c0e24a` completed in 6m36s and produced `sha256:2afcc58cda27681892c7bbb9554e9603024c5b74f53358fad893ea876374803c`. Both passed exact dependency/config/PyAV build gates. SSM command `175f12b2-fbae-432f-aacb-292e743c43d7` then passed every OCI provenance label and, with networking disabled on the L40S, proved Torch CUDA 12.8, JAX CUDA, runtime-specific LeRobot imports, actual PyAV decode, and a no-CPU-fallback ONNX Runtime 1.26.0 CUDA Add for both images. ECR returned exactly two `ImageNotFound` failures and zero images immediately before publication. Command `b10b2d70-10cc-4388-bc0a-062adb15f450` performed the one permitted push to immutable commit-qualified tags. LIBERO is `752160877725.dkr.ecr.us-east-2.amazonaws.com/pi05-repro@sha256:d76e6d73fca409e998304a6a8997f80fab1252fe0301d667a072f99dd6624f24` (13,130,306,741 compressed bytes), and DROID is `752160877725.dkr.ecr.us-east-2.amazonaws.com/pi05-repro@sha256:2afcc58cda27681892c7bbb9554e9603024c5b74f53358fad893ea876374803c` (13,228,460,172 bytes). Command `991317a9-392a-4714-818f-2d0082fa2e8c` pulled both by digest, verified RepoDigests and labels, and repeated all network-disabled GPU smokes successfully. These two digest URIs and literal source commit `229c08e` are the conversion provenance even if later runbook-only commits move branch HEAD.
53. A read-only conversion-state probe first reused zsh's read-only `parameters` name locally, then sent `set -o pipefail` to the POSIX `/bin/sh` used by `AWS-RunShellScript`; neither attempt mutated checkpoint/evidence state. The retained instance was not replaced. The replay used task-specific local names and POSIX `set -eu` (or, for Bash-only runbook functions, an explicitly invoked `/bin/bash`). Command `7bcd276e-7f21-4d95-a393-402c7f941a6e` then proved both converted output directories and `/mnt/openpi/evidence` absent before first creation, with 473 GiB free. Future SSM commands carry Bash-only scripts as an encoded payload explicitly piped to `/bin/bash` rather than relying on the document's default shell.
54. LIBERO conversion command `261b2f84-ebaa-4dfa-b91d-e69ae7fc4e1a` restored the released JAX teacher and successfully wrote the BF16 PyTorch model before its attached host validation reached `python: command not found`; the DLAMI provides `python3` but no `python` alias. The output was preserved rather than reconverted. Command `127c3b55-e969-4fc9-ba90-6a2f322c17e6` validated those exact retained bytes with `python3`: 3 files / 7,473,093,598 bytes, converted revision `c73bb6ff5cbaa3c7bba5f03ea38c22bd95e8274308285e2f17b6ed2d73688dd0`, manifest SHA-256 `b1eb42ac73351d749587e3c3fd1667bc140610819e73408099d39b262cd08daa`. DROID command `10a751a7-6e99-49c2-8e9b-135023f0ac85` completed conversion and validation in 1m51s: 4 files / 7,473,096,232 bytes, revision `b4e9dcd2767b497b707d912b708729a9edd5c91bcbf402f542cd682b32c943b7`, manifest SHA-256 `ed611e4814897b84b0f138e76eed3b9caaa05ec108632a993652a69910d0b78f`. The top-level staging runbook now invokes `python3`; model commands inside the pinned images continue to use their image-provided `python`.
55. Golden-corpus commands `34fb92d2-0f01-4047-872c-a603f68ffcaa` and `ed105e0f-2e2e-425d-b01f-3ece6a1e5714` created the only canonical 64-sample corpora with split seed 42. LIBERO seed 7001 produced NPZ SHA-256 `6135b50c385431dd31fced9f44afb60a7c39bd938c5ab92e2737771685515d68` and sidecar SHA-256 `426ddd82bbb40fb25e196e6a46c54b5c0c80351845ee76acd966ea13d9f86cf3`; DROID seed 7002 produced `312525ac495fac6586412a295e7f780c3de9c0ab94c655f331cd01d718ce9c60` and `e4c779a68678b562aa5f7020eb7381ec9652468bb21dd94b6d8cf8c7ed3b8bb0`. Framework command `d40aa528-57c9-4189-ab28-a51b64e44967` passed LIBERO on every sample (minimum cosine 0.9997032287, mean 0.9999744359, MSE 0.0000642574); report SHA-256 is `548d911292ba6ba8d798adf494cc1f45f3a574cbf5dda5e0c18cb8d7d0449265` and velocity NPZ SHA-256 is `5d8426da770d6749e5fd67190dcdc0e03e546644d84412c7f8132f850af0ccc5`. Command `f138aa48-6f98-4934-9517-04d19d397aa7` passed DROID (minimum 0.9997763989, mean 0.9999911080, MSE 0.0000524981); report SHA-256 is `a62efa1a869bfe053bf6adca575186e3f3a5de27cbd4c8d0546437545d2f39a9` and velocity NPZ SHA-256 is `a66b9117daae76c17ee44be8c8b244ac998035a1099f11d58fa5abdcf51f07ee`. A first local command renderer expanded `${corpus}` before submitting SSM and made no AWS call; its corrected SSM invocation then stopped before validation because `$teacher_pytorch` was an unintended shell variable. The next exact invocation reached the validator and, before any S3 call, exposed that container-recorded `/mnt/openpi/...` paths were compared textually with host-resolved `/opt/pi05/...` paths even though `/mnt/openpi` is the verified compatibility symlink. The validator now accepts an absolute report path only when strict resolution names the exact validated regular file; it still rejects missing paths, different files, final input symlinks, changed hashes, and extra provenance fields. Focused tests include both the parent-alias acceptance and wrong-file rejection.
56. The path-identity correction and its tests were committed as control-plane commit `28076005d0331df565aa08b23292caa6ffa2cf90`; model, image, corpus, and conversion provenance deliberately remain `229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee`. Its complete 980,495-byte controller bundle has SHA-256 `f386ec2c3d1eb7a0126a83834dda79364bbcf825b3a9cd8bfa776934204e1518` and was create-once published at `source/openpi-28076005d0331df565aa08b23292caa6ffa2cf90.bundle`, VersionId `K8yDppJJfGb6hqj6dTe6LAn4O6cp4bs4`; command `4649e74f-8707-4b0b-8c91-46036b107df0` verified and cloned it cleanly. Command `87f17c51-5388-48e4-87c3-b966dbeeaa68` then passed both local evidence validators and both mutation-free upload plans. LIBERO evidence revision `2207208d27ffa46b9ba4087927355c1d7079d22a143f2350358e6fc332c24698` was create-once published by `d7c188b0-86b4-4fde-a669-3f34295b0041`; its manifest SHA-256 is `6ee67609207815150b4508d8f3664850705500e09ddf41d4af88442ed7daf4d4`, VersionId `XoGHWe7CHXfTAaSnaw9mHhzubnWpELQV`, with golden NPZ/sidecar/report/velocity VersionIds `.Vz6VEMpBieU0hpvrftb1pO2kR3xjrFg`, `VVKvEIs.Xd0OjVPOqCtHEae8Xo57rf1r`, `.TrX8fNhUDBR64ASNfUsf0E6OA4AjHaO`, and `JQJQF8QxnUiAknebXFGAVYeIyEyzSB5G`. DROID evidence revision `d8f44f39eaba43a1f5232bbf8dc57c5802b7278593b99062b7ccaaa08a86dda0` was published by `9bae9eb2-b631-4051-8dd2-676b6b1c96e8`; its manifest SHA-256 is `8a0d662814b08ec27218c69e6a97dab0932d9537bd4d57b6b427358239cd2b0c`, VersionId `4UHgPDXA0X5rJLfhjfFZDTpGno0sggr0`, with payload VersionIds `nmaVVHWiwdQAkfMEV2wMD1so4KRmwXpM`, `pQaw65EyYogv.9gNNU_3oYXRAfC7svPV`, `TUoGHRNRGGU0Sgy7q9spiot.qXTObga6`, and `2.x4LFGIlYhU2IqSTM93weSl_kUe5WZz`. Both publishers wrote AES256/SHA256 objects and the version-pinned manifest last.
57. Pre-publication execution found the converted-teacher uploader had the same textual velocity-path assumption after its otherwise successful 229c validation and dry-run. The upload had not started, so no partial converted prefix existed. The uploader now resolves the report path strictly to the adjacent validated velocity file and retains its hash gate; the wrong-file case remains rejected. The complete 981,875-byte reviewed controller at commit `effa87db4506b9881422778e3503df3a67aed930` has bundle SHA-256 `952cba72ae743962b41842d19d03298fc8d0de6e31b8dca1fda2b071f0abef9b`, S3 VersionId `RJmM6ECMjqw590vSJWi2q3_FCtwaghGe`; command `2a239793-bc61-4979-bc66-66fb510ebfe7` verified its complete history and clean checkout. Command `12ae9846-e514-45f3-aa15-f1a475b22d12` executed that reviewed controller from the clean 229c model checkout, revalidated both converted trees, and emitted two mutation-free plans. LIBERO publisher `5ad84e0b-533e-440c-af86-2a4b78463d5f` completed in 3m45s with manifest VersionId `2lhXK.lU9urPfUKPftPS._nqx_fFyTZa`; payload VersionIds are `lovWxnfPjGXaumqqymRFSooyROWg0QqA` (`norm_stats.json`), `xkFsFoARHCXyKpuweYp7sSckoQcN24Ef` (`config.json`), and `CSsniON0z0hrMv7LrnLO0v9qnPlxYWGw` (`model.safetensors`). DROID publisher `c4a2fe86-e33f-4119-b649-53b0b7ee8231` completed in 4m06s with manifest VersionId `xgmhHet70zLej9LpoJ9bWROwVYvFj5ow`; payload VersionIds are `fr9dSF.rlbM4swMPpwqzHNWOuWnQZIN_` (`norm_stats.json`), `OlJuBIA6Pv9y_lVWsI1aWaPXoF9uqhzN` (`droid.lock`), `zG8hGH6Hy2jwU8F.9Ld2st5t76HOUELc` (`config.json`), and `.mJzDLYOwUQvdlE9ORGR5T8OEsPqMuKW` (`model.safetensors`). Both multipart models were downloaded again by exact VersionId and reproduced their local full SHA-256 before receipt and manifest publication. The emitted `worker_artifact` objects, not hand-built prefixes, are mandatory inputs for Shallow workers.
58. The retained workbench deadline was extended in place by exactly three hours for the two baseline replays and durable evidence publication; no replacement instance was launched. Reservation `cfa03ba8-2a09-42c0-9b28-3f7f881c554f` adds `$9.01272`, bringing the conservative ledger total to `$43.56148`. The complete four-entry ledger is S3 VersionId `WwdchX.Da46XNc5.cVFjkU7.qqryrA7h`, SHA-256 `13eb67119d0261a58f52a2b1633e125b2ff8e47214095c70807f96e88c316db9`. Both the external EventBridge Scheduler deadline and guest systemd timer were read back at `2026-08-04T12:26:34Z`; the external action remains `StopInstances`, so the 1 TiB volume must still be explicitly terminated after all state is durable.
59. The first exact-229c LIBERO evaluator image, `sha256:a3e458626c1f0bae067c96f7fdbc08435b1d2e3ddebb77de9d1349566c97e5d7` (15,580,963,582 compressed bytes), passed provenance, CUDA, and real EGL rendering. Manual attempt 01 stopped before server readiness because UID 1000 had no passwd entry and Torch's compiler cache called `getpass`; the same retained workbench was reused with explicit `USER`, `LOGNAME`, and `TORCHINDUCTOR_CACHE_DIR`. Attempt 02 then exposed that Docker `--network none` does not provide hostname resolution for the container hostname; replays use a fixed `--hostname pi05-libero --add-host pi05-libero:127.0.0.1` while retaining the network-none boundary. Neither failure required rebuilding infrastructure or replacing the instance.
60. Attempt 03 completed the first ten LIBERO episodes at 90% success but the evaluator process remained alive after its final summary. Inspection of the retained container found the main thread waiting on a futex with 18 threads still present; the `WebsocketClientPolicy` connection was never closed. Source commit `e30480a6de404c74a996863c4fde89367350cf70` adds an explicit idempotent client close and calls it after normal LIBERO completion. Fifteen focused tests and Ruff passed. Its complete 982,931-byte bundle has SHA-256 `12f2d627e63d80631bd74b78ca3848d9a054009088867377f2cdf64493f4f9be`, S3 VersionId `D0n4i9oLFVZqpJ8JTQpMPW891l8_llI8`, and was verified into a clean detached checkout on the workbench.
61. The corrected evaluator was create-once published as `752160877725.dkr.ecr.us-east-2.amazonaws.com/pi05-repro@sha256:51b352c1a7205d6bdae668f99060ebd05049042e1d89916993830acbdc63b374` (15,581,813,763 compressed bytes), with evaluator source `e30480a`, parent policy image `sha256:d76e6d73fca409e998304a6a8997f80fab1252fe0301d667a072f99dd6624f24`, eager backend, and exact LIBERO/LeRobot dependency labels. A digest-pulled, network-disabled L40S smoke passed CUDA, EGL, and the explicit client-close path. Attempt 04 then stopped before Docker because its wrapper passed three operands to `test ! -e`; the wrapper now performs three individual path checks. The empty output directory is retained as failure evidence and no instance was replaced.
62. Attempt 05 (`0ed5d724-31f8-4267-ade9-37ea75875efb`) exercised all four suites through the corrected close path and returned SSM `Success`. Its 40 one-trial-per-task episodes contain 39 successes (97.5%) and zero infrastructure errors: spatial 9/10, object 10/10, goal 10/10, and LIBERO-10 10/10. The run began `2026-08-04T08:21:08Z`, ended `2026-08-04T08:31:22Z`, recorded evaluator source `e30480a`, exact evaluator digest `sha256:51b352c...`, converted model revision `c73bb6ff...`, instance `i-038112fe75c610517`/`g6e.4xlarge`, and five content-hashed JSONL artifacts. This is a baseline smoke and promotion gate, not the later official 2,000-episode result.
63. The independent clean replay, attempt 06, ran under SSM command `eb79004d-7b3d-43a0-9d62-46cb663bc255` from `2026-08-04T08:32:26Z` through `08:42:34Z` and returned `Success`/0 without an intervening edit. It independently reproduced 39/40 successes (97.5%), the same 9/10 spatial and 10/10 results in each other suite, and zero infrastructure errors. Its manifest SHA-256 is `667962515bbb4bb9c14dda51dc0e479200ace8abf0ee81631c860806aeb28572`; timing SHA-256 is `45566bcf173ee1ccdc9511a8083aca544675b8662caefbfb8e432ec814820461`; replay log SHA-256 is `694deb0339e5f6384e50a97ac9bd680eba15bda8a07f812c57272cab1868d9e8`. The identical success counts across two fresh compiler caches establish the eager base as the paired local baseline and satisfy the gate for the bounded 300-step Shallow optimizer smoke.
64. After that gate passed, the reviewed 300-step LIBERO overfit controls were create-once published without launching capacity. `bootstrap/worker-bootstrap.sh` is VersionId `kHhL9j17nVdYgr3.Rott61WffK2TsgwD`, SHA-256 `8770108f5cbe662c8fc0bd9bf09c4c1820162ea7eb5c54e29d31880bfdc77052`; `specs/libero-shallow-overfit-20260804t080000z-a1-300.json` is VersionId `dqN3Bbz1fbV.WdEd_hSVsGcEet1_SrJX`, SHA-256 `7c8cc3fed42f56b37134d5b82196df16475f19609497f2e1997a7d00c05f8103`. Each key had exactly one version and no delete marker, and both exact-version heads reported AES256 plus the expected 229c source metadata. The execution-enabled bootstrap passed `bash -n`, its SHA-256 `d104154861e58c8ce991c79818dd7229ed411aecf008d554b6a4b3fc34373f00` matched the read-only launcher plan, and 80 focused launcher/worker tests passed before launch.
65. Reservation `7fa19409-dc47-43f6-ba65-13c832189e4f` launched one On-Demand `g7e.12xlarge`, instance `i-0eb2882dc5f80b010`, in `us-east-2a` for the 300-step gate. It reserves 4.25 billed hours / `$35.21584`; the conservative five-entry ledger now totals `$78.77732`, with S3 VersionId `ZvV1YyDYWbzzYi6EhjkPjCING9Py5nP3` and confirmed exact-version SHA-256 `6963bc63f692a72036e6708e0914312763d1c6d9f2c518798c5cb7b0ce9a8b52`. Live EC2 inspection confirmed ordinary On-Demand lifecycle, `CapacityReservationPreference=none`, two 97,887 MiB RTX PRO 6000 Blackwell GPUs, IMDSv2 required/hop-limit one, encrypted delete-on-termination root, and instance-initiated termination. The independent external schedule `pi05-deadline-7fa19409-dc47-43f6-ba65-13c832189e4f` targets `TerminateInstances` at `2026-08-04T12:44:41Z`.
66. That first 300-step attempt did not reach Python training. It validated and staged all 1,699 LIBERO files / 34,938,927,454 bytes, all 16 original-teacher files / 12,439,085,481 bytes, all three converted-teacher files / 7,473,093,598 bytes, and the exact policy image, then Docker exited 125 while creating the container. The generic worker bind-mounted the immutable source checkout read-only at `/workspace/openpi` and also requested a nested output bind at absent `/workspace/openpi/checkpoints`; runc could not create that mountpoint inside the read-only parent. The worker uploaded a complete terminal failure record before instance termination: log VersionId `Di.teBQaUTMalFEJ4OY8HHgYu2l7qD6S`, SHA-256 `4bb945116250532e9b8ea5d65d91f83075953e7b90856d238326aad3e4beb37f`; failed run-manifest VersionId `2QKXFUh_3zoW9MYA57sMplxvrRK.jn7_`, SHA-256 `21ddafd0629ff1236eab869db930b792cd48df2f7d7deaa6f58d181bab51abe5`; final-sync evidence VersionId `PdcDoB0nsvy7p15Hyd6U8n10gdRDTWvg`, SHA-256 `68dbf35a51e6cc8dda9885cd55868bebc44a9d843606c63e61a3c5099f4a3620`. No checkpoint exists and the instance terminated. Replays must remove the redundant nested source-tree bind in a reviewed controller while leaving model source/image provenance at 229c; never mutate the clean 229c checkout to manufacture the missing directory.
67. The two accepted eager LIBERO smokes were then sealed create-once by controller commit `3e8316a58dc714923de0404028811abde05711f2`. Its complete 1,009,563-byte bundle has SHA-256 `df8d8a65cb99f99f368b2ee0ecbed75a1d2ea324711464b002066dd9f62b200f`, S3 VersionId `2a2y_mFi7SXiYqzSiJGYOe4yPokgcAcj`, and SSM command `8eb5cc6f-9c71-4f50-b4fa-3bf8614ed501` verified a clean detached checkout. Publication command `8e20da47-d057-47ae-9cde-d7b7f7464bc0` wrote exactly 11 AES256/SHA256 objects for each run. Attempt 05 evidence revision is `8e5acd0b20ebb8cd35c57ab6de2b5dda26e239380049bbc3e1004f81e4cc5ed4`; its manifest is SHA-256 `5466047eb7f07aead181c922b9a8a6b2f4e0b74b4facdb45b86e94f7634fd8ec`, VersionId `4XpNWiVUIbri89EvDOlXHwCb0bE4yDGG`, and its S3 publication receipt is SHA-256 `e6c2742dcf096ebbde5ee0e5bbc421c7f28994506b71a94f5bfb9ee0fd304a50`, VersionId `xxtPS0qijQ0Ls.TzvC4QAEFnvxc6LQCG` (complete local publisher receipt SHA-256 `f6dba4628673038874e2f1e2addefb1b1dd37473b98476e70cd0316e643d9de7`). Attempt 06 evidence revision is `47354479aebaec0afeb93db3f8ff117b424bf7d58ed5ada1b9ee333fabf27d24`; its manifest is SHA-256 `b817d4e7aac07b92df05ddbb5dce706ab0c0c58a879832e3407c3b680cd5c81a`, VersionId `xu5IyCkYKR43x2orou5.16NcaoRdhsx1`, and its S3 publication receipt is SHA-256 `ad65e1e5acdf034eb028f7c2c29fbd534d171826392f49353441b998114ea42e`, VersionId `PkTegk3k3to4sL1nCV4JwbLAnEuv.rK9` (complete local publisher receipt SHA-256 `13c4328271084096ff5e0a134f8b59bef25e4a4083b67945267a1a765df65883`). Exact-VersionId downloads reproduced both manifest hashes; each prefix has exactly one current version of each of its 11 keys, no delete marker, and no incomplete multipart upload. Both manifests pin the pre-training cost ledger at VersionId `WwdchX.Da46XNc5.cVFjkU7.qqryrA7h`, SHA-256 `13eb67119d0261a58f52a2b1633e125b2ff8e47214095c70807f96e88c316db9`.
68. Controller commit `daf488de08a5e4894c6d581afdd585a99a9fa99c` fixes the failed worker without changing the 229c model/image provenance. It makes model and controller sources independently pinned, runs host orchestration from the controller checkout, removes the nested `/workspace/openpi/checkpoints` mount, pre-creates and validates empty mountpoints for the two intentional children of read-only `/mnt/openpi`, requires exactly one `--checkpoint-base-dir /mnt/openpi/runs`, and recognizes the real `torchrun` argv when sealing mandatory training metrics. A 1,020,930-byte bundle (SHA-256 `ca5e75dce2b0f8b40789887d9806fa8d2caa20e2e302c1a10f6cc04f26182528`, VersionId `U5zC2lK7IcmMNs3ZLT8vO.XxVWJ4fiRR`) and its 8,527-byte bootstrap (SHA-256 `f610578f4a8e51f8b9d693a9a62c19a24e025b4dd82420df7f2486b5d1586a61`, VersionId `obEQ.jINuBWUMu7xLwt8ZXSFYxiOWeXE`) were create-once published and round-tripped. A stronger fresh-clone `git fsck --full`, added before any spec publication or capacity launch, then proved that this workspace had been shallow at `15a9616`: the bundle omitted parent `c23745b5ad24e98f66967ea795a07b2588ed6c79`. The same defect exists in the legacy 229c model bundle `HY8r1VZTuShbxIAknhQgVyM6pVnWm9uk`. Both bundles can clone and check out their advertised trees, which is why `git bundle verify` alone passed, but neither is a self-contained full-history artifact. Treat `U5zC2...`, `obEQ...`, and `HY8r1...` as rejected for every future worker. They remain immutable evidence and must not be deleted, overwritten, or selected by a retry. The repository was unshallowed from the official upstream, the formerly missing parent now resolves, and local `git fsck --full --no-dangling` passes. Replacement bundles require a fresh non-shallow clone plus full fsck before publication. A separate local post-upload audit also found this workstation's `sha256sum` rejects GNU `--check --status`; no object was uploaded twice, and workstation replay compares direct digest output instead.
69. Before another paid launch, SSM preflight `b27779bd-da00-4e16-b5d4-cde42706f49e` proved the retained workbench still had a clean exact-229c model checkout, the digest-pinned LIBERO policy image, and no stale smoke path. Runtime smoke `be8e6ef0-131e-4a9c-ad61-b68bf2ad6b47` then exercised the corrected mount order against that exact image on the L40S. With the parent input mount read-only, both pre-existing child targets mounted and wrote successfully (`/mnt/openpi/runs` SHA-256 `1b368294ceab6cf10cfa0145e14d27dbf4bb9a7197a3864fb12a0e53c91a7391`; `/mnt/openpi/evidence` SHA-256 `3d8d6ab61a763b5438cb20afba7de19de2fe9cdbab9ea464b4fbf9ad9037f8dc`), an undeclared write below `/mnt/openpi` failed as required, `/workspace/openpi` remained non-writable, and Torch saw exactly one NVIDIA L40S. The exact Docker topology exited zero in 2.287 seconds. This is a mount/runtime control smoke, not a training result.
70. The replacement model-source artifact preserves model/image commit `229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee` while supplying its complete upstream ancestry. The 1,676,981-byte bundle is SHA-256 `9be1f91dfec636d1cbb63ad87b166e301b98835b91a6212f73fa5b5350d0f7b5`, key `source/openpi-229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee-complete.bundle`, VersionId `CN9PJHZ3oHC3hb7lwDTH9p3JEAVQmUhh`. Before publication, two independent audits fetched it into an empty bare repository and cloned it into a fresh checkout; both proved non-shallow status, exact clean detached 229c HEAD, the formerly absent parent `c23745b5ad24e98f66967ea795a07b2588ed6c79`, and `git fsck --full --no-dangling`. After create-once AES256 publication, an exact-VersionId round trip repeated clone and full fsck. A second independent remote audit reproduced the same result and confirmed metadata `history=complete`, one object version, and no delete marker. All future specs use `CN9P...`/`9be1...`; the older `HY8r...`/`bb5a...` pin is evidence only.
71. The final integrated host controller is commit `eed07ae314a1fdac530dcc79b59045bdaefb1a63`. It includes the dual-source/fsck worker, LIBERO renderer migration, and provenance-complete two-container RoboLab worker. The local gate passed 458 script tests with the platform-specific legacy `scripts/train_test.py` excluded, 129 focused RoboLab/generic tests, Ruff, format, byte compilation, Bash syntax, diff checks, non-shallow repository verification, and full Git fsck. Its 1,763,719-byte complete bundle is SHA-256 `d38bbe9a634e93afdacc76afbfe93c2bf59302edd61351ec60ce9da0ecad38ce`, key `source/openpi-eed07ae314a1fdac530dcc79b59045bdaefb1a63-complete.bundle`, VersionId `efxR27Hh5D2WazkrAs6rjz4OX693DQET`. Independent local and exact-VersionId remote audits each passed empty-bare fetch/fsck, non-shallow clone, clean detached checkout, full checkout fsck, and the formerly missing-parent check. The matching 8,888-byte bootstrap is SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`, key `bootstrap/worker-bootstrap-controller-eed07ae314a1fdac530dcc79b59045bdaefb1a63.sh`, VersionId `wClnhRbXEt6JbwbCgGlNZ2Q2ox.ny7Q4`; its exact-version round trip passed Bash syntax and singleton/no-delete checks. The bootstrap now runs full fsck for both independently cloned sources before emitting positive evidence or executing the worker.
72. The first local bootstrap-publication command assigned to zsh's special lowercase `path` array. This replaced the command lookup path, so its initial `sha256sum`/`awk` precondition failed before any AWS API call. Read-only history confirmed the intended key still absent. The identical command was rerun with task-specific variable `bootstrap_file`, and only that replay performed the create-once upload. Replays must not use `path`, `PATH`, `home`, or other shell-special names as task variables.
73. Independent review of the LIBERO A2 preparation gate found that its collision query used `Name=pi05-${run_id}`, while the guarded launcher tags the instance with the exact run ID, and that it omitted the transient `shutting-down` state. Both were corrected before any A2 object or capacity existed. The first authorized preparation replay then stopped before its sole S3 write because the pending JSON plan rendered four hours as `4.0` and the shell compared it textually with `4`. The guard now uses the numeric `jq` assertion `.max_runtime_hours == 4`. Exact-prefix history remained empty after both pre-write stops. The final preparation script is 14,035 bytes, SHA-256 `5f84e14968b7f7ee799f4cf40f41383f889acae63c645c80549ae4d4107f901c`; Bash syntax, the corrected exact-label/five-state query, its create-once boundary, and the no-launch boundary were re-reviewed.
74. The corrected preparation replay create-once published only `specs/libero-shallow-overfit-20260804t091725z-a2-300.json`, VersionId `S.wENata9yyVQsz3nwDyYBCgnkCuQWVL`, SHA-256 `47a125ab8d5f5e5948ecdb840bc48558fd5b8ebfa06b0778cbbc848513b6723c`. The key has one AES256-encrypted version and no delete marker. Its final execution-enabled bootstrap is SHA-256 `9cdcd2f76a3fd0c63329142fca8c9a0000ec812bf5703b2e4b5c51547b56c206`, is byte-identical to a fresh renderer invocation, and pins the complete controller/model bundles plus that exact spec VersionId. The subsequent launcher invocation remained read-only: it planned one On-Demand `g7e.12xlarge` for four hours plus the 15-minute reserve, `$35.21584`, with `CapacityReservationPreference=none`; no A2 instance, output object, or cost-ledger reservation existed at this checkpoint.
75. Reservation `4c8962fe-2f77-40e1-a04a-de2855a4e605` launched the independently reviewed A2 command on exactly one On-Demand `g7e.12xlarge`, instance `i-0f0b1cd871e911ed6`, in `us-east-2a`. Its ordinary lifecycle, `CapacityReservationPreference=none`, IMDSv2-only/hop-limit-one metadata, encrypted delete-on-termination 256 GiB gp3 root, exact command hash tag, and exact run-name tag were read back. EventBridge schedule `pi05-deadline-4c8962fe-2f77-40e1-a04a-de2855a4e605` is enabled and targets `TerminateInstances` at `2026-08-04T13:53:26Z`. The six-entry conservative ledger totals `$113.99316`; its exact S3 VersionId is `B1U0EitVIsu1EbrA6zOsEojdOzAyi8a9`, SHA-256 `4cc62f1beb80f31c0f431046c19933409772739d0a4b1a74c688787a65074285`. SSM came online, both complete source bundles cloned at their exact commits, and the reviewed controller entered input staging; no other capacity was launched.
76. The first local SSM polling loop used zsh's read-only special parameter `status`; assignment failed after its one read-only `DescribeInstanceInformation` request and made no AWS mutation. The immediate replay used task-specific `ssm_state` and observed `Online`. Shell monitoring snippets must avoid zsh special parameters just as publication snippets avoid its special lowercase `path` array.
77. After exact input staging completed (54,851,106,533 bytes) and the 13.1 GB ECR transfer expanded to a 39.51 GB local image, the A2 container remained GPU-idle during `torchrun --standalone` rendezvous. Logs showed repeated inability to resolve Docker's random hostname because `--network none` provides no DNS entry for it. A first read-only SSM inspection request used an invalid JSON escape around shell command substitution and was rejected locally by the AWS CLI before `SendCommand`; the corrected request confirmed `/etc/hosts` contained only localhost entries and no container hostname.
78. To continue diagnosis on the already-paid worker, SSM command `e250ebf9-06bf-4466-b7f5-3f0a2f331a39` appended only `127.0.0.1 043c30c7b439` to that running container's generated `/etc/hosts` and verified resolution. This manual runtime repair allowed both `torchrun` ranks to pass rendezvous and enter the first distributed barrier, proving the hostname half of the correction. It is not reproducible source provenance and cannot make A2 promotable. Replays add a deterministic Docker `--hostname` plus matching `--add-host` while retaining `--network none`.
79. The same A2 diagnostic then failed before its first optimizer step because the policy runtime inherited `NCCL_SOCKET_IFNAME=^docker0,lo`; a network-none container exposes only loopback, so NCCL reported `Bootstrap : no socket interface found`. The reviewed generic training container must explicitly set `NCCL_SOCKET_IFNAME=lo` in addition to deterministic hostname resolution. A2 has no checkpoint or metrics. Its final 6,168-byte log is VersionId `fjAnm1zZ_OYi0a0i.TgkIej_qVMuk9zO`, SHA-256 `beae1572db2873c5188580e9d27d3ab504d2c05319f83a32aa5139c96f21b9bb`; failed run manifest VersionId `DFYehfmAHHE97UgZi_z2NNvn263MNkNP`, SHA-256 `019c6b0fd75c82c964c0c4e5d11233c652a847ea05e302f177902cd20e5924ef`; final-sync VersionId `jamL5TK1jdYnPgHaJVnY._EzqG6hSAxr`, SHA-256 `528724deaaac8d3774978e85225a266b70d58ac56f0f819d32125b43a0a4184b`. Each of the five output keys has one version and there is no expected-output marker. The worker requested instance termination after sealing the failure; A2 is diagnostic evidence only and must never be selected as a continuation input.
80. The retained one-GPU workbench was reused to validate the source-level network correction without launching capacity. The first local renderer for the SSM parameters exposed unquoted JSON to zsh globbing and stopped before `SendCommand`; the corrected JSON-safe submission reached the container, where command `d1a1b9a8-12a3-4c8f-b7d0-0e6d2e1960b2` showed that `torchrun` does not accept `-c` directly as its training script. Replay `5be77555-86eb-4a2b-89b9-b23af0a331d8` used `--no-python /usr/local/bin/python -c` and passed a two-rank Gloo barrier in the exact policy image under `--network none`, deterministic `--hostname`, matching `--add-host ...:127.0.0.1`, `NCCL_SOCKET_IFNAME=lo`, and `GLOO_SOCKET_IFNAME=lo`; both ranks printed success. This validates hostname/control-collective loopback on retained infrastructure. The clean two-GPU replay remains the NCCL acceptance gate.
81. Controller commit `c6b197f52404663942719688797103b5c346c150` encodes that correction generically: every isolated worker receives a deterministic DNS-safe hostname derived from its run ID plus an exact loopback host entry, while only validated multi-process standalone `torchrun` commands receive worker-owned `NCCL_SOCKET_IFNAME=lo` and `GLOO_SOCKET_IFNAME=lo`. Specs cannot override those values, symbolic/ambiguous process counts are rejected, and network isolation is unchanged. All 461 script tests (excluding the known platform-specific legacy `scripts/train_test.py`), focused Ruff/format/compile/diff checks, the retained-workbench smoke, non-shallow repository fsck, and fresh bundle clones passed. Its complete 1,769,820-byte bundle is SHA-256 `14eb984afb99181ab44bc80919c02f0e5a7df622e1126026987581a803722cf0`, key `source/openpi-c6b197f52404663942719688797103b5c346c150-complete.bundle`, VersionId `jfsxN3skM_vNTF2pVIxE6c0gDyiIKgkj`. The unchanged 8,888-byte commit-qualified bootstrap is SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`, key `bootstrap/worker-bootstrap-controller-c6b197f52404663942719688797103b5c346c150.sh`, VersionId `J1Whhm6bg8IJiPVdSBasI13bvdFuvCtK`. Both keys are AES256 singleton/no-delete objects. Independent exact-VersionId downloads reproduced both hashes; empty-bare verification, mirror/working clones, non-shallow status, exact detached HEAD, Bash syntax, and full strict fsck passed.
82. The fresh A3 retry controls were published separately from capacity. `specs/libero-shallow-overfit-20260804t101804z-a3-300.json` is the sole version of its key, VersionId `P6dKehgZFIuykyWybqMizn__PisJdaVc`, 5,418 bytes, SHA-256 `9ac1189680698602e525ebd88917cb262e95197dd23d6f1792885cfa1e134529`, AES256 encrypted, with no delete marker. Its metadata pins controller `c6b197f52404663942719688797103b5c346c150` and unchanged model source `229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee`; an exact-VersionId download matched the frozen local spec. The final execution bootstrap is SHA-256 `cd2beabf4c6d64621303f1eecb403625337146c61ad2fe2f16ef6daf886d5bcb` and matched a fresh renderer invocation byte-for-byte. The bound read-only plan selects one ordinary On-Demand `g7e.12xlarge`, `CapacityReservationPreference=none`, four requested hours plus the 15-minute reservation margin, and `$35.21584`. Immediately before launch, the A3 output history and exact-label five-state EC2 query were empty and the authoritative ledger remained `$113.99316` with no A3 entry. No capacity was created by control publication.
83. The separately authorized A3 execution created reservation `96f76d00-2fcc-4391-8ade-b8e60663f262` and exactly one On-Demand `g7e.12xlarge`, instance `i-0670a84ff7700440b`, in `us-east-2a`. Live inspection confirmed `CapacityReservationPreference=none`, IMDSv2 required with hop limit one, instance-initiated shutdown set to terminate, two 97,887 MiB NVIDIA RTX PRO 6000 Blackwell Server Edition GPUs, and encrypted/delete-on-termination 256 GiB gp3 volume `vol-01d501f9079530dd5`. Independent schedule `pi05-deadline-96f76d00-2fcc-4391-8ade-b8e60663f262` is enabled and targets `TerminateInstances` at `2026-08-04T14:28:26Z`. The seven-entry conservative ledger now reserves `$149.209`; its exact latest VersionId after launch is `wpupNv_rr1DOoQIIOQIWpjmCneQ9NuVO`, SHA-256 `33db7e1253ef036324cf9aa6d53c73902d340d9b295f80c33ae9671d0fd8c362`. Cloud-init completed, SSM came online, both complete source bundles cloned at their exact commits, and 52 GiB of pinned inputs staged to local NVMe before the pinned image began unpacking. No concurrent GPU worker was launched.
84. A3 proved the reviewed network correction on the real two-GPU worker. Container `585ab6305781701c618ec79fef21f4d316e259a15169961326f009a71837a83e` had deterministic hostname `pi05-worker-7c41560b2661d3c3e5984e9179a599d7`, the matching `127.0.0.1` host entry, `NetworkMode=none`, exactly one worker-owned `NCCL_SOCKET_IFNAME=lo` and `GLOO_SOCKET_IFNAME=lo`, read-only source/input mounts, and only declared writable output/cache mounts. Both ranks selected distinct GPUs and completed NCCL bootstrap over loopback. What initially looked like a hang was a 342.68-second NCCL communicator initialization whose timing attributed 342.66/342.35 seconds to device kernels; bootstrap, topology, graphs, and connections were effectively milliseconds. It then connected four rings through `P2P/CUMEM`, passed the first barrier, established local microbatch four on each GPU (global microbatch eight, accumulation eight), loaded the exact LIBERO split and teacher, and began model construction. Replays must allow for this measured first-container initialization latency and must not classify it as a socket failure merely because the log pauses after `Comm config Blocking set to 1`.
85. During that long silent initialization, three bounded same-node diagnostics were started manually. SSM `a81a6c16-8a52-4363-800e-daa8363ae05b` set each CUDA device before `init_process_group`, passed `device_id`, and timed out after 90 seconds while still in the same slow kernel phase. SSM `cfe2afa6-2c37-43f7-a0dd-b3f494dae5fa` added `NCCL_P2P_DISABLE=1` and timed out after 60 seconds, also before its communicator finished. SSM `709b3fce-4932-4be4-b95a-2df6ebcea5ca` began a socket-only discriminator, but was cancelled as soon as the newly synced A3 segment proved the primary communicator had completed; the child diagnostic container required a separate stop/kill request while the instance was already entering shutdown. A first environment-inspection invocation had malformed JSON and was rejected locally before `SendCommand`. These diagnostic CUDA contexts overlapped the primary process, so A3 is valid failure evidence but not a clean causal test of P2P, cuMem, device ordering, or the image/driver. The next two-GPU diagnostic must run alone on clean GPU contexts; never repeat concurrent CUDA collectives beside a promotion candidate.
86. A3 failed at `2026-08-04T10:47:41Z`, before its first optimizer step, when both ProcessGroupNCCL watchdogs reported an asynchronous illegal CUDA memory access during DDP construction; rank zero terminated with SIGABRT and the container exited one. No checkpoint, `training-metrics.json`, overfit diagnostic, expected-output marker, or published input exists. The exact terminal log is `runs/libero-shallow-overfit-20260804t101804z-a3-300/logs/container-000005.log`, VersionId `CsF120JJSbhFpQOQvxYfXWTLccjkT0mM`, SHA-256 `0b3837fac1152fea099824be5259f591f5c202bd2ce6254efacf7b485dbc6d7d`. The failed run manifest is VersionId `Ac6SeqISqUJ7KwzMQFBDpZYkzFA3X_96`, SHA-256 `10c5d993b4d7a3bc02986779a460f157382bdf4f1474c423e3d62e27add406da`; final-sync evidence is VersionId `C6UFKyoNpQsO1ZnKxdVq66zvcoFwmyT6`, SHA-256 `0c7277e0d09bc72727760a508f5d3505b130d61a9467fc8086c003f356bf1192`, and records seven receipts plus successful final sync. The prefix contains exactly six append-only log segments and those two manifests, with no delete marker. The worker initiated instance termination immediately. Because overlapping diagnostics confound the illegal-access cause, A3 is diagnostic only and cannot authorize the prepared 2k pilot.
87. Instance `i-0670a84ff7700440b` reached `terminated` with reason `Client.InstanceInitiatedShutdown` only after the complete A3 final sync. Independent exact-VersionId downloads reproduced every hash in its eight-key prefix; all eight objects are AES256 singleton versions with no delete marker. To continue without DDP, commit `776069268d85d3bfc8feb632f3a4770106b01366` keeps ordinary Shallow launches locked to `g7e.12xlarge` and admits one `g7e.4xlarge` only when both category `corrective_run` and workload `shallow_training` are explicit. A cross-workload fallback remains rejected. Ruff, format, byte compilation, 126 relevant launcher/worker/training/checkpoint tests, and an independent policy review passed. Its 1,774,980-byte HEAD-only complete-history bundle is SHA-256 `e0ef2a041c63617f9c8300de27fbf9f7b71275c18134ced8ee3413725c633644`, key `source/openpi-776069268d85d3bfc8feb632f3a4770106b01366-complete.bundle`, VersionId `1rS.xU8DWM4uyZGaJOU2gSEjEQWi.77g`. The unchanged 8,888-byte bootstrap is SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`, key `bootstrap/worker-bootstrap-controller-776069268d85d3bfc8feb632f3a4770106b01366.sh`, VersionId `HaD15VhZJua_bd_mrFTi9pzDesXAuUkM`. Both create-once keys are AES256 singleton/no-delete objects. Local and exact-VersionId remote audits passed bundle verification, fresh bare and working clones, non-shallow exact detached HEAD, formerly missing-parent presence, full strict fsck, exact hashes, and bootstrap Bash syntax.
88. Commit `28f2deb276c94218ece962499f11161c98a28c31` supersedes the A4 controller pin only to record the reviewed dual purpose of `g7e.4xlarge`: export/compile/latency work plus the explicit single-GPU corrective Shallow fallback. It does not broaden the launcher exception or alter model, image, data, or training semantics. Its 1,777,183-byte HEAD-only complete-history bundle is SHA-256 `d1da8dd19b8c16b122add4c3bab44b5dec652c84ac1d9301e8a2fccb7dd83afe`, key `source/openpi-28f2deb276c94218ece962499f11161c98a28c31-complete.bundle`, VersionId `5mrwHjcn0bNi48JAhodM_98mjdb78Lxo`. The matching unchanged 8,888-byte bootstrap is SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`, key `bootstrap/worker-bootstrap-controller-28f2deb276c94218ece962499f11161c98a28c31.sh`, VersionId `ZP0o3MZbf2_DIBuQCTzuCHF6bspjlaJK`. Both create-once objects are AES256 singleton versions with no delete marker. Local and exact-VersionId remote audits passed fresh bare and working clones, non-shallow exact detached HEAD, formerly missing-parent presence, full strict fsck, exact object hashes, and bootstrap Bash syntax. A4 controls must pin this final controller rather than the otherwise valid 776 artifact.
89. The separately reviewed A4 preparation then create-once published only `specs/libero-shallow-overfit-20260804t105223z-a4-300.json`, VersionId `TLwkBBsAtNQ.sjc.EtVC__m4a2Bf31.I`, 5,366 bytes, SHA-256 `88401d15d32522af15db32f750f7198240954cba52b94ca8851976939cbe7023`. The exact-version head reports AES256 plus controller commit 28f, model commit 229c, and the exact run ID; a round trip matched the frozen local bytes, and the key has one version with no delete marker. The 24,846-byte preparation control (SHA-256 `ee3fb97642aa2c3e9902305b47069c4aeec55c6936cd1113ea2a755b91f83692`) hard-gated the exact failed A3 manifest/final-sync tuple, absence of A3 promotable outputs, the complete controller/model artifacts, a clean operational tree, a fresh A4 output/instance/ledger namespace, and the direct single-process Python argv before that sole write. The final VersionId-bound bootstrap is SHA-256 `29d55c78af115333e8188325f58ecdb30904ca9896b2ccf315bd90279a4adcb1`, passed Bash syntax, and was byte-identical to an independent renderer invocation. Its read-only launcher plan is SHA-256 `659e4cbec33dcc2cae76508f1638fa697f40053bace0eac7c0e87f4c05bdd96d`; it selects exactly one ordinary On-Demand `g7e.4xlarge` in `us-east-2a`, no Capacity Reservation, six requested hours plus the 15-minute reserve, and `$24.9885`. After preparation the A4 output prefix and exact-label five-state EC2 query remained empty, the seven-entry ledger remained VersionId `wpupNv_rr1DOoQIIOQIWpjmCneQ9NuVO`, SHA-256 `33db7e1253ef036324cf9aa6d53c73902d340d9b295f80c33ae9671d0fd8c362`, totaling `$149.209`, and no capacity or spend reservation existed.
90. The separately authorized A4 execution created reservation `28b32b27-a6c0-4931-b409-e3017e7a2858` and exactly one ordinary On-Demand `g7e.4xlarge`, instance `i-0a5a2502e114aaedc`, in `us-east-2a`. Live inspection confirmed `CapacityReservationPreference=none`, AMI `ami-01901bc01d5d9bb55`, IMDSv2 required with hop limit one, instance-initiated shutdown set to terminate, and encrypted/delete-on-termination 256 GiB gp3 root volume `vol-0e376018f0fcd37b5`. The exact Name, corrective category, Shallow workload, and command-hash tags were read back. Independent schedule `pi05-deadline-28b32b27-a6c0-4931-b409-e3017e7a2858` is enabled and targets `TerminateInstances` at `2026-08-04T17:20:23Z`. The eight-entry conservative ledger now reserves `$174.1975`; its exact latest VersionId is `iHJPKSK4ufhTO0g4J4YZn371PlgGVa6B`, SHA-256 `429aefbb10e5aef7eb0004421f1c220c21bdf9b6c515be6bee6f54e51158dc33`. No diagnostic or second training worker is permitted beside this candidate.
91. A4 cloud-init completed in 51.52 seconds, SSM came online, and both EC2 health checks passed. Read-only SSM command `ee1052f4-470d-490a-8dc5-62e5aad7f9e7` confirmed `pi05-job.service` active, the model bundle detached at exact commit 229c, and the controller bundle detached at exact commit 28f. Read-only staging check `a1158fb7-f2b1-43dd-bf8b-6491bc3247bc` made no GPU call; at 11:27Z it found the same single worker process active and the exact digest-qualified Docker pull running, which establishes that immutable input staging had completed and image retrieval was in progress. No container log object is expected until the image is verified, the output manager is created, and the training container starts.
92. A4 entered the direct single-process container at `2026-08-04T11:30:54Z`. Log segment zero is VersionId `BXTLJLheNngLY1WiaQ7rnQhx5VZRTu69`, SHA-256 `03248e8f2149a67ccd99785fa126bb671d27614ddac34447a8cb15bf293923de`; segment one is VersionId `nIf_4fTY7ie_jhxxYC9fxxWqvuchFUl6`, SHA-256 `298443a253d3a203fff5bd276bf3ac28bf55a2de0bfa8f6d0d297d6abc7d07d4`. They prove world size one, local/global microbatch eight, accumulation eight, optimizer batch 64, fixed one-batch noise/time/mask, and held-out episode 276. Segment two is VersionId `b6agqX4.DU3hJd5W4bY97RXnR1Uhijp0`, SHA-256 `6c4570c6dac27b60fc3e8213ec64a39b6930fcc13b89829405a8e841694f2a4e`; it proves exact layer map `(0,2,4,6,8,11,13,15,17)`, 633 mapped state keys, BF16 and gradient checkpointing. GPU allocation/reservation was 12.45/20.03 GiB after model creation and peaked at 30.68 GiB during training. Segment three is VersionId `lSU1_Xi6MIdAnprvi1ENSkS.ap.aeFx5`, SHA-256 `8525d0ac9918a8f9f1c3238157d673fef5724ab4554f578f64ea9b8de0204441`; optimizer step zero completed at 11:33:52Z with finite loss 4.9232, and the ten-step report at 11:34:46Z showed approximately 5.39 seconds per optimizer step, finite loss/KD metrics, and an estimated 26 minutes remaining. No OOM, NCCL/Gloo path, second container, or error appeared.
93. A4 completed all 300 optimizer steps and passed its methodology-only overfit gate. The first-20 mean loss was `4.861105489730835`, the last-20 mean was `0.00137333256425336`, every loss was finite, and relative decline was `0.9997174855457972`; final loss was `0.0012933362741023302`, held-out FM/KD MSE were `0.0006168886902742088`/`0.0006764474674127996`, KD cosine was `0.9996635913848877`, and per-joint normalized RMSE mean/max were `0.016945060342550278`/`0.09823093563318253`. Completion log segment 30 is VersionId `5LZtnNyRaRggs2FbCPap5drD0ugrAVjE`, SHA-256 `352b3b3e8b80bc8b0448eea4f8103ecc2166dd12449c06c7587b07fad5c9b336`. The immutable succeeded/exit-zero run manifest is VersionId `JyxugbqH_T.B9biVyfcETR46Sa5VTJxG`, SHA-256 `6b642cbd90aba982fb59e84a9db7e0fbfa3d976878d81c08da28eca5be81a12c`; exact-version final-sync evidence is VersionId `RmSnrJEqgm3wsXj6NFBYZa9hGv3Bd9Fd`, SHA-256 `7dfcce4bfdc1f4230574eb14ceb776c5a97537a8ac4fb343fa2cf2b0baf2af21`, with `final_sync_succeeded=true`, 34 receipts, and the exact manifest receipt. The run prefix has 41 singleton AES256 versions and no delete marker. Its eight checkpoint objects total 13,386,484,612 bytes: norm stats `PbNz90J04HlfdfXpZNTWjOKTxKs1tEsr`/`b3a44bb2810436fb62917decaea58bd4d9110255df527dea21e8fd40c960bd84`; metadata `SR81HNNpZxhJpBgKvLJ_YFxTrfKR9FSQ`/`8796b70dc3f5de5584eb85be9ca5e0114b50bf62af755220dc1beddb0c89690f`; model `goIhUfd2o.Bz20OIVRuJX8lgylgH4rZO`/`decf6c68d3a1af235f9c85e85bc75575d9bff93ae212b11ca80a4ca9de2be0f3`; optimizer `7Dx.zs2CQu.8OWhg0gO79zwpdn__6E7O`/`c2037e6561d5b9cb7b9c92ca0278ef327e9f567fb32d955064b34329e10deda0`; diagnostic `nefL9ggNj9an3jliTTv7RynXBvZLSf32`/`c34144ff02c393e265b82ba6e436adb77546b2c2d18958c1913051e47e81dd91`; resume state `9tpvaifBcRaZma.DFawwsOG2BXauCxC4`/`bf268d5127e72be2c91dc28f3db054e808497b782d1450de0c121a7f01ade930`; training metrics `rsEWNs2bcbSF9XsrgZnsdP0mCVVUpIwx`/`5a48a24c0adbd4c7669c7e734f7a3d9384d9a4181386ef07027b5acc60018280`; and W&B ID `ihXZJCuRu5VMhZMyrfqfhmoGYpBPC92k`/`d6bdb5da1ebfd1a0189d1712099f3825fc64bd4abed06eda30afdabaf6818b61`. Workbench SSM `5b185b29-72f5-4236-a998-da76bd059f4f` failed before any download because the document's POSIX shell rejected `set -o pipefail`; replay `375be167-67e8-4d2d-b450-b5d12d5926b9` used `set -eu`, downloaded every object by its exact VersionId, and independently reproduced all eight hashes in 2m34s. A4 deliberately has no `publish_destination` or reusable `published_inputs`; its one-batch checkpoint must never be resumed. Only after the durable final sync did instance `i-0a5a2502e114aaedc` self-terminate; terminal state code 48 and `Client.InstanceInitiatedShutdown` were observed at `2026-08-04T12:08:53Z`.
94. As a second independent checkpoint gate, workbench SSM `4b73969d-2809-4a83-84c9-d41cf942e785` hard-linked the eight already hash-verified exact-version downloads into a numeric step-300 tree and ran `scripts/repro_checkpoint.py --require-overfit-report` inside the exact immutable LIBERO policy image with networking disabled, a read-only container root, read-only source/checkpoint mounts, and no GPU. It completed in 21 seconds with `/checkpoint/300` and `A4_STRUCTURAL_CHECKPOINT_VALIDATION_OK`, proving the safetensors archive, complete finite optimizer state, metadata, schema-v2 resume contract/fingerprint, normalization hash, and overfit report are mutually consistent. These local hard links remain audit evidence on the persistent workbench volume and do not make A4 resumable or published.
95. The DROID 300-step gate was resolved only after A4 success and termination, without staging or promoting any A4 artifact as a DROID input. The 8,558-byte methodology record has SHA-256 `7b9c88620709bdee6615ff9792e7533232bfff10dbd03ac250855726c6b21243`; it binds the exact A4 manifest/final-sync/diagnostic tuples, all eight checkpoint receipts, instance `i-0a5a2502e114aaedc` as terminated/code 48/`Client.InstanceInitiatedShutdown` after final sync, and a zero-active-Shallow query. The DROID spec remains fresh teacher initialization with controller 28f, model source 229c, immutable MolmoAct2 DROID revision `e44d3138c64cfeb1c24fbbce087b475fb1233728`, the converted joint-position teacher, direct Python, 300 optimizer steps, one-batch overfit, no resume/overwrite/publication destination, and SHA-256 `fc101e964502c823b6373d808aacae8e0ea05ca3ff2e9144d664e829cc249504`. Preparation SHA-256 `9892cbb7fb0f4e4bfd3254897910584f6934c3139b7243b4cdbb2a9e3e29d362` and independent review SHA-256 `3780ac8dd3bef4795a305caafb27da7b28ef9dc37f31342c845309b6f99c62bf` passed 122 relevant tests and two clean audit-only replays; the preparation contained exactly one future create-once AES256 spec PUT and no launch or reservation call. The authorized replay created only `specs/droid-shallow-overfit-20260804t112518z-a2-300.json`, VersionId `QHueugLwZsDNlgK_NvJTOd.PaeQ0rp08`, with exact bytes, metadata, singleton history, and no delete marker; capacity/output/ledger namespaces remained empty. The VersionId-bound final bootstrap is SHA-256 `c18eb80fd3c87faead0f4817519f8f441a8903c8b9f67641fc7b760e470de266`; the read-only final plan is SHA-256 `6c03629579a3d866a031d81a2bc1fdfc2e4f532659df3df7c26933720e37e4fe` and selects one On-Demand `g7e.4xlarge` for six requested plus 0.25 reserved hours, no Capacity Reservation, `$24.9885`, with `$2,550.814` remaining after its reservation and the non-compute reserve. An independent post-publication audit and a separate root audit reproduced the spec, renderer output, plan contract, empty namespaces, A4 terminal state, and zero active Shallow workers. The root audit's first final ledger assertion queried nonexistent `.projected_usd` fields and stopped after all other checks had passed; the immediate read-only replay summed the ledger's actual `.usd` fields to `$174.1975` and returned `DROID_POST_PUBLICATION_AUDIT_OK`.
96. The separately authorized DROID execution created reservation `89948bbb-966b-4bae-a325-95af346c9928` and exactly one ordinary On-Demand `g7e.4xlarge`, instance `i-02f230ed0529e2b14`, in `us-east-2a`. Live inspection found no spot/market lifecycle, `CapacityReservationPreference=none`, the exact Name/category/workload/command-hash tags, IMDSv2 required with hop limit one, and instance-initiated shutdown set to terminate. Root volume `vol-0707f7877fed87e76` is encrypted 256 GiB gp3 and delete-on-termination. Independent schedule `pi05-deadline-89948bbb-966b-4bae-a325-95af346c9928` is enabled, self-deleting, targets only that instance with `TerminateInstances`, and fires at `2026-08-04T18:29:38Z`. The nine-entry conservative ledger is VersionId `yKuO4a8MHmMCbq0SXq2Ksc18.KpjbgVh`, SHA-256 `11a3e6da53f02629ebc194b3ebd5c8f255b75c0bc7a94d408999749c63222622`, AES256, and totals `$199.186`; the exact DROID reservation is `$24.9885`. The retained workbench independently entered its scheduled stopping state before this launch and is not a Shallow worker. No second training or diagnostic process may overlap this candidate.
97. The evidence-gated 2k continuation policy at controller commit `0b679a3af0c2df4ae80e95b69ab0865be5aa98e6` was bundled before later DROID evidence-only runbook commits. The first prepared publisher correctly became unreplayable because it required branch HEAD to equal 0b. Its replacement, SHA-256 `ad13e5a9351554d2204fcdd8649d1ba110cf58758554a104017111c77849b9ae`, requires 0b to be an ancestor of a clean non-shallow checkout, proves every individual descendant commit is single-parent and changes only `RUNBOOK_AWS.md`, and binds all operational/bootstrap/configuration/test inputs byte-for-byte to 0b; it does not weaken the exact bundle-head check. All 122 worker/trainer/launcher/container-contract tests, Bash syntax, complete-history verification, exact single HEAD, strict fsck, and empty destination histories passed. The authorized replay then create-once published only the controller bundle and identical bootstrap, with no spec, ledger, or EC2 call. Bundle `source/openpi-0b679a3af0c2df4ae80e95b69ab0865be5aa98e6-complete.bundle` is 1,784,584 bytes, VersionId `K4N1WPDWTqVPq5mfRbMx_X6EV0eIErPd`, SHA-256 `bf415b57aef47e3ca8c640f63c3603c7c426247247e62000a6cc4f313ac0a25c`; bootstrap `bootstrap/worker-bootstrap-controller-0b679a3af0c2df4ae80e95b69ab0865be5aa98e6.sh` is 8,888 bytes, VersionId `v9._MVJjb0A39zdU.6w.0c8ARXkywgUm`, SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`. Both are AES256 singleton/no-delete objects with complete-history metadata. A separate exact-VersionId round trip matched local bytes, validated bootstrap syntax, fetched the bundle into a new bare repository, passed strict fsck, and returned `CONTROLLER_0B_POST_PUBLICATION_AUDIT_OK`. Git's informational `HEAD points to an unborn branch (main)` notice came from initializing that empty bare audit repository before the explicit bundle fetch and was not an artifact failure. No 2k spec has been published; its final preparation remains gated on DROID termination, the then-current ledger, and a separately reviewed launch-time concurrency wrapper.
98. Fresh LIBERO 2k controls were prepared but deliberately not published or launched while DROID is active. The immutable reviewed template is 5,371 bytes, SHA-256 `4fa8421ca70606e522ef9b2b270147499d3ee1a21add5011e65a0c2c759dc0d2`, and pins controller 0b/VersionId `K4N1WPDWTqVPq5mfRbMx_X6EV0eIErPd` while retaining A4's exact model image, released dataset, JAX/PyTorch teachers, runtime, batch semantics, and seed. It is fresh teacher-layer-map initialization, direct Python, 2,000 steps, save only at 2,000, no resume/overwrite/one-batch option, and has one matching immutable `publish_destination`; A4 is methodology evidence only and never an input. Pending bootstrap SHA-256 is `0cfc82c26d7428b58abbd3be9b2eaab9f0f599d505d7cac12e9d946614daa081`; pending four-hour-plus-0.25-hour plan SHA-256 is `b2c58fe82da5ec6eb6240e5e6d875e46490f0d79cd0de2d6f7becfeb695898a9`, reserving at most `$16.99218` with a `$2,533.82182` remaining-budget snapshot. Preparation SHA-256 `7975228e1e10a200df0fb15a8842b96e88bb39935f68f935ac50065b8c09c60f` binds the exact latest nine-entry ledger tuple `yKuO4a8MHmMCbq0SXq2Ksc18.KpjbgVh`/`11a3e6da53f02629ebc194b3ebd5c8f255b75c0bc7a94d408999749c63222622`/18,236 bytes and rejects stale ledger authorization; its only possible mutation is one conditional spec PUT, and its launcher call is plan-only. Independent review found that a preparation-time zero-worker gate alone left a later paid-launch TOCTOU, so a separate fail-closed execution wrapper was added. Its first draft expected launcher result mode `execute` instead of the actual `executed` and would have failed only after a successful launch; this was corrected before use. Final wrapper SHA-256 `fb3d46dcd62b8657d51d1510c95894abcfa21ae6ece8a2cf0ebb01feb01b7c6e` binds every returned spec/bootstrap/plan hash and VersionId, revalidates current S3/A4/ledger/namespaces, replans stable fields, performs the Project+Workload zero-active check as the last read, and contains exactly one paid launcher `--execute`. Review SHA-256 `887e6a607b2c8e57bc68367825715c51e4e02fef15da1dcf4ed666d1477bd217` records 122 passing tests and an independent PASS/conditional GO. An all-authorizations preparation replay stopped exactly at `active_project_gpu_instances=1` while DROID `i-02f230ed0529e2b14` was running, before spec materialization or any AWS mutation; the LIBERO spec/run/ledger namespaces remain unused.
99. DROID A2 completed immutable staging and entered Python at `2026-08-04T12:49:07Z`. The held-out split completed at `12:49:16Z` and the loader reported `local_batch_size: 8` at `12:52:29Z`, but the first `next(iter(loader))` emitted no completion or failure for more than 30 minutes. During that exact interval the healthy running instance's encrypted, delete-on-termination root gp3 volume `vol-0707f7877fed87e76` sustained approximately 7.869 GB/min of reads, 100.06% of its 125 MiB/s provisioned throughput, with approximately zero writes and queue depth near two; the staged 258.7 GB dataset itself is on the verified instance-store NVMe. The pinned path uses four `spawn` workers, a near-full 17,758,044-row in-memory filtered Arrow table and frame index, default eight-batch prefetch, and later creates a second loader for offline W&B sample images. This is root-container cold-read/cache-thrash evidence, not GPU work or a new network transfer. A read-only preflight re-bound the exact volume to running/healthy `i-02f230ed0529e2b14`, confirmed 256 GiB gp3/3,000 IOPS/125 MiB/s, encryption, and delete-on-termination. To preserve the completed staging and manually correct the observed infrastructure bottleneck without another instance, the only authorized live mutation is an in-place throughput increase on that exact volume to 500 MiB/s, with size, type, IOPS, attachment, filesystem, data, instance, and termination controls unchanged. Ohio gp3's additional 375 MiB/s is approximately `$15/month`, or about `$0.02/hour` and at most roughly `$0.10` through this run's remaining deadline, covered by the existing non-compute reserve. No model, dataset, command, seed, checkpoint, worker count, or training semantic changes.
100. The exact in-place volume modification began at `2026-08-04T13:25:42Z`, retained 256 GiB gp3/3,000 IOPS/encryption/attachment/delete-on-termination, and raised only throughput from 125 to 500 MiB/s. Observed reads immediately rose from approximately 7.869 to 19-20 GB/min, but no first batch returned; instead EC2 guest reachability became impaired at `13:34:00Z` while system and attached-EBS checks remained healthy. At the predeclared 45-minute first-loader cutoff, `13:37:46Z`, the prefix still contained only three initialization logs, no checkpoint, no run manifest, and no final-sync evidence. One ordinary `StopInstances` request at `13:38:19Z` used neither force nor OS-shutdown skipping. After a full five-minute graceful window left the instance in `stopping` with no late S3 evidence, one force-stop at `13:43:43Z` still did not skip OS shutdown or terminate/delete EBS. The instance reached `stopped`/code 80 at `13:48:10Z`; exact root volume `vol-0707f7877fed87e76` remains encrypted and attached, and the S3 prefix remains the same three singleton logs. Because `pi05-job.service` was started manually by one-time cloud-init but has no `[Install]` section and was never enabled, a bounded restart of this same already-reserved On-Demand instance will not rerun training. The restart is authorized only to restore SSM and read the retained root journal/Docker evidence; it must not start the job unit, pull an image, mount/stage DROID, run a GPU command, or alter the root. After evidence capture, terminate the same instance and verify deletion of its delete-on-termination root. This avoids both a replacement instance and an EBS snapshot while remaining within reservation `89948bbb-966b-4bae-a325-95af346c9928` and its original external deadline.
101. The bounded same-instance forensic restart began at `2026-08-04T13:49:31Z`; ordinary On-Demand lifecycle and `CapacityReservationPreference=none` were unchanged. SSM `f0b4a283-35be-4980-9398-ecf11cd3a5a8` completed read-only and proved `pi05-job.service` is `static`, `inactive/dead`, result success, PID zero, with no active training rerun. It also preserved the prior-boot unit journal. SSM `4a06a295-c293-4a54-b27f-4ee92bc744a3` found the exact training container stopped with exit 255 only when Docker restarted at `13:49:52Z`, `OOMKilled=false`, `Error=""`, and no restart; its retained 12,443-byte JSON log has SHA-256 `e9580acb2b02278dfdb92464bc6c6670e95482c3312be8696cc031a9f76ebb74` and ends with the same two worker-import warnings as S3 segment two at `12:53:59Z`. There is no prior-boot shutdown journal after the stop request, no OOM/killed-process evidence, and no additional trainer output. Read-only sysstat SSM `b90ea156-b3d2-47fb-9fc0-e6e885a7daf1` found zero swap activity and, at the retained sample, approximately 37 GB free RAM plus approximately 53 GB cache, while page scanning/stealing was extreme (`pgscank/s=82642.46`, `pgsteal/s=165271.04`) and I/O pressure was about 31.8%; this is page-cache/I/O thrash, not host or GPU OOM. The exact run prefix still has only the three singleton AES256 initialization logs and no delete marker, checkpoint, run manifest, final-sync evidence, or promotable/published input. The retained root has now yielded its relevant journal, container state/log hash, and sysstat evidence. Termination is authorized only for exact instance `i-02f230ed0529e2b14`; verify terminal code 48 and deletion of exact delete-on-termination root `vol-0707f7877fed87e76`. The failed one-batch command and immutable spec must never be replayed or resumed.
102. After a final account/instance/volume binding check, one ordinary `TerminateInstances` request targeted only `i-02f230ed0529e2b14` at `2026-08-04T13:55:59Z`; CloudTrail event `73ba9f7d-4a0d-4c23-a5ee-60fbfcd2be44` records `force=false` and `skipOsShutdown=false`. The instance reached `terminated`/code 48 by the `14:02:20Z` observation, and exact encrypted delete-on-termination root `vol-0707f7877fed87e76` now returns `InvalidVolume.NotFound`. The deleted root and previously erased instance-store scratch are not recoverable; the relevant journal, container and sysstat evidence was captured before deletion. The final exact A2 S3 inventory remains three singleton AES256 logs only: `container-000000.log`, 6,415 bytes, VersionId `BMzD_sHD6bBM9NyEF5NAPJcDcTBVocid`, SHA-256 `2e4b157cad01e3e65e940e826a1c709da0eaadd8004913e636ba39fb7d54512d`; `container-000001.log`, 980 bytes, VersionId `HrfDxEdOImoOHPsx_E9fW_WM_V.CN0LK`, SHA-256 `0a93782103f345cac68269b197b22d26b92b0f39df690e5b5abb82e2983e7cca`; and `container-000002.log`, 181 bytes, VersionId `5D80oe3xxZPMN9FBglxgko3eS7vgHMnq`, SHA-256 `24e8c89ad45b65281e51763856593fe41b3180e0f6db4055a2783ae9ec9f8ee5`. Exact-version downloads match each metadata hash. There are no delete markers or multipart uploads and no checkpoint, run manifest, final-sync evidence or publication. The original deadline schedule remains as a fail-safe for the already-terminal ID and has `ActionAfterCompletion=DELETE`; it cannot launch capacity. A Project+Workload query now returns zero pending, running, stopping, stopped or shutting-down Shallow workers.
103. The prepared LIBERO 2k controls became NO-GO after AWS aged successful A4 instance `i-0a5a2502e114aaedc` out of `DescribeInstances`, whose original logic required one returned terminated/code-48 record. The preparation and paid wrapper were patched locally to accept exactly one original terminal record or, only for a zero-result response, require the 8,558-byte A4 methodology record SHA-256 `7b9c88620709bdee6615ff9792e7533232bfff10dbd03ac250855726c6b21243`, exact manifest/final-sync tuples, the committed terminal sentence, exact historical ledger reservation `28b32b27-a6c0-4931-b409-e3017e7a2858` bound to that instance, and a fresh zero-managed-Shallow five-state query. They reject active, running, wrong-ID, multi-result, missing-evidence and ledger-mismatch fixtures. Patched preparation SHA-256 is `0d216cb9a93c2e1f6da86ad41b20ed4387a093c3eb9b597cfc7b9c81f50682e5` (28,472 bytes); paid wrapper SHA-256 is `967d4eaa046908191c8280a9ce517126e24134d7f9b2a1a32478d973de5fdc8b` (24,275 bytes), with exactly one paid launcher execution and the final zero-worker read immediately before it. During a fully authorized preparation replay intended to be mutation-denied, the parent zsh printed but did not export the `aws` shell function into child Bash, so the real CLI performed the sole intended create-once spec PUT at `2026-08-04T14:17:08Z`. Preserve it rather than creating a delete marker: `specs/libero-shallow-pilot-20260804t121807z-a1-2000.json` is one latest AES256 version, VersionId `KYMhVf9kJzJi5vPj7VULtXg8RFFXedBZ`, 5,371 bytes, exact template/metadata/download SHA-256 `4fa8421ca70606e522ef9b2b270147499d3ee1a21add5011e65a0c2c759dc0d2`, with no delete marker. The preparation then completed successfully and generated VersionId-bound bootstrap SHA-256 `a837bd7d0d8ed3d03a7b1f53ac44a57568c8112f69ad0c3df053745d076c4f86` (905 bytes) and read-only plan SHA-256 `52b20c80439c1df396d8e016eaaf93e689a422371590011ef17c3d91949816cb` (1,283 bytes), reserving no cost and launching no capacity. Root and independent audits found zero target run/output versions, delete markers and multipart uploads, zero target EC2 instances, zero active Shallow workers, no target schedule or reservation, no launch result, and the authoritative ledger unchanged at VersionId `yKuO4a8MHmMCbq0SXq2Ksc18.KpjbgVh`, SHA-256 `11a3e6da53f02629ebc194b3ebd5c8f255b75c0bc7a94d408999749c63222622`, 18,236 bytes, nine entries and `$199.186`, with zero target-label entries. All 122 controller/worker tests and the terminal fixture matrix pass. Review record SHA-256 `06964d203e168b97f9d24916e80c5249b86c4214591f3275057201c3318fe694` (17,153 bytes) remains NO-GO until this incident entry is committed, spec/plan modes are restored to `0400`, prep/wrapper remain `0500`, and a final independent review reseals current HEAD and hashes. No paid launch is authorized by this entry.
104. The final post-incident reseal at clean, non-shallow HEAD `cb3b214c1585a31eddeb1978e774ad54949facd1` passed independently. Every descendant of controller `0b679a3af0c2df4ae80e95b69ab0865be5aa98e6` is single-parent and RUNBOOK-only; runtime inputs remain byte-identical. Local modes are spec/plan/review `0400` and bootstrap/preparation/paid wrapper `0500`; their hashes remain exact except the truthfully resealed review, now 16,992 bytes, SHA-256 `b6abc4de529c52da70c73ac952db6ff300880e23d07c7dcb960d3da16c671a28`, with verdict `GO_FOR_ONE_LIBERO_2K_PAID_WRAPPER`. The exact bootstrap renderer comparison and full plan contract pass. A live audit reconfirmed singleton spec VersionId `KYMhVf9kJzJi5vPj7VULtXg8RFFXedBZ`, zero target outputs/multipart uploads/EC2 instances/ledger entries/schedules/launch result, zero active Shallow workers and Capacity Reservations, and unchanged ledger tuple `yKuO4a8MHmMCbq0SXq2Ksc18.KpjbgVh`/`11a3e6da53f02629ebc194b3ebd5c8f255b75c0bc7a94d408999749c63222622`/18,236 bytes/nine entries/`$199.186`. The one separately authorized paid action is the exact wrapper SHA-256 `967d4eaa046908191c8280a9ce517126e24134d7f9b2a1a32478d973de5fdc8b`, bound to controller bundle VersionId `K4N1WPDWTqVPq5mfRbMx_X6EV0eIErPd`, bootstrap VersionId `v9._MVJjb0A39zdU.6w.0c8ARXkywgUm`, spec VersionId/hash `KYMhVf9kJzJi5vPj7VULtXg8RFFXedBZ`/`4fa8421ca70606e522ef9b2b270147499d3ee1a21add5011e65a0c2c759dc0d2`, bootstrap SHA-256 `a837bd7d0d8ed3d03a7b1f53ac44a57568c8112f69ad0c3df053745d076c4f86`, and plan SHA-256 `52b20c80439c1df396d8e016eaaf93e689a422371590011ef17c3d91949816cb`. It may reserve at most `$16.99218` and launch exactly one ordinary On-Demand `g7e.4xlarge`, `CapacityReservationPreference=none`, for four requested plus 0.25 reserved hours; conservative remaining budget including the `$250` non-compute reserve is `$2,533.82182`. Do not replay preparation. The wrapper must revalidate the latest ledger/namespaces and perform the zero-Shallow read immediately before its one launcher `--execute`; any drift is a fail-closed stop.
105. The separately authorized paid wrapper completed successfully from clean HEAD `2d9a01afea0dddfe29bf6f64f3b70bb53b4f9adb`. Its 1,623-byte launch result has SHA-256 `ff48e3d0124292ef9911c158f191586f32cbd246cb34aa8b7ce4c957b0ca7940`, mode `executed`, reservation `8885a696-c292-4585-9877-ef651d31bcb6`, and exactly one instance `i-050a00ebfd76fb0c8`. Live readback proves ordinary On-Demand `g7e.4xlarge` in `us-east-2a`, `CapacityReservationPreference=none`, IMDSv2 required/hop limit one, instance-initiated shutdown `terminate`, exact project/workload/name/command-hash tags, and encrypted 256 GiB gp3 root `vol-0963840e6b264813f` with 3,000 IOPS/125 MiB/s/delete-on-termination. External schedule `pi05-deadline-8885a696-c292-4585-9877-ef651d31bcb6` is enabled, targets only that instance at `2026-08-04T18:32:43Z`, and has `ActionAfterCompletion=DELETE`. The current ten-entry authoritative ledger is VersionId `vCsnbHjcYfSrLiXKglc.Akkt85ixYBAl`, SHA-256 `1a3ae16caad5da52798c3f70991065830a6306bab5a725afa081c37bceb08504`, total `$216.17818`; the exact target entry is state `launched`, On-Demand cost reservation `$16.99218`, four requested plus 0.25 reserved hours, `launch_error=null`, and bound to the same instance/schedule/command hash. No Capacity Reservation was created. Only read-only monitoring is authorized until a terminal artifact or separately recorded bounded intervention gate.
106. DROID A2's first-loader failure is corrected in reviewed model-source commit `41072947f77b004662649f19443f6fccdce5290f`; its four-file patch SHA-256 is `e3a7c97b650e10ecd6809a52287cc92525809b9d9bc2d1c87b3c429035d7cfac`. Large LeRobot v3 training splits now construct the full memory-mapped dataset with `episodes=None` and apply the existing compact whole-episode range view, avoiding the eager near-full `to_table(filter=...)` and 17.76-million-entry absolute-to-relative dictionary while preserving absolute frame/delta/video/split semantics; small v3 validation subsets retain native filtering and v2 behavior is unchanged. One-batch mode now fails before DDP/checkpoint/W&B mutation unless `num_workers==0`; disabled W&B creates no sample loader and enabled one-batch W&B reuses the already materialized batch. The intended fresh retry command must explicitly include `--one-batch-overfit --num-workers 0 --no-wandb-enabled`; A2 remains non-resumable. Root and independent reviews found no correctness blocker. Focused/golden/equivalence runs passed 49+19 tests, the combined controller/worker/trainer/loader suite passed 144 tests, Ruff check/format and full strict fsck passed; one broader unrelated legacy JAX/Orbax test remains excluded because the local JAX lacks `monitoring.record_scalar`. The clean commit's complete-history single-HEAD bundle is 1,797,045 bytes, SHA-256 `e61aa80ac7a4eb9d4c5d0db78f8cb43c1a164d07915a6284ee645d5a6444ce78`, local mode `0400`; fresh bare fetch/fsck and clean non-shallow detached clone/fsck pass. Exact destination `source/openpi-41072947f77b004662649f19443f6fccdce5290f-complete.bundle` has zero versions and zero delete markers. One create-once AES256 PUT of only that exact bundle is authorized after rechecking the empty history and account; require metadata `source-commit=41072947f77b004662649f19443f6fccdce5290f`, `sha256=e61aa80ac7a4eb9d4c5d0db78f8cb43c1a164d07915a6284ee645d5a6444ce78`, and `history=complete`, then exact-VersionId round-trip, singleton/no-delete, complete-history, single-HEAD and fresh-clone/fsck verification. This authorizes no spec, ledger, S3 run output or capacity mutation.
107. The exact DROID loader-correction bundle was create-once published as the only authorized mutation from entry 106: `source/openpi-41072947f77b004662649f19443f6fccdce5290f-complete.bundle`, VersionId `adkWlX8nTbZhbCV44O2RymLgW6ca.cXZ`, 1,797,045 bytes, SHA-256 `e61aa80ac7a4eb9d4c5d0db78f8cb43c1a164d07915a6284ee645d5a6444ce78`, AES256, metadata `source-commit=41072947f77b004662649f19443f6fccdce5290f`, `history=complete` and the same SHA. It is the latest and sole version with no delete marker. Exact-VersionId download matched local bytes and hash; bundle verification reports complete history and exactly one HEAD at the declared commit; a fresh empty bare fetch passed strict full fsck, and a fresh working clone is non-shallow, clean, detached at the exact commit and passes strict full fsck. No spec, ledger, run output or capacity mutation accompanied publication.
108. A fresh DROID A3 draft correctly remains NO-GO before spec publication. The existing DROID-v3 image `sha256:2afcc58cda27681892c7bbb9554e9603024c5b74f53358fad893ea876374803c` is amd64 and labeled `org.opencontainers.image.revision=229c08ea2a13a70cbbf1a9c8a1f31cb1ca674dee`; worker identity requires equality with corrected source `41072947f77b004662649f19443f6fccdce5290f`, so reusing it would fail before Python. The ignored read-only A3 template/bootstrap/stale-plan/NO-GO-review drafts are 5,726/913/1,283/4,366 bytes with SHA-256 prefixes `857a2d2d`/`a7696b01`/`0a50d750`/`cdcd24b1`; structural validation passes, but they are not final controls and no A3 spec, ledger entry, instance or wrapper exists. Existing workbench `i-038112fe75c610517` is stopped/code 80, ordinary On-Demand `g6e.4xlarge` in `us-east-2a`, `CapacityReservationPreference=none`, with its preserved encrypted 1 TiB gp3 root `vol-0de00b022d7154c96` at 3,000 IOPS/500 MiB/s; no deadline schedule remains. A bounded reuse is authorized solely to build, GPU-smoke and create-once push the DROID-v3 policy image for source `41072947f77b004662649f19443f6fccdce5290f`, using the existing Docker cache and the exact published source bundle rather than live files. Cost guard projects 2.25 billed hours at `$3.00424`/hour, `$6.75954`, raising conservative AWS reservations from `$216.17818` to `$222.93772`; with the `$250` non-compute reserve, `$2,527.06228` remains under the `$3,000` cap and workbench-category reservations remain far below `$350`. Before `StartInstances`, append reservation `be174269-5d0d-4e5f-aca7-912c13c51935` to the exact latest ledger by ETag CAS, bound to the existing instance, label `pi05-workbench-droid-image-4107294`, two-hour maximum plus 0.25 reserve, shutdown `stop`, and deadline `2026-08-04T17:00:00Z`; create and read back external auto-deleting schedule `pi05-deadline-be174269-5d0d-4e5f-aca7-912c13c51935` targeting `StopInstances` for only that ID, then start only that stopped instance and install a matching guest stop timer. Build only immutable tag `droid-v3-41072947f77b004662649f19443f6fccdce5290f` from `git archive` of the exact complete-history bundle, with DROID LeRobot revision `0b067df57d21d3a02d6c511f1609172fa39ac29b`; require empty ECR tag before one push, amd64 and all provenance/dependency/decoder labels, and network-disabled L40S smokes for Torch/JAX CUDA, PyAV, v3 LeRobot and no-CPU-fallback ONNX Runtime CUDA both before push and after digest pull. Preserve the build log create-once under `manual/image-builds/droid-v3-41072947f77b004662649f19443f6fccdce5290f/`, update the ledger to launched by a second CAS, and stop the workbench immediately after durable evidence. This authorizes no LIBERO mutation, A3 spec/launch, mutable tag replacement, dependency change, Capacity Reservation or second instance.
109. Reservation `be174269-5d0d-4e5f-aca7-912c13c51935` was appended by ETag compare-and-swap before capacity started: the reserved eleven-entry AES256 ledger is VersionId `u6Nd7vMvT2kUoN7JmlJ5j2o7Fl.LP_VJ`, SHA-256 `897693f03296f7a439336f7c2238ee13ba8aa52eaa29a274acefab37e7a55eb9`, 21,177 bytes and `$222.93772`. External self-deleting schedule `pi05-deadline-be174269-5d0d-4e5f-aca7-912c13c51935` was then created and read back enabled with exact `StopInstances` input for only `i-038112fe75c610517` at `2026-08-04T17:00:00Z`. The same existing ordinary On-Demand workbench entered running at `15:00:02Z`; no new instance or Capacity Reservation was created. A second ledger CAS records state `launched`, the exact instance and schedule, VersionId `gqbGajZYQ3SoMxcZ7ACc71glQySnZz6t`, SHA-256 `0d6998e123dd6cb0515b0c8a10e6cc3313499463a81e978cf9e5be8f0d2f35a6`, 21,415 bytes and the unchanged `$222.93772` total. SSM command `065d7d00-90ae-414f-8719-696714ad8616` replaced the retained stale guest timer calendar and verified its next trigger at the identical `17:00:00Z`; EC2 confirms instance-initiated shutdown is `stop`. Host preflight found one L40S/46,068 MiB with driver 595.71.05, Docker 29.6.2, 421 GiB free root space, 521 GiB free initialized instance-store scratch, and the expected cached 229c training images/base layers. The exact new ECR tag and attempt evidence prefix are empty. The reviewed, ignored mode-`0400` build control is 22,385 bytes, SHA-256 `ebd7a4c1a9743af3f32780d897b9813f10040e545166bb942430771e01541e9e`; it verifies account/IMDS on-demand lifecycle, exact launched ledger bytes, bucket versioning, ECR URI/immutability/encryption/scan settings, conditional-PUT CLI support, bundle metadata, complete history in a new empty bare repository, and a fresh clone/full fsck of the exact `41072947` bundle. It builds only the v3 tag with the existing cache, repeats the repository-contract and tag-absence gates immediately before its sole push, verifies the simulator/decoder/dependency labels, and requires pre-push plus post-digest-pull network-disabled GPU/runtime smokes. It conditionally publishes only an exact attempt build log, structured evidence and publication receipt; each PUT is followed by exact-VersionId byte/hash/HEAD metadata validation and a singleton/no-delete version-history gate. Execute it once through SSM as a gzip/base64 payload whose remote SHA is checked before explicit Bash invocation. A failure leaves the same workbench available for bounded in-place diagnosis until the dual deadline; success is not accepted until the three S3 objects and ECR digest independently verify, after which stop this exact workbench immediately. No DROID A3 spec or worker launch is authorized here.
110. The first encoded-control dispatch, SSM command `423d7a69-a753-4e29-af3e-7f02634f8560`, exited in 0.064 seconds with response one because this host's `dd` rejects `oflag=excl`. It failed in the transfer pipeline before creating the remote control, attempt root, Docker build, ECR tag or S3 evidence; read-only SSM `c624447f-70a8-4788-bc9b-f450b878408b` verified both remote paths absent, while controller readback still returns `ImageNotFound` for the exact tag and zero evidence-prefix versions/delete markers. Preserve this failed command as installation evidence; do not replay it. Independent review also found that the instance role deliberately lacks `ecr:DescribeRepositories`, making the build control's redundant host-side repository-contract readback a certain pre-build AccessDenied. Keep that contract in an immediate controller-side readback, where the authorized user can require exact repository URI, `IMMUTABLE`, AES256 and scan-on-push, and remove only the inaccessible redundant host calls. The corrected ignored mode-`0400` control is 21,160 bytes, SHA-256 `d1d532ee613e7959cf989cc581611a8d00888cc7a4a53af1ec33516e2fc64dca`; every source, ledger, build, smoke, tag-absence, push and exact S3 publication gate is otherwise unchanged. Transfer it to a fresh hash-qualified remote path through a fresh temporary file, verify its hash, and create the final remote path with an atomic hard link; retain the temporary hard link as evidence rather than deleting it. Immediately before one corrected SSM dispatch, recheck the live ordinary On-Demand/CR-none instance, dual deadline, exact latest ledger, empty tag/prefix, S3 versioning and controller-visible ECR repository contract. This bounded in-place correction uses the already-running reserved workbench and authorizes no new ledger cost, capacity, tag, prefix, LIBERO mutation or A3 spec/worker.
111. Corrected build dispatch `bc27c0a5-f20b-43ff-b3e6-0ffd195a67ca` verified both transferred hard links against SHA-256 `d1d532ee613e7959cf989cc581611a8d00888cc7a4a53af1ec33516e2fc64dca`, then completed the exact source-bundle, complete-history, fresh-clone/fsck, dependency, configuration, PyAV and LeRobot-v3 Docker gates. It exited response one after 20m55s only because the added local identity check expected `docker image inspect --format '{{json .Config.Entrypoint}}'` to render literal `[]`; BuildKit canonically omitted the key for Dockerfile `ENTRYPOINT []`, so the Go template failed before local GPU smoke. Raw read-only SSM `36cbed75-4203-4f14-9e2b-663f25ac8862` proves the retained image is Linux/amd64 ID `sha256:50b0845b8e826364c1e9f0a61a5a43b8c6dced7818b6f236f46a77ad182b83cb`, 13,228,573,367 bytes, has no `Config.Entrypoint`, has `Config.Cmd=["bash"]`, and carries every exact source/upstream/v3/tokenizer/policy/simulator/PyAV/ONNX Runtime label. The exact ECR tag still returns `ImageNotFound`, and all three attempt evidence histories remain empty; no smoke, tag, push or S3 write occurred. Preserve the built image and 264,085-byte partial build log rather than rebuilding. The reviewed recovery control is mode `0400`, 17,389 bytes, SHA-256 `4463a9547354d9909051a3db9d7fc62c0aae8c7324e4bf997acec9cc1932c322`. It pins the retained image ID and original control/attempt bytes, accepts only absent/null/empty entrypoint together with exact `Cmd=["bash"]`, revalidates source/ledger/tag/prefix, performs the required local network-disabled GPU smoke, repeats the absent-tag gate, pushes once, pulls by the returned digest, repeats the label and network-disabled GPU smoke, appends recovery evidence to the retained build log, and publishes the same three create-once exact-VersionId artifacts. A live independent audit returned GO and found the instance role permits every host call. Immediately before its sole SSM invocation, controller-side checks must again bind the running ordinary On-Demand/CR-none instance, exact ledger/deadlines, immutable AES256 scanned repository, absent tag, and empty S3 histories. This authorizes only that in-place recovery, never another Docker build, another tag, new capacity, LIBERO mutation or A3 launch.

112. The retained-image recovery was executed exactly once by SSM command `2fac373f-db57-4dcf-a5ac-0c38759a1d63` from `2026-08-04T15:49:41.315Z` through `15:50:31.315Z`; it returned `Success`/zero. Both pre-push and post-digest-pull network-disabled GPU/runtime smokes passed on the same L40S: Torch reported CUDA 12.8, JAX selected `CudaDevice(0)`, PyAV decoded LeRobot-v3 media, the v3 dataset/runtime imported, and ONNX Runtime used CUDA with no CPU fallback. The one immutable ECR tag `droid-v3-41072947f77b004662649f19443f6fccdce5290f` resolves to the unique OCI image-index digest `sha256:50b0845b8e826364c1e9f0a61a5a43b8c6dced7818b6f236f46a77ad182b83cb`, whose reported compressed size is 13,228,563,825 bytes; repository immutability, AES256 encryption and scan-on-push remain enabled. A direct scan-findings lookup returned `ScanNotFoundException` for the OCI index, so no vulnerability-scan result is claimed or used as an acceptance gate. Recovery-control SHA-256 `4463a9547354d9909051a3db9d7fc62c0aae8c7324e4bf997acec9cc1932c322` and the successful command ID supplement the original failed-control fields retained inside `evidence.json`. Exact create-once AES256 evidence under `manual/image-builds/droid-v3-41072947f77b004662649f19443f6fccdce5290f/20260804T150449Z-i-038112fe75c610517/` is: `build.log`, VersionId `5WbG00WlyFKWTPVJZkI1v9NY555D3E24`, 268,945 bytes, SHA-256 `e686dea99391e3c2ee415bdffbcce2d36b73afa2fec62867f5772a62e3d7321c`; `evidence.json`, VersionId `TLKqvJ2mdpZLgfKOLBwNnADII2DM5ofg`, 2,513 bytes, SHA-256 `2d82fbba0dec1f8420aa8a2185f35077afd1a3cfe0c414af10abddfa2c132a6f`; and `publication.json`, VersionId `DETCJiaFhDLSUTYOERrgfumM_5a.OSKe`, 1,120 bytes, SHA-256 `b3b69bcd99f7db5a72d471456515baf84a1f194ced062f8e79bbb080db93a90f`. Controller and independent audits verified exact-VersionId round trips, metadata hashes, three singleton latest versions and no delete markers. `StopInstances` was issued only for workbench `i-038112fe75c610517` at `15:52:01Z`; it was read back stopped/code 80 at `15:58:26Z`. Its encrypted 1 TiB gp3 root `vol-0de00b022d7154c96` remains attached/in-use at 3,000 IOPS and 500 MiB/s. The independent `17:00:00Z` stop schedule remains enabled and auto-deleting as a harmless fail-safe. No new capacity, ledger reservation, DROID A3 spec or worker was created by the recovery or stop.

113. While the LIBERO 2k worker continued without intervention, commit `1e957e46eace26ea95df34fb9ee7c34cbc4c15fd` hardened the later SnapFlow and offline-evaluation boundary without mutating AWS. Fresh SnapFlow controls now accept only direct single-process Python on the exact LIBERO distilled or DROID distilled/BC25/BC50 numeric source descriptor, the batch-four/BF16/accumulation-one recipe, a non-published 300-step diagnostic or published 5k pilot, exact output path and terminal save interval, and no unreviewed, equals-form or negated Tyro option. Continuations restore full state only across `5000->10000->20000->30000`, retain the 5k save interval, forbid model-only/teacher overrides, and require DROID `num_workers=0` with W&B disabled on `g7e.2xlarge`. The evaluator exact-allowlists the released full-depth teacher for Shallow/BC and the accepted numeric Shallow/BC teacher for SnapFlow, binds the student's `shallow_teacher_transplant` or `pytorch_source` model hash, full-validates numeric SnapFlow teachers, records lineage, and permits BC recovery to reuse the canonical paired DROID corpus while explicitly not treating it as a BC-held-out generalization estimate. The manual runbooks now use exact numeric source variables and distinguish W&B's resume identity from the worker-enforced loader count. Independent review found no blocker; root verification passed Ruff format/check, Python byte compilation, `git diff --check`, and 174 focused worker/evaluator/checkpoint/trainer/model/config/promotion/quality/RoboLab tests. Offline promotion acceptance deliberately remains a manual launch-review gate. Any worker or evaluator that relies on this commit must use a newly published controller/source bundle and commit-matched immutable image; existing 4107294 DROID Shallow image remains valid only for the already-scoped A3 loader diagnostic.

114. Commit `967771c19057d0fabe1679290155c7be1827c6d2` hardened the next manual stages without mutating AWS. Corrective single-GPU LIBERO Shallow resumes now fail closed on the exact `2000->5000->10000->20000->30000` ladder, direct-Python argv, numeric checkpoint input/output identity, and `corrective_run`/`shallow_training`/`g7e.4xlarge` launch metadata; ordinary two-process resumes remain distinct. LIBERO paired-rollout stages now use the raw full model SHA-256 accepted by the evaluator and bind its role through the quality report. The existing canonical equivalence manifest is explicitly not misrepresented as a worker artifact: offline evaluation must first publish a separately reviewed, create-once adapter that inventories the same four exact-VersionId payloads. Stage 4 now passes the action mask into TensorRT latency diagnostics, so padded model dimensions cannot fail or dilute the active-joint gate; CUDA ONNX validation registers only `CUDAExecutionProvider`, disables CPU execution-provider fallback, and rejects any active CPU provider. The runbooks also bind every production phase for a track to one final runtime digest, correct the compiled publication directories to `artifacts/tensorrt/{libero,droid}/fp8`, and identify live G7e hardware as RTX PRO 6000 Blackwell rather than L40S. A broad local sweep first exposed and then fixed two integration regressions: the shared metadata validator again accepts the separate RoboLab spec shape, and the compiler contract retains its tested fail-closed wording. Final verification passed 473 broad script tests in an invocation that excluded checkpoint conversion and `scripts/train_test.py`, then all eight checkpoint-conversion tests separately; it also passed 141 focused continuation/compile tests, seven targeted regression tests, Ruff 0.11.12 check/format, Python byte compilation, and `git diff --check`. Only `scripts/train_test.py` failed when separately reproduced, due to the existing local `jax.monitoring.record_scalar`/Orbax incompatibility; checkpoint conversion is not affected. No S3 object, ledger entry, image, schedule, or capacity was created or changed.

115. LIBERO run `libero-shallow-pilot-20260804t121807z-a1-2000` completed all 2,000 optimizer steps and durably closed before its deadline. The final hash-covered training sidecar reports loss `0.023423800710588694`, FM MSE `0.012865463446360081`, KD MSE `0.010558337933616713`, KD cosine `0.9949779137969017`, and per-joint NRMSE mean/max `0.05419031856581569`/`0.28219831362366676`; it is VersionId `WPAbu63Th2oNP9o6bwBHgugwJhu9.EWe`, 574 bytes, SHA-256 `7801a0631e08bef4759c0b71305928329705b97bc078019ee28b86e85b58c1c9`. The reusable seven-file checkpoint is 13,386,467,850 bytes. Its exact worker-input descriptor has revision `1e839cd780345dd1703c837641d30599f05ed6cf13318eb0aede9e3869b9f0be`; manifest VersionId `NzEi.GFCLeKqQKQPN8kVqAEmPa1dNX7G`, SHA-256 `71c717849c7e4764ff3d3776850ce3527ad589bfbbe7b985b0b559cb54c6e5bf`, and 4,222 bytes. The succeeded/exit-zero run manifest is VersionId `uIYVsHOnNIDGP.CAo9xHiiuacxbDnVD2`, SHA-256 `88ce997f84f80f32987b3fd0c5a43bced0f27fa50f712256d7975b2d14ff0326`, 134,165 bytes; final-sync evidence is VersionId `ZGz0hs.grUsF_aB7NNw4EtH0gmcw3K7V`, SHA-256 `eb4c9be012c93727f4c3a1a0d59ec87b5b44f35f6316ffac2d8b8fe221d002c2`, 844 bytes. A local read-only auditor was iterated in place after its first replay duplicated an already-present `checkpoints/` path component and its second omitted exact object VersionIds from the worker revision hash. A third replay reached only the expected nonterminal EC2 gate; after AWS pruned the terminated instance's shutdown-behavior field and normalized its reason to `User initiated`, the final rule retained the immutable launch-result `shutdown_behavior=terminate` check while accepting the terminal pruned readback. Final helper SHA-256 is `691b3b51d3454635bf1700989cfb30a51fe74769b7b016a2c6e804006482226b`; its PASS evidence is mode `0400`, 3,444 bytes, SHA-256 `df1d7ea48e69b009fac3b2827e3a9fd9e6dd4c27cc1ae0ac74913cfde6ccfca5`. It exact-VersionId validated 190 pre-manifest receipts, 197 singleton objects / 13,387,143,790 bytes, every HEAD tuple, zero delete markers, exact prefix closure, instance `i-050a00ebfd76fb0c8` terminated/code 48, and zero managed nonterminal Shallow workers. No corrective AWS mutation was needed.

116. After entry 115 closed, controller commit `db12a82bb1755f110bf75dd56873235693964494` was published through the independently reviewed create-once control. Its complete-history, literal-single-`HEAD` bundle is VersionId `cq47BFoavO9nNuyJkxwEsr9mhA38R7wF`, 1,876,345 bytes, SHA-256 `6db298902211513c2380fd6e2b6c583d07a70320dfe71a75c0e03ef974c95f64`; the identical 8,888-byte worker bootstrap is VersionId `J8AiPuPiokG.wjNB7sETBlhQ_E9KzP.l`, SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`. Both are sole/latest AES256 versions with no delete marker and passed exact-version round trip, fresh bare fetch, strict fsck, clean detached checkout, and Bash syntax gates. A first local 1,908,949-byte bundle with custom ref instead of literal `HEAD` was rejected before upload and preserved as local evidence, SHA-256 `7a34989733466b2b8661e308007cbbcaf1a48b0974a2f9a62bf88f8914feed66`. The reviewed LIBERO equivalence adapter was then created once at revision `2207208d27ffa46b9ba4087927355c1d7079d22a143f2350358e6fc332c24698`: VersionId `C9PZMbBXAw29QHQrzZ964p8Ewoo6kllM`, 1,671 bytes, SHA-256 `f798006e62ab9b8e96780e28fe3f109e82a38e926b932e8b0ed98fffd115a176`; it inventories the four original exact-VersionId corpus payloads without copying them. The resulting mode-`0400` worker descriptor is 1,534 bytes, SHA-256 `49cc5539d4d7e572935b3ea9a92313e13a6047fbda5e4cf6d9745d8bae70bad2`. The authoritative ledger remained VersionId `gqbGajZYQ3SoMxcZ7ACc71glQySnZz6t`, 21,415 bytes and `$222.93772`; no spec, schedule, ledger, image, or capacity mutation accompanied these three fixed publications.

117. DROID one-batch diagnostic `droid-shallow-overfit-20260804t144649z-a3-300` crossed the paid boundary only after a final byte-identical readback of its sole create-once spec: VersionId `LHFIV9S53Ja3r0FUQgJIRuJKJlv7O5Xd`, 5,726 bytes, AES256, SHA-256 `86b9846287c097ca0091f434f0ddeaeed3108f196b0477f4c76ba838ec487451`. Local bootstrap-command and reviewed-plan hashes were respectively `b1b7a1064a7ea6cd5eaea4e867e65616d9e33cde19a445813c7aa77d3de0f9e6` and `c4a425b036e52b9487808ebe5b97fc50565e67e187b597c38e187059ded6328a`; both launch scripts passed Bash syntax checks. The first operator invocation mistyped the plan-hash environment variable. The independently constructed exact authorization string therefore failed closed with exit one before the wrapper could reach its paid launcher. Immediate readback proved that no local launch result, matching EC2 instance, or matching ledger entry existed. The invocation was corrected in place by copying the literal reviewed hash; no spec or infrastructure was recreated. The successful wrapper wrote mode-`0400` launch result SHA-256 `1c4ee6b8a27eb67401e360cbbaeb71deaff0011108f524b37d7267a70234bc07` and created exactly one ordinary On-Demand `g7e.4xlarge`, `i-03e600b158a55d0e8`, in `us-east-2a`, reservation `412a655b-9dc4-43f1-bcf0-bedce21fcf2a`. Live EC2 readback confirms `InstanceLifecycle=null`, `CapacityReservationPreference=none`, required IMDSv2, instance-initiated shutdown `terminate`, and encrypted delete-on-termination 256 GiB gp3 root `vol-06d61261bec36aca4`. Self-deleting EventBridge schedule `pi05-deadline-412a655b-9dc4-43f1-bcf0-bedce21fcf2a` is enabled with no flexible window and exact `TerminateInstances` input for only that instance at `2026-08-04T22:08:38Z`. The 4.25-hour conservative reservation is `$16.99218`; authoritative ledger VersionId `DYCVww4VxKdXBcyW6ZdzNxuyeEDM2oUW`, 23,437 bytes, SHA-256 `2b7ce01652e9cba9e31781c84a4d35c23b1b7c6da0d7bad45baa1d02055a0f6b`, now contains twelve entries totaling `$239.92990`, leaving `$2,510.07010` after the `$250` non-compute reserve. SSM reached `Online` while EC2 status checks were still initializing. The worker remains bounded by 45 minutes for staging/container startup, 20 minutes from loader start to first batch, 15 minutes from first batch to optimizer step, 60 minutes from step zero to step 300, and 20 minutes for terminal publication; diagnose it in place within this same instance rather than launching a replacement.

118. That DROID A3 diagnostic completed all 300 optimizer steps, published terminal evidence and terminated normally without a corrective instance. The final run manifest is VersionId `O7JQQs2VQaB0KCjDJwkYK1O4AN9S2EAI`, 46,802 bytes, SHA-256 `9d2239bdafdf9a229aa91bd3fef6927566dfef74a04eb4a581955287f1dc2942`; final-sync evidence is VersionId `RPjmGpIPTPZ5kT7SczioHnc6JNZLXBqm`, 842 bytes, SHA-256 `ac3eb19cab0c0ad0c3efda0b886ffea1a1a260251f61479cc8edee60f95ea652`. The final training sidecar reports KD MSE `0.00013768166536465287`, cosine `0.9999799728393555`, and per-joint NRMSE mean/max `0.009729010984301567`/`0.011025791987776756`. Its first-20 and last-20 one-batch loss means are `0.0905081432312727` and `0.00014596249820897356`, a finite relative decline of `0.9983872998274199`, so the one-batch gate passes. Model SHA-256 is `468650a13968ad41054c19603b2aa6539b832dc7590448a4be2899b1f654e86d`. A read-only terminal auditor initially assumed the metrics object was unwrapped, then assumed `receipt_count` excluded the manifest receipt; both schema assumptions were corrected locally and replayed without an AWS mutation. Final auditor SHA-256 is `f464b7f8c44ea9b097adb71d125bf380066cdf316618eaf9562e039207b979ac` and its mode-`0400` PASS output is 2,090 bytes, SHA-256 `369fe02bd16d600ac9187f441baf6a042d98ce22ef00703a38c366f414a19ca4`. It verified 40 singleton objects, 38 receipt payloads, exact round trips for 36 small payloads, the two large checkpoint HEAD tuples, zero delete markers, and ordinary On-Demand instance `i-03e600b158a55d0e8` terminated/code 48.

119. The stopped persistent workbench was reused rather than replaced for the LIBERO db12 image/evaluation work. Reservation `69c8fe7f-e18c-4d88-bac4-4dd935322554` adds a conservative 5.25 hours / `$15.77226`, yielding authoritative thirteen-entry ledger VersionId `ASdXvNqXf0C_8Rg8SO.hSrC5tCxwU1kJ`, 24,628 bytes, SHA-256 `833c1d15b93300010a121b745e30c279bc5a769798e789682ce0f994b63837b7` and total reserved spend `$255.70216`. Instance `i-038112fe75c610517` remained ordinary On-Demand `g6e.4xlarge` with `CapacityReservationPreference=none`. Its first replacement cloud-init boothook retained a stale guest timer and blocked normal cloud-init completion; the same instance and root volume were kept, a corrected v2 boothook was installed, and cloud-init/SSM recovered without another start or instance. External self-deleting schedule `pi05-deadline-69c8fe7f-e18c-4d88-bac4-4dd935322554` and guest `pi05-hard-deadline.timer` were both read back enabled for exactly `2026-08-04T23:15:00Z`. This manual recovery must be replayed as an explicit idempotent timer/cloud-init update, never as instance replacement.

120. The db12 LIBERO v2 policy image was built once on that retained L40S. SSM command `9c1e94b9-5cbb-4486-9660-2fd7a1ace926` used control SHA-256 `28ad8659d85ec276041367eb111a6a48d7d96c2de768a4e2ecb9ee5e9cb55511`, completed in 3m24s, and passed a local network-disabled CUDA/Torch/JAX/PyAV/LeRobot-v2/ONNX-CUDA-no-fallback smoke. The first publication control stopped before ECR mutation because the scoped workbench role lacks the redundant `ecr:DescribeRepositories` permission; controller credentials had already proven the immutable/AES256/scan-on-push repository contract. Recovery retained the built image, removed only that inaccessible redundant check, and SSM `4d60a184-e02c-4d17-bfd6-34ed4249397b` published immutable tag `libero-v2-db12a82bb1755f110bf75dd56873235693964494` at OCI index `sha256:71652fdcf2f8dcb8ac143a5af68b61c115a0c5896f5b829fa19668a55129319d`, amd64 child `sha256:b3b04ee052f8085b4d9a734b5319b69f419a576cd196c28116b3853912f2069d`, config `sha256:4a4274d6b49ad4d73ee957c1aafac7ed19199f9a77987a13a2c05ebd537c3bf8`, and 13,130,416,507 compressed bytes. Post-digest-pull network-disabled smoke also passed. Create-once evidence prefix `manual/image-builds/libero-v2-db12a82bb1755f110bf75dd56873235693964494/20260804T182000Z-i-038112fe75c610517/` contains exactly four singleton versions and no delete marker: failed pre-mutation log `rDTd.iIueCsrwiIbSJ96w.UVjVpZWiKM` / `f7c740913ce98277b5f719126236482b1c254d3054ac398ef2fff087bb97e1dd` / 433 bytes; recovery log `DVOZTNSn09TAeLf391dxhhFYxKtNmIW5` / `eb254e50595bd12252843c51c320ebee60c80887fd73a40d6cf9793dbedf6ddc` / 4,654 bytes; evidence `.JvA6Czq_FHfLI_DHz5Qx70yQJ98fdJU` / `fe048b524232b167a68c9cac0cd8438fd795138cc7e6261857f3260a980ebb1f` / 2,015 bytes; and receipt `ZWvwa9LvLXjvk7wKsDtwS6S3Z.AZKL4n` / `50debc400d8da3b300cddd2a5280f780caa7f519c4cba80fdf918887fb936db7` / 1,452 bytes.

121. The db12 LIBERO evaluator was then built from that exact parent policy image. Build control SHA-256 `cb5df2ad5398bbe443dce137fdf9eecf00631f9556704a0a4fd974c38f8a8c17` ran as SSM `c13f205b-70c6-4489-81c5-85086a73f51e` and produced retained local ID `sha256:f142f7119538416f578234a1b492437121e6aa89f0f213e9eb952461c2b0554c`. Its policy-runtime smoke passed; the evaluator half failed only because the test command omitted the required `PYTHONPATH=/opt/openpi/packages/openpi-client/src:/opt/libero`, producing `ModuleNotFoundError: openpi_client` after the image was already valid. Recovery control SHA-256 `22f59d32f4e2f5d872d31d40134b4637d75d740b86b1d8cdaaf4ff779038acfc`, SSM `e721eadb-672e-4662-8c15-31ebc3309e1d`, reran the exact retained image with that environment and passed Python 3.8/Torch 1.11 CUDA plus real EGL rendering. Publication SSM `0b542d9d-5da8-4c5e-a65b-fb18af57077f`, control SHA-256 `edfb81f55729cd010c53c205ce322e4321300d934d448b9d6bee6afc0b68c044`, published immutable tag `libero-evaluator-db12a82bb1755f110bf75dd56873235693964494` at index `sha256:f142f7119538416f578234a1b492437121e6aa89f0f213e9eb952461c2b0554c`, amd64 child `sha256:c7ad995827225b2f6dba61fee696a944ed4a5017e6f022a4fba827d48400f651`, config `sha256:d13769131487b68fe0310baeb6528144f5e3f54eb3a12516018c6c4b8190659f`, and 15,581,088,149 compressed bytes. Read-only closure auditor SHA-256 `e9c3eab55a647979e6adc27fa22da618a2547c3c22e359e9ac7c289539b1347d` produced PASS output SHA-256 `e8eddfeb906c63cd7d8ed03e247867eedc77184f3f14f2e3ca3fbeb63a0b5127`; it exact-downloaded/rehashed the five singleton evidence objects under `manual/image-builds/libero-evaluator-db12a82bb1755f110bf75dd56873235693964494/20260804T185500Z-i-038112fe75c610517/`, verified their receipt linkage and found no delete marker. Their VersionIds are build log `J1xwt8O5WvfjrU_5HmbFMZKh0O5lhX_e`, smoke-recovery log `dUEzf9j8w4aCt1R_TNN.uW85WDhct3CN`, publication log `XT4Hsr5MGjY9QcYs5rKV1i6u6Poy1mMm`, evidence `GhSCs8AN9aqh2oprUqtUqUDuY0nFQhK_`, and receipt `7zHFS7J1FKwMhqbxNVdEDdcYh.CNhcTV`.

122. The retained workbench then performed the first empirical LIBERO 2k offline gate without another capacity reservation. After a read-only preflight confirmed the dual deadline, 521 GiB empty initialized NVMe scratch, exact cached evaluator digest, and exact retained teacher, manual control SHA-256 `a301214762b91a9aa3010023b52f58b2002f3e26eb6aad079741d33f58d67ab8` ran as SSM `07df247c-c033-4858-a1ea-021bfaa0be00`. It exact-VersionId staged and hashed the complete 13,386,467,850-byte resumable student checkpoint plus the four-file golden/equivalence artifact, validated the retained 7,473,091,464-byte teacher, and ran 64 fixed observations at batch four in the db12 evaluator with Docker networking disabled. Transfer, full optimizer/checkpoint validation, inference, and create-once publication took 5m49.717s total. The action-chunk result is KD MSE `0.011716030738783714`, cosine mean/p05 `0.9466431399191352`/`0.8760831652772526`, RMSE `0.10824061501480724`, maximum per-joint NRMSE `6.959169864654541`, and 1,220 normalized-range excursions; velocity KD MSE/cosine/max-joint-NRMSE are `0.020234743978387253`/`0.9908291589072651`/`0.46450573205947876`. This is a structurally valid diagnostic, not a promotion pass: it empirically selects continuation to 5k before rollout spending. Read-only closure auditor SHA-256 `e11bbbca2641078a733508e1ded2ec8036ed8108ebabb2df6cf6e080551dd427` produced PASS output SHA-256 `5da5c5b327e2ea8458fcd0617d3af8f831937429190e781cca81a169427cd0db`. The six singleton/no-delete outputs under `manual/offline-eval/libero-shallow-2000/20260804T192000Z-i-038112fe75c610517/` are control `xFHvf8P.jjAKqi.oQhMi5THwsZ4.C2Cp`, log `ZSTD6s_WOWlYhoUHMgMT.VE6Rub__vZz`, report `Z.0xdd44.rwbPVEL8yc7DV4vdkfiB1Kv`, arrays `1fWV.09cELmbQDUZTXNRB0MX7_lug0dz`, evidence `YM2BsMWkqfZyUE3Av0dhqQHlKamQoC6K`, and receipt `0VI5zs4N5mgPPHO5ymVJukm8L_OWYo67`; report/arrays SHA-256 values are `7e2ad2f80be2b250ac3af982e32cb8ae203eb0de948ec6b09328e23e7bcc2cf8` and `9fc42268efbfc3cff1b5eb2c54711d178675a1d9767889bf8fd4c9b698f3513b`. `StopInstances` was issued only for the workbench after this closure passed, and it was read back stopped/code 80; its external deadline remains enabled as defense in depth.

123. A fresh read-only materialization gate combined the already sealed LIBERO terminal audit with current EC2 concurrency and found zero managed nonterminal Shallow/DROID workers; helper/output SHA-256 values are `9b6e07f0cb95f2ca08683a86329981dcf511f9c38aed95c8307fa71e5de1b68b` and `81e2bb4388913576bdddb91fe8a78b781e745779e66eaaebe5b13402203d6742`. The 2k-to-5k materializer copied the sole published checkpoint descriptor byte-for-byte, preserved the original experiment identity and full-state `--resume`, and passed the db12 worker dry run. Candidate spec SHA-256 is `7bfb1ad25286fb09d25fba9f8b5edaa67fdf865fd170655d50c5d1b36c7eecbd`, 6,266 bytes. The first publication shell used zsh's read-only `history` variable and stopped after its read-only S3 listing, before `put-object`; replacing it with task-specific `spec_history` allowed the one authorized conditional PUT. The AES256 singleton spec is VersionId `vGuqcdMOan5PIccetzKbYyPjURNsbSnf` at `specs/libero-shallow-continuation-20260804t165358z-a1-5000.json`, with exact-VersionId byte/hash round trip and no delete marker. The controller-rendered executable bootstrap is SHA-256 `3de41287b9fd9ae2d0c405323e41a38722a0307bd3efb7fed9a5f9f6b49ee970`. A fresh read-only launch plan is SHA-256 `f6cfa935f7a1a65f133faa1497b7cb8301660297b4ee50f9ab5a0dab3b8b171e`: one ordinary On-Demand `g7e.4xlarge`, `CapacityReservationPreference=none`, 6-hour hard runtime plus 15-minute reservation buffer, encrypted/delete-on-termination 256 GiB gp3 root, external termination schedule, projected `$24.98850`, and `$2,469.30934` remaining after the existing thirteen-entry `$255.70216` reserved ledger plus `$250` non-compute reserve. No capacity or ledger reservation has yet been created for this continuation; the next action is a separate exact-hash paid authorization after one last singleton/concurrency/ledger replay.

124. Paid wrapper SHA-256 `c10c3e1831fa8dbb23daf8a1bb15f24a7a5b6368a4fb179ce9beac546eca480e` first completed an audit-only replay of the exact spec/controller/model bundles, evaluator-derived empirical metrics, training image, authoritative ledger, empty run/instance namespaces, controller-rendered bootstrap, stable plan fields and current zero-worker gate; it made no paid mutation. A separately authorized replay then created reservation `20c04d01-8850-4cf5-8f9e-1f8aaf432d6e` and exactly one ordinary On-Demand `g7e.4xlarge`, instance `i-0489a1bb92cb9ddb9`, in `us-east-2a`; launch-result SHA-256 is `59dd9a3a08d575baa050aec8c201c02bb6d61ba4c87a938c59c21dc9acb953e7`. Live readback shows `InstanceLifecycle=null`, `CapacityReservationPreference=none`, required IMDSv2/hop limit one, instance-initiated shutdown `terminate`, and encrypted delete-on-termination 256 GiB gp3 root `vol-0ae527d6ebf7873e0`. Self-deleting schedule `pi05-deadline-20c04d01-8850-4cf5-8f9e-1f8aaf432d6e` is enabled with exact `TerminateInstances` input for only that instance at `2026-08-05T01:43:41Z`. The fourteen-entry authoritative ledger is VersionId `stSvmPaMKRuaxx1NPJhqSKVI2r5fpVcM`, 26,663 bytes, SHA-256 `fefd4c1c206b7ffe48f2956043afb76420d61c48d5294740b2ed189b8d95a16e`, totals `$280.69066`, and reserves `$24.98850` for this six-hour-plus-buffer run, leaving `$2,469.30934` after the non-compute reserve. SSM became online, cloud-init completed without errors, `pi05-job.service` is active/running, and the guest hard-deadline timer independently reports the same exact deadline. The worker is now staging on its initialized instance store; do not launch overlapping Shallow capacity.

125. The retained workbench's reviewed DROID-db12 reuse plan is SHA-256 `0f97f0329f359321b08b3bfc017df08dfb2fc0fd534889fe1789de3861b325f4`; its first paid wrapper is SHA-256 `e22ef5a2a07ac27dab77714b757c1655c74124aeff2192dc6b1b14c32f8bc6e2`. It conditionally added only reservation `ad5bca03-324b-4bae-877d-c991a9156708` for 1.75 hours / `$5.25742` and installed an exact external stop schedule, but `StartInstances` returned `InsufficientInstanceCapacity`; no instance started and both the immutable image tag and evidence prefix remained absent. The resulting fifteen-entry `launching` ledger is VersionId `aF_Od2KX4FJuVlmE2Le0N3DqbF9kOy0r`, 27,627 bytes, SHA-256 `7b1c284d5f3e05972ac7b0de6077cae764a275b0aa0924ee2a3393e5f6156d3d`, totaling `$285.94808`. Same-instance recovery control SHA-256 `0c4ecc948bbab76645cf3cbf0835989da10274746ac207470813ede24167bd71` first passed audit-only, then retried only stopped workbench `i-038112fe75c610517` under that same reservation/schedule; it created no replacement, reservation, or deadline. Recovery result SHA-256 is `decfd0c1b33ff37659a0462631c1f48d1aedbae2c75e2b21c8c014db822aae1d`; the unchanged-cost launched ledger is VersionId `9UyUOANA6BzlNJrNei_oY1pTiFnWrKCU`, 27,865 bytes, SHA-256 `90018a980c41cea9f11b7c6e2a6395d5eb3e0dfabe8b8d6026f98cb9f0ddb064`. Guest-deadline control SHA-256 `56ec406b78dacd4c4b2c9e47681a4a281433f8babd908d764b6172beab75a8c9` ran as SSM `1f66f643-4c1a-4533-8fba-ac736fe5cddd` and bound the local shutdown timer to the same `2026-08-04T21:19:00Z` external deadline. Build/publish control SHA-256 `611ed85c29193b6136486f8e00a5019db173fb5209fed0be413e75731e7ef0ec` ran once as SSM `9bd73d04-b442-48cd-b902-66f7a197902f`; it built from complete-history db12 source, passed pre-push and post-digest-pull network-disabled L40S smokes for Torch/JAX/PyAV/LeRobot-v3/no-fallback ONNX CUDA, and create-once published tag `droid-v3-db12a82bb1755f110bf75dd56873235693964494`. The immutable OCI index is `sha256:021fb922a83ea45f2bc00f7a33f066474b3f551a77131f7ee1792e02f79be2bf`, amd64 child `sha256:5be807a4c7b7f08cf467398cf531f814bd383c7b4cd0b07b4c683be13a89d3cd`, config `sha256:d40f310471cc0474772ad7b9b979bc57bca0dd3bac820fe024248d06234c3160`, and compressed size 13,228,576,262 bytes. The three singleton/no-delete evidence VersionIds are build log `QSpng9bZd7al196uX2qrMmndNCOjpUak` / SHA-256 `f58f994723671e4b886726c18d48c7a2c47e9119dde4b8a38e3d47c5910f48bd`, evidence `CpS7mIua51plEgqTG72th4p26wZUPsVN` / `e3fd31a35f6cd33d76cc0e6a2ceea5a91efa54b65668189d588f1bc76e440cdb`, and receipt `WBgfuZLE7P1Pp_FDucvXq7yKfMKi3A_Z` / `62be9e275ac482a23584f204c4ac429313b512d301fd8c95784612debf2532bf`. Read-only closure auditor SHA-256 `41b80477b3875868f93c1537bde20ba1f780f0c27ed4b626d956f5cd221345ab` directly hashed the OCI config blob, validated its complete runtime/provenance labels and receipt closure, and produced PASS SHA-256 `4931f76d7e80b6b8619a66ebf7ec80868fd66743650476dadbdebdbcdd7b312c`. The workbench was then explicitly stopped and read back stopped/code 80.

126. Fresh two-process DROID pilot `droid-shallow-pilot-20260804t200900z-a1-2000` passed the db12 worker dry run with `torchrun --standalone --nproc-per-node=2`, exact released teacher, filtered-DROID revision, `num_workers=0`, disabled W&B, and a single create-ready 2,000-step output. Its 5,775-byte spec SHA-256 is `fecba3527a4a4a8c7c0df106b9b4df031f4b50794d54f9b3d18fe0150ee5faa1`; materializer SHA-256 `69ea9629c972a4af3ded06f2cb1fe429cc6d9dee4fe5de4aa830532e04f731e2` conditionally published the sole AES256/no-delete spec as VersionId `Dj.S3BJbTbgYSS55hTijdoTRFQvu7CVG`, then rendered bootstrap SHA-256 `79c8e795f6ccf35e09a456461c278273a1e81793b80e6dbb6c28662d7b388a53` and four-hour plan SHA-256 `b6a668b31a37267f0414bed54f8dd083cafbf9ca6b53fee2a0759906df525d3b`. Paid wrapper SHA-256 `366ac1159981f69cfe628e04d3e5804679a2e85a2d0f6014e1b1f6db026ae20d` first replayed every source/image/spec/ledger/output/quota/plan gate in audit-only mode. Immediately before launch the retained workbench was stopped, the only account-wide running instance was the 16-vCPU LIBERO `g7e.4xlarge`, and quota `L-DB2E81BA` was exactly 64 vCPUs; the 48-vCPU `g7e.12xlarge` therefore fits alongside it exactly. This evidence-bounded parallel launch supersedes entry 124's conservative no-overlap note and materially shortens elapsed time without increasing either run's reserved duration. The paid replay created reservation `dd61305f-b52f-4fd1-9efe-ae645ebb78d6` and exactly one ordinary On-Demand `g7e.12xlarge`, `i-04825fee00785b0e0`, in `us-east-2a`; launch-result SHA-256 is `9bd26e460d239d65efbdc0d9931e75eaff73bc3503b455653bfe58bb74bdd92f`. Live readback shows two RTX PRO 6000 Blackwell Server Edition GPUs with 97,887 MiB each, `InstanceLifecycle=null`, `CapacityReservationPreference=none`, required IMDSv2/hop limit one, instance-initiated shutdown `terminate`, and encrypted/delete-on-termination 256 GiB gp3 root `vol-0c77378dfd5dcd33a`. Self-deleting schedule `pi05-deadline-dd61305f-b52f-4fd1-9efe-ae645ebb78d6` is enabled with exact `TerminateInstances` input only for that instance at `2026-08-05T00:18:04Z`. The four-hour plus 15-minute reservation is `$35.21584`; authoritative ledger VersionId `zz.HPYxNR1q0UwCiaI9MkzeaEQ7Rnm97`, 29,888 bytes, SHA-256 `106fd937d2b04217c84265b0b3fdfab27b49470323707142a245733dcabee649`, now has sixteen entries totaling `$321.16392` and leaves `$2,428.83608` after the `$250` non-compute reserve. SSM observed both GPUs and initialized 3.5 TiB instance-store scratch while cloud-init was still running; the next gate is successful `pi05-job.service` start, two-rank first optimizer step, and terminal publication within this same instance.

127. The already-published canonical DROID framework-equivalence evidence was adapted for worker/offline-evaluator staging without regenerating the corpus or allocating capacity. The upstream revision is `d8f44f39eaba43a1f5232bbf8dc57c5802b7278593b99062b7ccaaa08a86dda0`; manifest VersionId `4UHgPDXA0X5rJLfhjfFZDTpGno0sggr0`, SHA-256 `8a0d662814b08ec27218c69e6a97dab0932d9537bd4d57b6b427358239cd2b0c`, and 5,364 bytes bind 64 fixed samples, `pi05_droid_l09_distill`, passing framework cosine evidence, and four exact-VersionId payloads totaling 11,582,917 bytes. Reviewed adapter SHA-256 is `f9ec0d8cddaa75b5d2df5f4560f0935d3e0f7f21205733fb454666a6d0e506da`, 1,684 bytes; pending descriptor SHA-256 is `7f14fd82e9359e019e60da553752bc285ae1ecf440f2748b870649b276fcb514`, 1,533 bytes. Publication control SHA-256 `e12b7350b5f32de565bfd3af3cf1b36451aa94c77b7e01b0db747a130e0a158c` first passed audit-only with an empty destination and zero mutations, then performed one `If-None-Match: *` AES256 PUT at `adapters/worker-assets/droid-framework-equivalence/d8f44f39eaba43a1f5232bbf8dc57c5802b7278593b99062b7ccaaa08a86dda0/manifest.sha256.json`. Its sole/latest VersionId is `oglEmUp_JZMNmM3ijXS_JV5tpcDpdKyw`; there are no delete markers. The rendered mode-`0400` descriptor is 1,539 bytes, SHA-256 `261c8d432a1a3cf849125f7041150c71fe5f7f347675636706501d404bba8106`. Read-only closure auditor SHA-256 `a5095e538a40569a94f5d5db3b25597e4080e67c71be5b0a271cdaa5e4eb09b5` exact-downloaded and rehashed the upstream and adapter, validated all four payload histories/HEAD tuples and the final descriptor, and produced mode-`0400` PASS evidence SHA-256 `367710000ea7d8a22fd4eba82da1ed44022e70b152c1f5a40b2d7070395b4151`, 1,352 bytes. This step added no instance, schedule, ledger entry, copied corpus object, or compute cost.

128. The clean DROID 2k run `droid-shallow-pilot-20260804t200900z-a1-2000` staged all 258,712,592,431 DROID bytes plus both exact teachers, pulled the immutable db12 image, and entered the intended two-rank command, but failed before optimizer step one. NCCL 2.26.2 initialized both ranks in 391.02 seconds, attributed 390.98 seconds to kernels, and connected four channels in each direction through `P2P/CUMEM`. Both ranks then raised `CUDA error: an illegal memory access was encountered` while `DistributedDataParallel` constructed `dist.Reducer`; both watchdogs aborted and `torchrun` exited one. Unlike the earlier LIBERO A3 incident in entries 84-86, no diagnostic CUDA process overlapped this run, so the clean recurrence rejects the existing two-GPU path for the immediate DROID pilot. The failed manifest is VersionId `bTkr2MCuhrmryFEslwKJhMGud6KlM5LF`, 11,338 bytes, SHA-256 `83dcd565ff48f150e37c86220d4416e2ff8450270277d10c2889ba4877e33519`; final-sync evidence is VersionId `SYZgaVCVGmI0c3sMhQrgXi8zOMWhq07l`, 836 bytes, SHA-256 `e81c6f34ec9edc3e68ef7f95c0f133f4bfd70a909631fc5384e5640d28ae72c4`. Its prefix contains exactly four singleton log objects plus those two manifests, no delete marker, no metrics, no checkpoint, no expected-output marker and no published input. Ordinary On-Demand `g7e.12xlarge` instance `i-04825fee00785b0e0` terminated/code 48 after successful final sync. Read-only auditor SHA-256 `c75521b36fd5280d135c09e57e7153a0c6b1a187401851bdf5fcf134f0325ccc` exact-VersionId downloaded and rehashed the spec and all six run objects, checked every receipt/HEAD tuple, both rank traces, immutable image presence and terminal EC2 identity, and emitted mode-`0400` PASS evidence SHA-256 `591ecad93f6f03519b86a568980a34b57fe9bb8cbe133e09ca5069a756f6df29`, 1,775 bytes. The worker's unconditional `ExecStopPost=poweroff` made same-node repair impossible after the error; that lifecycle is not repeated for the corrective retry.

129. The corrective DROID choice is based on measured GPU time. The accepted single-GPU A3 logs were exact-hash matched against all 30 manifest receipts and show a stable `5.59-5.65 s/it`; every ten-step report took about 56.0-56.4 seconds. At that measured rate 2,000 optimizer steps require approximately 11,240 seconds, or 3h07m, before fresh-loader and terminal-publication overhead. This one-batch measurement does not prove real shuffled-video-loader throughput, so the corrective run reserves six hours plus the standard 15-minute margin (`$24.98850` at the pinned `g7e.4xlarge` rate) and its first real-data reports remain the empirical throughput gate. The launcher now permits `--retain-after-command` only for the explicit `corrective_run` + `shallow_training` + one `g7e.4xlarge` fallback (or the pre-existing export/compile session). Ordinary Shallow and two-GPU Shallow remain unable to retain. The guest unit therefore does not power off when the command returns, while the independent exact-instance `TerminateInstances` deadline remains mandatory; a failure can be inspected and repaired against the already-staged NVMe data on the same paid node, and a success is manually terminated only after terminal evidence passes. This is the selected next DROID attempt because its model/memory path already completed 300 steps at 30.68 GiB peak, it avoids the rejected DDP reducer entirely, and it costs less than another `g7e.12xlarge` attempt.

130. A separately retained source correction prepares the future two-GPU path without claiming a hardware pass. `setup_ddp` now resolves `LOCAL_RANK`, binds its CUDA device and sets distributed diagnostics before communicator construction, then passes that exact `device_id` into NCCL initialization; CUDA barriers explicitly name the current device. Multi-process isolated workers select NCCL's legacy allocation path with worker-owned `NCCL_CUMEM_ENABLE=0` and `NCCL_CUMEM_HOST_ENABLE=0` while keeping PCIe P2P enabled. This is consistent with NVIDIA's NCCL 2.26.5 Docker/NUMA fallback guidance for older 2.26 runtimes (https://docs.nvidia.com/deeplearning/nccl/archives/nccl_2265/user-guide/docs/troubleshooting.html), but remains a hypothesis until a clean G7e two-GPU reducer smoke passes; the selected one-GPU DROID retry does not depend on it. The DDP, worker-environment and retained-corrective lifecycle changes passed 127 focused launcher/worker/trainer tests, Ruff 0.11.12 check/format, Python byte compilation and `git diff --check` on macOS; the frozen CUDA dependency graph cannot be installed natively on that platform, so actual NCCL validation stays an AWS gate.

131. Corrective DROID materialization used launch-control commit `8442486bda49bef98bf1f9f2dd214b6dab036e6f` while intentionally retaining the already-published db12 worker/model source and immutable DROID image, because the selected direct-Python path does not execute the new DDP code. Reviewed spec `droid-shallow-pilot-20260804t210300z-a2-2000` is 5,724 bytes, SHA-256 `7bf91121ef4d42a4a3a075ef78101c031d5acffd83c27dd38f5db91033057f91`; it preserves the exact dataset, released teacher, seed, optimizer batch, 2,000 steps, save/output identity, `num_workers=0` and disabled W&B while changing only the fresh run/experiment identity and `torchrun` to direct `python`. Materializer SHA-256 `cae244dad82b6c2bcb2153ebb7fa7472a16dd983557116dc0874398f179c370b` first passed a mutation-free replay against the failed-run closure, current 16-entry ledger, image/source/spec dry run, quota, current LIBERO worker and empty destination. The authorized replay performed one conditional AES256 S3 PUT: spec VersionId `mppcd4Aqn3RWj0FrARmAX86rqVHIIeqt`, with singleton history and no delete marker. Controller-rendered bootstrap SHA-256 is `b5c83d51faee53287ebc77cb6715361238b0368952bdbb04117ffb7f5bb70dd6`; the six-hour-plus-15-minute retained corrective plan is SHA-256 `2b06cf2ced7c9d3f44d46aa524f14fdbef677f3596abe13ad12de6113aef6418`; materialization receipt SHA-256 is `1b497b97b3a35b036cbc41b2e72ec7394b3b34ad2c443b0b8e227522e542bea7`. No paid capacity or ledger entry was created by materialization.

132. Paid wrapper SHA-256 `e97e7ae57e59e981c911408924696498098903b56f8cf19322d4ef9c6181777e` passed audit-only, then launched exactly one ordinary On-Demand `g7e.4xlarge`, instance `i-0928bcaa6d1d63212`, under reservation `10a88e10-cf60-43d4-8fc5-da7ec0fd38a1`; launch-result SHA-256 is `f7c86c0369ba8fb0b8ba6b842103af165617eec1066549b573bd3cf0d4d8ea10`, 1,620 bytes. Live readback proved `CapacityReservationPreference=none`, IMDSv2 required/hop limit one, instance-initiated shutdown `terminate`, encrypted/delete-on-termination 256 GiB root `vol-0f1b1f647cb52212c`, and one 97,887 MiB RTX PRO 6000 Blackwell. Independent self-deleting schedule `pi05-deadline-10a88e10-cf60-43d4-8fc5-da7ec0fd38a1` and guest timer both target `2026-08-05T03:10:27Z`. The systemd job unit has no `ExecStopPost`, so command exit retains the same instance for inspection/repair until explicit post-audit termination or that deadline. The 17-entry authoritative ledger is VersionId `WxeetoAwLieJOJxT.hrT5wM.63t9Klce`, SHA-256 `1904e4539bb63f41680882a01b790ebff0928be12cc75b524338950bbe308055`, 31,906 bytes, totals `$346.15242`, and reserves `$24.98850` for this run. The first post-launch wrapper assertion incorrectly queried nonexistent ledger field `.reservation_id` instead of `.id` and stopped only after safely preserving the successful launch result; corrected recovery-wrapper SHA-256 is `a03cc6c01fd9cd991cc2139f3341fe9e00775d28985c25822c54224e80e9c804`. Read-only launch auditor SHA-256 `b43bfcefc5b515a7b3b7fd90ad20fa13cec594ce4712116ba7f3b448c0314970` independently checked the spec, live instance/volume/shutdown behavior, external and guest deadlines, retained unit, GPU, exact ledger and empty initial run namespace; its mode-`0400` PASS output is 1,450 bytes, SHA-256 `78b14141e686cbc959ae3618a6f0fbd02bc243cde618eb80fdcaec7adf454d3e`. SSM launch gate `c29eaaad-55c9-41ce-a646-3c5d3337b1c9` returned success; the worker is now staging on the initialized 1.7 TiB instance-store volume, and no second DROID process is authorized.

133. The healthy LIBERO 5k node was retained in place for its immediate offline gate without extending its paid deadline or launching evaluation capacity. Read-only SSM `53ce2f4b-d8f9-40a2-b6e6-63ef8438f24f` proved job PID 1990 active, the sole effective post-stop action was `/usr/bin/systemctl --no-block poweroff`, the drop-in destination absent, and both deadline layers still targeted `2026-08-05T01:43:41Z`. Control SHA-256 `f086d235bacce717d8de8c0b2e9436ac9695d3e121c9d9cc4bc868528f55a798` then added only `/etc/systemd/system/pi05-job.service.d/retain-after-command.conf`, whose SHA-256 is `d74e411e048a39cd5c86f38c782caa4df4b02f9c3fba11b8b5bebeacd59d18f5`, with an empty `ExecStopPost=` reset. SSM `fba4dd03-a1fb-4114-bafc-1b81421d9d32` returned success after `daemon-reload`; the service remained active with the identical PID, effective `ExecStopPost` became empty, and the enabled guest timer retained the exact deadline. The independent external schedule was re-read enabled, self-deleting and targeting only `i-0489a1bb92cb9ddb9` at the same time. A first local command renderer reused zsh's read-only `parameters` name and stopped before `SendCommand`; its replay used `ssm_params`. No training file, process, command, checkpoint, data, optimizer state, ledger entry, schedule or instance count changed. After 5k terminal publication, use the retained local checkpoint for offline evaluation, then explicitly terminate this node before the unchanged deadline.

134. The two terminal offline gates were prepared and hash-pinned before either training job completed, without starting an evaluator or allocating capacity. LIBERO control SHA-256 `1fa14e9087f64d4ed0a0c0299df3f6a155fdde62ca9b331a3229bcb32fcb5861` was syntax-checked and installed mode `0500` as an unexecuted same-node control by SSM `6b859ca3-8b2f-44b3-999a-b94bb3b66bb8`. A first policy-image prefetch probe, SSM `25448195-d7b8-462a-b0e9-e2972587d018`, inherited `/bin/sh`, rejected `pipefail`, and exited before login, pull or container execution; Bash-encoded replay `611cd409-32a7-4b28-80d1-39bd656d586c` pulled the immutable db12 LIBERO policy image and passed a network-disabled evaluator import/help gate while the training container remained active. The initial DROID evaluator control was revised before use after exact VersionId readback of canonical `golden.json` proved action horizon 15 rather than LIBERO's 10; the superseded installed control was never executed. After rebinding to the extension evidence below, final DROID control SHA-256 `3967ee00eab6ef0899e66775fc234e7106fa700575f63d9eb1709ed2419d3126` was syntax-checked and installed mode `0500` by SSM `63f64884-6805-4b80-9e73-faa1c9de2fd5`, also without execution. Each control requires successful worker termination, exact local/S3 manifest and checkpoint hashes, exact teacher/corpus/image identities, a network-disabled 64-sample GPU evaluation, and singleton create-once output publication before it can report PASS.

135. Real shuffled-video DROID throughput invalidated the overfit-only runtime estimate but was corrected without losing the staged 258,712,592,431-byte dataset. The direct single-GPU run initialized the reviewed layer map and completed its first ten optimizer steps with KD cosine `0.98570`, KD MSE `0.10436`, joint NRMSE mean/max `0.24152/0.73086`, and measured `103.7 s` per ten steps; step 40 independently measured `103.3 s`. At 10.37 s/step, the 2k checkpoint projected about seven minutes past the original `03:10:27Z` hard deadline before terminal upload. Local Python 3.9 initially exposed an actual cost-guard compatibility bug: runtime `isinstance(value, int | float)` is unsupported despite the script's direct-macOS compatibility contract. It was changed to the equivalent tuple form, after which the direct guard projected one additional retained `g7e.4xlarge` hour at exactly `$3.99816`, with `$2,399.84942` remaining after all committed compute and the `$250` non-compute reserve. Extension control SHA-256 `55de7225d1b779976aaeb26045fc07af87f410cec94d8237baa2def9e10bfd5b` first passed audit-only with candidate-ledger SHA-256 `d2c8ae0da7e4933a4b3e9ff23f166effd3d1d59b7049d1997eb4037629bac2b1`, then used reservation `36256e55-1276-400b-be43-4c867ebd64fd` and two conditional ledger writes: launching VersionId `Lylt97_x6BPBhABXv5uybMnhrvmSrYhT`, followed by launched VersionId `5FUQ1Mjzx0p50ncGMzJ93NAFQuNTcTfG`. The final 18-entry ledger is 32,992 bytes, SHA-256 `89ae61b44ad82bc970764aad3a68c1bdeaa8a655bedc14b8edc33d4f2bbb92f7`, and totals `$350.15058`. The existing self-deleting external schedule was updated in place to terminate only `i-0928bcaa6d1d63212` at `2026-08-05T04:10:27Z`; SSM `b394685f-8b6c-44da-8f50-ea1c4f0eb163` set the guest timer to the identical deadline, and readback found active timer SHA-256 `c4f3175054d8dd955e8f7443872a06d23db8126d12a86ba39d2e411e607a39b3`. The instance, PID, training container and service stayed running throughout; no EC2 launch, stop, restart, dataset restage, checkpoint reset, Capacity Reservation or non-On-Demand purchase occurred.

136. Measured DROID video-decoding throughput also narrowed the future continuation contract without changing the active 2k process. Commit `5f0d39f400d3cafc6f25ccbed6de409475342dc1` permits a direct single-GPU resume for either reviewed Shallow track only on the existing `2k -> 5k -> 10k -> 20k -> 30k` ladder and only with `corrective_run` + `shallow_training` + `g7e.4xlarge` launch metadata. For DROID, its byte-for-byte command keeps exactly one `--no-wandb-enabled` flag, which preserves the accepted checkpoint's resume fingerprint, but forbids an explicit `--num-workers` override; absence of that override restores `pi05_droid_l09_distill`'s reviewed `num_workers=4` setting. The loader already uses a seeded generator, worker initialization, spawn context and persistent workers, so this re-enables deterministic decode overlap rather than changing data or optimizer semantics. Removing the W&B flag, adding `--num-workers 0`, reordering arguments, skipping a checkpoint, using another instance class/category/workload, or adding any other recipe option is rejected. The worker, launcher and cost-guard suite passed 110 tests; focused Ruff 0.11.12 check/format, byte compilation and `git diff --check` also passed. This is source preparation for a possible 2k-to-5k continuation, not a measured four-worker speed claim; it made no S3, EC2, schedule, ledger, checkpoint or running-container mutation, and the active DROID 2k run remains on its immutable zero-worker command.

137. Local preparation for the paired 400-episode LIBERO gate found a provenance dependency before any spec was published or evaluator was launched. The first rendered base spec contained only the converted PyTorch checkpoint; although schema validation passed without reading remote manifests, execution-time cross-contract validation would reject that converted checkpoint because its exact original JAX teacher was not selected in the same worker spec. Commit `b19cb829c1ca43665dd74a23d7a3826097b7ad26` adds an optional `--checkpoint-provenance-artifact` renderer input, stages it separately, and rejects an overlapping destination while leaving the policy command bound only to the converted checkpoint. Eighty focused LIBERO renderer/evaluator/worker tests, Ruff 0.11.12 check/format, byte compilation and diff checks passed. The corrected uncommitted-control inputs are: JAX descriptor 713 bytes / SHA-256 `699fd1f713867656bcfaf2f96298aec49842731ab80eb5543209d41d6dc441ad`; converted descriptor 1,380 bytes / `c9a449d699031f18d895240f575c382c2f0910453884933f24692026afba976c`; and dry-validated 400-episode base spec 5,944 bytes / `e27114af002a50d24126f2f3f5116304aba716cba41ea2fdf2bbd652f38311e3`. It pins the db12 evaluator and parent-policy image, base and candidate-independent seed 42, 10 trials per each of 40 tasks, `g6e.4xlarge`, and a conservative three-hour-plus-launcher-margin projection of `$9.76378`. The spec remains local and unpublished; paired base/candidate publication and capacity stay blocked on the 5k terminal offline result.

138. The future-controller artifact needed by either the provenance-correct LIBERO renderer or the deterministic four-worker DROID continuation was published without allocating capacity. A detached exact-`b19cb829c1ca43665dd74a23d7a3826097b7ad26` clone produced a complete-history bundle whose sole advertised head is that commit; local and fresh-bare-clone `git fsck --full --strict` passed. Publication control SHA-256 `5d2487d0f249ac90279c4345164b04144ba0e7104f98087812f30fca490e589f` first passed audit-only against two empty object histories, then performed only two conditional `If-None-Match: *` AES256 writes. The 1,932,865-byte bundle is SHA-256 `a9eaf10414b20c3793870e6190bc8d58cd079554b9d3bf5af0edb162f3687540`, key `source/openpi-b19cb829c1ca43665dd74a23d7a3826097b7ad26-complete.bundle`, VersionId `9wbTbDOmjY5RkHXuMdxTXTeBhRStlVGU`. The unchanged 8,888-byte bootstrap is SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`, key `bootstrap/worker-bootstrap-controller-b19cb829c1ca43665dd74a23d7a3826097b7ad26.sh`, VersionId `SUd.ZZrkD_D67gCqaII25f6etYcMseHK`. Immediate audit-only replay exact-downloaded and rehashed both singleton/no-delete objects, repeated bundle verification/fresh fetch/fsck and Bash syntax, and made no mutation. No spec, run output, ledger, schedule, budget, checkpoint, image, instance or running process changed.

139. The reusable 400-episode base LIBERO worker was materialized before the 5k candidate gate so its paired observations can serve 5k, 10k or later Shallow checkpoints without rerunning the base. Materialization control SHA-256 `3d2bd300db4413bbb9b1dc19f71893f50659de69a1f1d7bb97b52fc1ed8e8551` first passed audit-only, including exact-VersionId download and execution of the worker's converted-teacher/JAX-upstream cross-contract validation. It then performed one conditional AES256 write: the 5,944-byte spec SHA-256 `e27114af002a50d24126f2f3f5116304aba716cba41ea2fdf2bbd652f38311e3` is the singleton/no-delete object `specs/libero-base-intermediate-20260804t220000z-a1-400.json`, VersionId `Uc4DswpAE3lKVqViUWIpe1ENa2p6phU0`. The db12 worker rendered bootstrap SHA-256 `d311dcca07dccb7fffa1477448b5b07a64c8967e0dc9f6e181d72af7514f007b`; the initial `us-east-2a` three-hour read-only plan was SHA-256 `5bee8f4c6d7efacdcf04e0da8de320cb80e590746b56f7dbf8f0fb52a93b3baa`. The spec pins the converted 18-layer teacher plus its exact original JAX provenance, db12 evaluator index `sha256:f142f7119538416f578234a1b492437121e6aa89f0f213e9eb952461c2b0554c`, parent policy `sha256:71652fdcf2f8dcb8ac143a5af68b61c115a0c5896f5b829fa19668a55129319d`, seed 42 and ten trials for each of 40 tasks. No paid capacity or ledger reservation was created by materialization.

140. The first base-evaluator launch attempt was closed and moved to the alternate AZ without duplicate capacity. A preliminary paid-wrapper audit stopped locally because the launcher had semantically cached the exact remote ledger with sorted keys, changing only local byte order; the wrapper was corrected to exact-hash the remote VersionId and compare canonical JSON. Corrected wrapper SHA-256 `1e074873d8a5a5ea892553145d0b9dee7ae77cc4fb6ab3722f5e87c45c4c4efe` passed audit-only and attempted ordinary On-Demand capacity in `us-east-2a`, but all three AWS retries returned explicit `Server.InsufficientInstanceCapacity`. Reservation `71d9a436-6652-40ce-94c4-32b23e502137` was conservatively recorded `launch_unknown` in ledger VersionId `SlcLVOXxQYEZ.SjGMft5.j8AzJdqQ6GP`, SHA-256 `365f6c9021e04ecfccbd8a4194b12c61e361fcaac4bdf623fa57632b5661c577`, 35,308 bytes. Reconciliation control SHA-256 `0c6abb4683754ca84effb5ce597a25a8dbd2867398e4803d39b15b6386d76fd7` proved zero instances by exact client token and label, zero tagged volumes and network interfaces, no deadline schedule, and three CloudTrail failure events (`028f3073-6fbd-4ce7-b840-f03957f55175`, `bae1fb46-c5d5-4c1f-95d0-820bf306c000`, `d86b32de-9c2a-4a2c-9bc8-9144562eaab7`) with null response elements. It retained the ledger row but marked it `cancelled` with `actual_usd=0`; resulting VersionId `MgVH88zGPFXauybg3EcKEKbrsbpObhrX`, SHA-256 `3539b45b8195e3297d10b96581b06ad024a317594fa679752143cee8c84ddfb1`, 35,855 bytes, excludes that failed request from committed spend. Read-only alternate plan SHA-256 `a9998353a2a278379b850a0fe9af4a72b3fc6d3e68e1861b7c1f1a256ee5cbda` selected the error-recommended pinned subnet in `us-east-2b`; final wrapper SHA-256 `a5aebbbb8d87f5714869f95cce89c693291a6e20dd29be017f0628524c6c1b1f` passed audit-only and launched exactly one ordinary On-Demand `g6e.4xlarge`, `i-0186d4207965c7d98`, under reservation `195336b9-5e0c-425e-ac28-e6a8787ca117`. Launch-result SHA-256 is `53124ab0521fc36a69f86bbd55190184f2e036c1073eec7d264720576ca629a3`. Live readback proves `CapacityReservationPreference=none`, evaluation AMI `ami-06517bc7fad3c6a48`, encrypted/delete-on-termination 256 GiB gp3 root `vol-0d6daf1e7062c260f`, instance-initiated shutdown `terminate`, and enabled self-deleting schedule `pi05-deadline-195336b9-5e0c-425e-ac28-e6a8787ca117` targeting only that instance at `2026-08-05T01:12:16Z`. The final 20-entry ledger is VersionId `gyhLJu8IVPg2jaqSsFeMSNH1RrLZ3a6O`, SHA-256 `58e4df959376f546ce960049035dd6c29f54c8fbc971b82e9451e9f0e4c686b9`, 37,882 bytes; committed compute is `$359.91436`, leaving `$2,390.08564` after the `$250` non-compute reserve. No candidate rollout was launched.

141. The first `us-east-2b` 400-episode base evaluator did not publish a run log, manifest, or expected-output marker. Instance `i-0186d4207965c7d98` entered its worker during image staging and then terminated after `pi05-job.service` failed; no external `TerminateInstances` CloudTrail event caused that transition. The launcher's unconditional disposable-worker `ExecStopPost=poweroff` therefore deleted the only immediately inspectable root volume before the exact Docker failure could be read. Encrypted snapshot `snap-0163eada14dcab899` was created from root volume `vol-0d6daf1e7062c260f` before EBS deletion completed; it preserves the 256 GiB failed root for read-only journal inspection. The stopped persistent workbench `i-038112fe75c610517` was restarted under the already-reserved `69c8fe7f-e18c-4d88-bac4-4dd935322554` window, without a replacement instance or a second launch reservation, because it already contains the exact 47.6 GiB unpacked evaluator image and can attach a diagnostic volume in `us-east-2a`. This is failure evidence only; the base evaluation remains incomplete until the root cause is identified and a fresh worker succeeds.

142. Disposable launch user data was hardened after that evidence loss. A successful `pi05-job.service` still requests immediate poweroff, but `ExecStopPost` now checks systemd's `SERVICE_RESULT` and leaves a failed instance reachable through SSM until its independent exact-instance EventBridge deadline. Retained jobs remain unchanged. This preserves a bounded same-node repair path without weakening the prepaid external termination control. The focused launcher suite passed 38 tests, Ruff 0.11.12 check/format and `git diff --check`; this source change does not affect already-running instances and must be bundled before the next fresh disposable launch.

143. Snapshot progress showed that the failed-root inspection would not complete before the workbench's `23:15Z` deadline. Audit-first extension control SHA-256 `5dd39df759f5156e3588b38ae5735ddeec2a4f170114160ab9c5695e9d400179` reserved exactly two additional On-Demand `g6e.4xlarge` hours / `$6.00848` as `d4484210-ab14-4f20-a479-5eb07abf989c`, without launching or replacing capacity. It conditionally wrote launching ledger VersionId `v6jCORLTw2HVvP.wwCzVELkJ_29hYiu6`, updated the existing self-deleting schedule and guest timer in place from `2026-08-04T23:15:00Z` to `2026-08-05T01:15:00Z`, and finalized 21-entry ledger VersionId `s3S_9Zgk9_5nCCaCvCT4q77krranJhLU`, 38,984 bytes, SHA-256 `6160a14fa8a7a29fe0be0b9762e335c421fb4d120cd91400f8d24cb1cb0f4b69`. Committed compute is `$365.92284`; SSM `187200cc-c8f9-47ea-9086-b98739c16ed6` verified the identical guest deadline. Stop this workbench as soon as diagnostic evidence is durable so its reserved window does not delay the fast-lane quota handoff.

144. Live Service Quotas readback fixes the concurrency envelope at 64 On-Demand G/VT vCPUs. The fast lane has first claim on two `g7e.2xlarge` one-step workers (16 vCPUs total). After the current low-step gates, both existing one-GPU `g7e.4xlarge` trainers may continue the long Shallow ladders in place (32 vCPUs total), while one `g7e.4xlarge` compile or evaluation worker uses the remaining 16 vCPUs. This `32 + 16 + 16 = 64` allocation permits both tracks to overlap without Spot, reserved capacity, or a 48-vCPU `g7e.12xlarge` starving the fast lane. The workbench must be stopped before the full envelope is used. Start the two fast workers before long-lane continuation whenever a quota race exists; no capacity was added by this read-only scheduling decision.

145. The fast lane now has a deterministic fresh-SnapFlow spec renderer instead of hand-edited JSON. It consumes an existing track spec plus the exact published checkpoint descriptor from an accepted Shallow run, retains only the dataset and original JAX asset checkpoint, binds the same `CONFIG/EXPERIMENT/STEP` destination into both the staged artifact and `--pytorch-weight-path`, and emits the reviewed direct-Python batch-four 5k command. DROID additionally retains its measured 64 GiB-host safety flags `--num-workers 0 --no-wandb-enabled`; LIBERO retains its configured four workers. The renderer refuses overwrite, wrong source config, missing/duplicate required artifacts, nonnumeric checkpoints, and schema-invalid output. Both tracks, including a 5k LIBERO source and a 2k DROID source, passed the closed worker contract in 61 focused renderer/worker tests plus Ruff 0.11.12 and diff checks. This is control preparation only: no checkpoint was promoted, spec published, instance launched, or cost reserved.

146. The concurrent long lane also has a deterministic Shallow-resume renderer. It copies the exact accepted worker checkpoint descriptor, retains the track's dataset plus original JAX and converted PyTorch teacher artifacts, pins the already-published `b19cb829c1ca43665dd74a23d7a3826097b7ad26` controller descriptor, preserves the source experiment and seed, and emits only the direct-Python `2k->5k->10k->20k->30k` ladder command. DROID deliberately removes the diagnostic zero-worker override so the reviewed four deterministic loader workers can overlap video decode while preserving `--no-wandb-enabled`; LIBERO preserves its ordinary loader config. Sixty-five focused Shallow/SnapFlow/worker tests, Ruff 0.11.12, format and diff checks passed. Dry renders against the actual pending LIBERO 5k spec and active DROID 2k template produced worker-valid 5k-to-10k and 2k-to-5k contracts. This prepared no remote spec, capacity, deadline, ledger row, or checkpoint promotion.

147. The failed base-evaluator root was inspected read-only on the retained workbench after snapshot `snap-0163eada14dcab899` completed at `2026-08-05T00:03:26Z`. The diagnostic reused its one already-created encrypted 256 GiB gp3 volume `vol-0dd9f5ea6774d3891` after the first control attempt failed locally while rendering a quoted remote heredoc; no replacement capacity or second volume was created. The ignored manual control was repaired in place and re-audited after each failure: literal `read` replaced the nested heredoc command substitution, an exact existing-volume contract made the created volume resumable, base64 plus explicit Bash avoided SSM's `/bin/sh` `pipefail` rejection, exact disk symlinks prevented partition aliases from selecting the EFI partition, and exact `findmnt --mountpoint` avoided mistaking `/` for an existing mount. Final control SHA-256 `2ae7578dde333fd043df73e5e6c168fa9cd2515e02747c59f733eda2e5c25f8f` mounted `/dev/nvme2n1p1` as ext4 with `ro,norecovery`, then SSM `e6cbcbc8-2a10-4113-b8cc-0b930c2b825e` published the 27,862-byte report SHA-256 `8b187750c2caa2d67573523ad08fa76975326b7918336eaeb3739292430ef2c8` as the sole/no-delete AES256 object `manual/diagnostics/failed-libero-root-20260804t230000z-a1/root-journal.txt`, VersionId `PtgMH.nKok3qtrlBfpapkETCLBbdvWeI`. Its journal proves the worker failed before evaluator execution with `WORKER REJECTED: pulled eager LIBERO evaluator must not claim a TensorRT compiler identity`. Exact image inspection found no TensorRT compiler ancestry; the only intersecting label was the ordinary eager-policy `ai.openpi.onnxruntime-gpu-version=1.26.0`, which the policy validator requires but the eager LIBERO branch accidentally included in its blanket TensorRT-toolchain deny set. The narrow controller correction permits only that required overlap, explicitly verifies its exact eager value, and continues rejecting every actual compiler-only ancestry/toolchain label. Fifty-eight worker tests, including wrong-ORT and real-compiler-claim regressions, pass with Ruff 0.11.12 check/format, byte compilation and diff checks. A retry requires a new controller bundle/spec/run ID and a fresh bounded launch; none was published or launched by this diagnostic.

148. The exact evaluator-identity correction is committed as controller `f4de527e967ba873830946e92147f401e1e5cb67` and was published without rebuilding the unchanged db12 evaluator image or allocating capacity. Publication control SHA-256 `4b8b70d5c48856b9e128baab165d74ae6b13ea625498fd1c3c54b9a0f25de2af` passed audit-only against two empty histories, then performed only two conditional AES256 writes. The complete-history single-HEAD bundle is 1,921,383 bytes, SHA-256 `a1b3bc10b6155dbf6b8d43eaf36ac9c831eb4372c7c972c8e12888f8246d3934`, key `source/openpi-f4de527e967ba873830946e92147f401e1e5cb67-complete.bundle`, VersionId `GcfbLBhD5x_JsGKhK7CASbNNJWWBH7F1`; local verification plus a fresh empty bare fetch passed strict full fsck. The unchanged 8,888-byte bootstrap is SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94`, key `bootstrap/worker-bootstrap-controller-f4de527e967ba873830946e92147f401e1e5cb67.sh`, VersionId `FIuT7V4RBLKs4rTDJAyejM4K_DdWAaE8`. Immediate audit replay exact-downloaded and rehashed both singleton/no-delete objects. This changed no image, spec, run output, ledger, schedule, instance or paid reservation; a fresh evaluator retry must keep model/image source `db12a82...` while using this controller bundle.

149. After the diagnostic report's exact VersionId, singleton/no-delete history, local SHA and unmounted state were reverified, temporary volume `vol-0dd9f5ea6774d3891` was detached and permanently deleted, and source snapshot `snap-0163eada14dcab899` was permanently deleted. The raw failed filesystem is therefore no longer recoverable; its relevant unit, journal, cloud-init, syslog and storage evidence remains durably recoverable from report VersionId `PtgMH.nKok3qtrlBfpapkETCLBbdvWeI`. Persistent workbench `i-038112fe75c610517` was explicitly stopped at `2026-08-05T00:30:21Z` and read back `stopped`; its encrypted 1 TiB root remains preserved. Obsolete workbench stop schedule `pi05-deadline-69c8fe7f-e18c-4d88-bac4-4dd935322554` and terminated evaluator schedule `pi05-deadline-195336b9-5e0c-425e-ac28-e6a8787ca117` were deleted after their targets were no longer live. The two active training deadlines were not changed.

150. LIBERO continuation `libero-shallow-continuation-20260804t165358z-a1-5000` completed all 5,000 optimizer steps and closed successfully on retained instance `i-0489a1bb92cb9ddb9`. Its final logged point at step 4,990 reports loss `0.0172`, KD MSE `0.00687`, KD cosine `0.99673`, and per-joint NRMSE mean/max `0.04527`/`0.22336`. The exact run manifest is VersionId `ZbJwoQDmBHndUujbneabQn8_fxa_QjHd`, 194,166 bytes, SHA-256 `fe27e6bba663f620c25007128c92e71c85951d317568f7e0103381006c880642`; final-sync evidence is VersionId `h1.ckG_lmqvIWt7VLcUZ1VLB1xNcdHBM`, 858 bytes, SHA-256 `71d3ef835794b8fe2cf5d16018ee950b955b07c930d0a3f2a8544779bc9608d4`; and worker-input descriptor VersionId `szJpmZ4nVRAoQoIXbQA5BMigE7vfysvH`, SHA-256 `d642c5e2747ba77434df6ba63219ac48eaf64d85c45912647d245fb4dcc6fffe`, binds model SHA-256 `762ca3de4adaed3483b8fcd72bff059c4a2988ef7d1eb6622d9a1f0066b288ee`. All seven checkpoint objects, the three terminal manifests and their histories were audited singleton/no-delete before promotion. The first same-node gate dispatch, SSM `72346ada-fe77-4deb-bf6d-6dea7cb4c376`, failed before creating an attempt root because the prepared control had not actually been installed at `/opt/pi05/libero-shallow-5000-same-node-offline-eval.sh`. The unchanged 22,329-byte control SHA-256 `1fa14e9087f64d4ed0a0c0299df3f6a155fdde62ca9b331a3229bcb32fcb5861` was then published create-once as VersionId `amwNws18AThLeL8ByJHnpgTYhYMUsJge`, installed and verified by SSM `5ebfd133-3dfb-4d27-9c0f-5c6970688658`, and executed exactly once as SSM `5617cf7b-6d83-4742-9ae4-9c633c424f38`. The 2m40s network-disabled batch-four evaluation passed its structural and publication gates. Its six singleton/no-delete artifacts are control `P7VZb5Sp36hbBZWFVVVPyLiEzTs5aiBX`, log `EE45k.k6MbyqvNMWmPDqyBfucuQ5y2iE`, report `sv3__7zP_5ABC_dFov9YSpXj9Ga2Q1Zx`, arrays `Wu3oLqpj0jHgceWImRkvY61rEX5HpXTs`, evidence `DzxcSkiaixp5E8BRoPIRuPNdEUTjOWcC`, and receipt `ygiKoIllyoi39bTcGZgiyNCMRMnnWdOm`; report/arrays SHA-256 values are `2523075f98246ef483201b7272d195de9fba0b72983ac49f85d8c86e63d6c651` and `d5da24943f5e160a545347855cb981cb0a5cc9b74c1dc840828d88039c1e6daa`. Compared with the exact 2k offline corpus, action KD MSE improved 13.01%, action RMSE improved 6.73%, action cosine rose `0.9466431 -> 0.9550210`, maximum action-joint NRMSE improved 28.61%, and range excursions improved 8.69%. Velocity KD MSE regressed 14.66% and cosine moved `0.9908292 -> 0.9897089`, so this is evidence to run the already-bounded SnapFlow pilot and 10k continuation, not a final quality or closed-loop acceptance claim.

151. Fast-lane capacity was allocated before the next long rung. The accepted checkpoint descriptor, deterministic renderers and explicit `b19cb829c1ca43665dd74a23d7a3826097b7ad26` controller produced two dry-valid worker specs. The 4,770-byte SnapFlow spec SHA-256 `367bc18254708317b18ac868d5d81f7b69fbb266af1d1da0165a2f525088a3d6` is singleton/no-delete object `specs/libero-snapflow-pilot-20260805t005500z-a1-5000.json`, VersionId `WsQhejxafU15JIuaEi7HiJJdzCVvXVB0`; it stages the exact 5k Shallow checkpoint and runs a fresh 5k `pi05_libero_l09_snapflow` experiment. The 6,268-byte continuation spec SHA-256 `e73adf40d340bab9141be2ef1d5c3ba0b01f6e4e8a4a49de628bf4a47c852ff6` is singleton/no-delete object `specs/libero-shallow-continuation-20260805t005500z-a2-10000.json`, VersionId `m4o.9rNJ4q5xj_R0WWP45.glz46VFTqv`; it restores the exact 5k optimizer state and targets 10k. Ordinary On-Demand `g7e.2xlarge` fast worker `i-05e6333955386563b` launched first under reservation `96b7afae-346c-411f-a0b8-49232706d522`, a conservative 24.25-hour / `$81.55566` reservation and independent external plus guest deadline `2026-08-06T01:01:08Z`. Corrective one-GPU On-Demand `g7e.4xlarge` long worker `i-004c505e9e1e4bf1f` then launched under reservation `23de8b53-9c0e-498c-97a7-6e65b158a7d4`, a 10.25-hour / `$40.98114` reservation and deadline `2026-08-05T11:01:45Z`; it is retained after successful training for same-node gating. Both use `CapacityReservationPreference=none`, encrypted delete-on-termination 256 GiB gp3 roots, IMDSv2, controller bootstrap VersionId `SUd.ZZrkD_D67gCqaII25f6etYcMseHK`, and enabled exact-instance self-deleting schedules. A read-only SSM probe first repeated the known `/bin/sh` `pipefail` mistake as command `9861de42-276a-4bfd-b917-0c945026de88`; corrected explicit-Bash command `cf3325f7-6a4e-491f-ab50-0f0885114aaf` verified both live guest deadlines, launch metadata and active workers without changing either job. Long-worker readback showed the expected exact 5k optimizer transfer in progress; fast-worker readback showed its verified controller process active. The exact authoritative 23-entry ledger is VersionId `FYpMm2Qnfi0GVTGVQ49tr1VWWkBqkRlH`, 43,051 bytes, SHA-256 `afbce9de523aea33afca076a0065aec3bb235e855ccd21178763fca732971647`; committed compute is `$488.45964`, leaving `$2,261.54036` after the `$250` non-compute reserve. After all 5k evidence and both next-stage contracts were durable, termination was requested only for the retained source instance at `2026-08-05T01:02:18Z`; its obsolete schedule was removed at `01:03:59Z`, and EC2 reached `terminated`/code 48 by `01:09:16Z`. Delete-on-termination root `vol-0ae527d6ebf7873e0` is permanently gone and not recoverable; its accepted checkpoint, manifests and offline evidence remain recoverable from their exact S3 VersionIds. No paid reserved, Spot or Capacity Block capacity was used.

152. The DROID terminal same-node control was made executable before the 2k job reaches its upload boundary, without interrupting training. The reviewed local 22,581-byte script SHA-256 `3967ee00eab6ef0899e66775fc234e7106fa700575f63d9eb1709ed2419d3126` was published create-once as `manual/controls/droid-shallow-2000-same-node-offline-eval/3967ee00eab6ef0899e66775fc234e7106fa700575f63d9eb1709ed2419d3126/control.sh`, VersionId `tc8wr19RiurQBBP2xbPXC_xky6mitdxi`. SSM `ed73664e-f922-4fbf-935f-228657c3a3fa` exact-VersionId downloaded it to a fresh temporary path, verified the hash, and atomically installed `/opt/pi05/droid-shallow-2000-same-node-offline-eval.sh` as mode `0500`. Training remained active; at step 1,240 it reported KD MSE `0.01669`, cosine `0.99698`, and per-joint NRMSE mean/max `0.07962`/`0.35070`. Measured time remains about 10.17 seconds/step, projecting optimizer completion near `2026-08-05T03:14Z`, roughly 56 minutes before the unchanged exact external and guest deadline `04:10:27Z`. Do not dispatch this control until the source service is inactive/success and terminal artifacts are present.

153. The diagnosed base-LIBERO evaluator was retried only after the eager-policy identity correction in entry 148 and the entry-140 `us-east-2a` capacity failure/reconciliation justified the exact pinned `us-east-2b` subnet. The fresh 5,944-byte spec retains db12 model source and evaluator image while changing only controller source to `f4de527e967ba873830946e92147f401e1e5cb67`, run identity and S3 output prefix. SHA-256 `9d4f2c80cc708e137df148c2b38b3146df486b8d2b97d6f5d86ee8d0ec7d8b27` is singleton/no-delete object `specs/libero-base-intermediate-20260805t011000z-a2-400.json`, VersionId `6ZUykTNSu5BhB7ST2BH7zwetDxDAXLJG`; the exact f4de bootstrap command SHA-256 is `a5669d9a8ae021be25d0bfe36b8a32ea322cdb1a803125fde1aeb4d103ccdc9d`. Ordinary On-Demand `g6e.4xlarge` `i-07113ab71e463e8e5` launched in `us-east-2b` under reservation `3ac2e72d-22ad-4c7c-a431-0e4d2100c17b`, with `CapacityReservationPreference=none`, required IMDSv2 and encrypted delete-on-termination root `vol-0079093db106d7353`. Its enabled self-deleting external schedule and active guest timer both target only that instance at `2026-08-05T04:11:53Z`; the corrected launcher will retain a failed node for inspection while a successful run powers off immediately. Read-only SSM `a1664c06-aa54-466b-b2a0-d44fd44e0135` verified exact launch metadata, model checkout `db12a82...`, controller checkout `f4de527...`, active service and matching guest deadline. This proves staging is healthy but does not yet claim the image-identity or rollout gate passed. The additional conservative 3.25-hour reservation is `$9.76378`. Authoritative 24-entry ledger VersionId `Pu0C1PKVzEzbA3Fb_CJVeS9wOe91utZe`, 45,078 bytes, SHA-256 `6af42781d7eda2976bbbc16a2a5421151af3deee0dfad40824d2e614317a2961` is byte-identical locally and remotely; committed compute is `$498.22342`, leaving `$2,251.77658` after the `$250` non-compute reserve. The four active jobs use 56 of the exact 64-vCPU quota.

154. The first LIBERO SnapFlow pilot failed on its first backward pass but was retained on the same paid `g7e.2xlarge` by the entry-142 launcher fix; no replacement instance, reservation or capacity was created. Its immutable failed prefix contains five log segments plus run manifest VersionId `hHAZ9czWN7Dt9RUWPpeZ50CwTxSwCjP7` and final-sync evidence VersionId `gmVfLd3nU4I7dcOHNohJkPLVhB_Q9_Qp`, with no checkpoint or expected-output marker and no delete markers. Narrow journal extraction SSM `f9e65ce6-9075-43b5-b81f-9e4886dad5a0` proves the exact fault: Transformers `DynamicCache.batch_select_indices` reduced the shared prefix KV cache from batch four to the two consistency samples in place; non-reentrant gradient-checkpoint backward then recomputed the earlier full-batch expert pass and failed concatenating cached batch two with new batch four at `modeling_gemma.py:310`. Image inspection SSM `6f3e5808-7b88-40e7-b1e2-eab24e7142be` independently read back pinned Transformers `4.53.2` and its in-place cache method. The narrow source correction deep-copies the frozen prefix cache before selecting consistency indices, preserving the full-batch cache for checkpoint recomputation; a regression explicitly exercises an in-place cache and proves the original remains unchanged. The combined SnapFlow, renderer, trainer and worker regression set passes locally: 105 tests in 2.86 seconds, with Ruff 0.11.12 check/format and `git diff --check`. This is a same-node recovery intervention under the existing `$81.55566` reservation and unchanged `2026-08-06T01:01:08Z` hard deadline; the failed run remains immutable and the corrected replay must use a new run ID, exact source bundle and run prefix.

155. The f4de base-LIBERO retry passed the corrected eager-image identity gate and reached policy-server construction, then failed before the first rollout because the deliberately numeric non-root container identity `1000:1000` has no passwd entry in the immutable evaluator image. PyTorch `torch.compile` called `getpass.getuser()` while deriving its Inductor cache directory and raised `KeyError: getpwuid(): uid not found: 1000`; SSM `c262e02a-b79c-47b4-890e-9d1f6d073949` captured the terminal service and traceback on retained instance `i-07113ab71e463e8e5`. Its immutable failed prefix has two logs, run manifest VersionId `KDT4G4b3bAcDy1vBnukv16eZ0ERJBRHO`, final-sync VersionId `EYJS9zISbkkbm_3qDGxPnFibA4SI3b0L`, no expected-output marker and no delete markers. The narrow controller correction injects worker-owned `USER=pi05` and `LOGNAME=pi05` into every numeric-UID container, while rejecting spec overrides; the existing isolated writable `/cache` remains the actual compiler-cache location. The same 105-test and Ruff/diff gate in entry 154 covers this correction. The evaluator stays on its original `g6e.4xlarge`, original `$9.76378` reservation and unchanged `2026-08-05T04:11:53Z` hard deadline; replay must preserve the failed prefix and use a fresh exact spec/run ID on this node.

156. Commit `b3fb2e2438b76f6d8f4b5c22caf1860257c0e46c` contains only the two retained-node runtime corrections plus entries 153-155. Its complete-history 1,958,462-byte bundle SHA-256 `7855b3271df74d2f68fb1ab3f5f86b9b439466e7f574588cb141e8273c396f04` and unchanged 8,888-byte bootstrap SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94` passed local strict fsck, bundle verification, a fresh empty-bare fetch and audit-only empty-history checks. Create-once publication wrote `source/openpi-b3fb2e2438b76f6d8f4b5c22caf1860257c0e46c-complete.bundle` VersionId `0NjAy8c.dSe9OqFQcwCgKZZGHT7vFhq2` and `bootstrap/worker-bootstrap-controller-b3fb2e2438b76f6d8f4b5c22caf1860257c0e46c.sh` VersionId `DlW3e7QvBvbAi2dPg0.8Hrj.uigTUR7O`; immediate audit replay exact-downloaded and rehashed both singleton/no-delete AES256 objects. No capacity, ledger row or deadline changed.

157. The retained evaluator replay uses a new immutable prefix rather than overwriting its failed run. Deterministic renderer output `libero-base-intermediate-20260805t013000z-a3-400` is 5,944 bytes / SHA-256 `ad13ef05e22c6ec4b9315569f4235d78848a30981eb134a7dd7ea1d6c81c8ebf`; create-once spec VersionId `fyxOr3a9l.htg9ic36.xoyT_uoDw.ASk` keeps db12 model/image source and changes only controller source to b3 plus run identity. The first installation SSM `0ee0c445-83a7-45b0-855b-25b2a4085db7` exposed that AWS CLI removes a pre-created `mktemp` destination during `get-object`; explicit fresh-path installation succeeded on the next dispatch. Initial control SHA-256 `7da7c2801e57821b4c8f1337801e75ec12a84724b8e1c6dd6dab493f8395ecb2`, VersionId `AB91eoM3sWF0PwQ3pLGaJCDWc1Pz8x5y`, then failed read-only because an empty `list-objects-v2 --query KeyCount` rendered `None` rather than `0`; diagnostic SSM `15022cf7-5fdd-40bc-a4cd-1527f4f7e530` identified the exact check. Corrected control SHA-256 `70ba639ebfd21d1dcdc85941f776cd9ca15bf9b753f61f56e4b217de87c12ea2`, VersionId `0ZTxu1A.3BQyPz7CICorElQ1cnPP_n1b`, uses explicit JSON array lengths. SSM `eb273a74-31c2-4b41-b55b-35ddf823f8e9` archived the f4de controller checkout/evidence without deletion, installed and fsck-verified b3 at the fixed controller path, dry-validated the new contract, and started `pi05-evaluator-recovery-a3.service`. It passed the former UID/cache failure, loaded the model, opened the policy server and entered `libero_spatial` on the same `i-07113ab71e463e8e5`; the original external and guest deadline remains `2026-08-05T04:11:53Z` and no new reservation was added.

158. The b3 LIBERO policy-image rebuild remains on retained fast worker `i-05e6333955386563b`, with 163 GiB free root storage and no replacement capacity. Initial build control SHA-256 `bf4ada5da5e68e2f7e3659546f1431475ddb4f5b55facd5a344680fc2995d95c`, VersionId `Vkzy3CoWmim8dIbfBgZffOy1X8KAEr9S`, exact-fetched and verified the failed SnapFlow manifests, current source bundle, instance/deadline and absent immutable ECR tag. Unit `pi05-snapflow-image-build-b3.service` then failed before a Docker layer executed because the private AWS DLC registry `763104351884.dkr.ecr.us-east-2.amazonaws.com` returned `401 Unauthorized` during base-manifest resolution. The 1,741-byte failed log is SHA-256 `6e70c9d50d674a3eae54b6a2568c9090d11267c057bd1f5e1031809b3d27cea8`. Recovery control SHA-256 `80e0265d209b1dfb58f3f764f99dd9befe3e9819b90102e2d529f08e6f293e6c`, VersionId `u8bG61QQm_MdK9GxesM3pXZR3XI9gWA.`, adds only an ECR login for that base registry, reuses the exact source checkout/build directory and refuses a pre-existing target image/tag. SSM `d72881fd-5154-4d51-bd81-78aab599d6a2` started `pi05-snapflow-image-build-b3-recovery-01.service`; authenticated base resolution and the source build are now active under the unchanged `2026-08-06T01:01:08Z` external and guest deadline. This is installation work inside the existing `$81.55566` reservation, not a second paid action.

159. The retained fast worker completed the b3 policy image without replacement capacity. Recovery 02 corrected only the non-root handoff permissions; recovery 03 built the exact local image but exposed that the runtime image deliberately omits the repo-wide test-only `pynvml` conftest dependency; recovery 04 independently proved GPU and numeric-UID `getpass` behavior, then ran the 12 relevant tests with `--noconftest`. Recovery-04 control SHA-256 `0a692d87e8d1575c2f46e1959b1602645c309a72e1d471b8707d70f9fac5973e` is VersionId `Oup1900XM5cgG51196O3wK5uD..dk9n6`. The immutable corrected policy image is `752160877725.dkr.ecr.us-east-2.amazonaws.com/pi05-repro@sha256:bdb1460b3c8d37c4dfc0bb914e9b80661b67c6cd12020973175a788c51ee4271`; the result object is VersionId `yQIqfI.eD5TiiuIaMu2KwhJCKAWQiNSs`, SHA-256 `5bcc5fb8e028f2752761a602a40fc6a8e44c63df71c6415a9ee535e7a2babd36`. The four attempt logs and result are versioned under `manual/image-builds/libero-v2-b3fb2e2438b76f6d8f4b5c22caf1860257c0e46c/20260805T014000Z-i-05e6333955386563b/`; no capacity, ledger row or deadline changed.

160. A fresh corrected SnapFlow replay preserved the seven-version failed a1 prefix and uses new run `libero-snapflow-pilot-20260805t020500z-a2-5000`. Its 4,770-byte spec SHA-256 `0f2d8251b2449560691fba4704ee8653bbaf909d1af982fe00dbd389dda4ac3d` is singleton/no-delete VersionId `0cwzwPMgM3wKhlxDjZbzJhLrLMhV5dLe`; it binds the exact accepted Shallow-5k descriptor and corrected b3 image. Same-node replay control SHA-256 `d1f0f63810b565e454673d3decadc23ab125127209f0d5d1a6b0b9492b50ab7b` archived 11 superseded fixed-path files recoverably and started independent `pi05-snapflow-recovery-a2.service`. Because synchronous `systemd-run Type=oneshot` left the parent control waiting on that independent unit, recovery 01 verified the first backward before stopping only the parent cgroup; recovery 02 corrected its assertion to accept systemd's expected `failed/signal/15` terminal state for that deliberately detached parent. Recovery-02 SHA-256 `1a7d69ed32a65d6ff88b29410f3e23c1e84834fe0e41724730e10cd231a361e8` is VersionId `.1tCkvfrdvoHu8qB.kg3PIsrcm8xNVHV`; exact result VersionId `ldFYOTnKtS2uLeb5gIp80EUbgIZ5DiZL` records first-backward PASS at `2026-08-05T02:21:31.696Z` with total loss `0.02817`, FM MSE `0.05631` and shortcut MSE `0.00028`. Training remains healthy near 7.45 steps/s under the original `$81.55566` reservation and `2026-08-06T01:01:08Z` deadline.

161. Retained evaluator `i-07113ab71e463e8e5` completed all 400 base-policy LIBERO episodes before a terminal collector-only failure: 386/400 aggregate (`96.5%`), with suite totals spatial `99/100`, object `99/100`, goal `94/100`, LIBERO-10 `94/100`, zero infrastructure errors and 67,725 environment steps. The immutable evaluation manifest is VersionId `tvb5VrjKOtJkbfDgEnAkykz_JqRWTlYS`, SHA-256 `f4c64325ba2e6e4eab424b9360dda4f3ef3081eeae585e1da75d2bc807b81a77`; run manifest VersionId `.lPXJqfS4rUmeSC7Sl6Qk7iWaEu3ExRn` and final-sync VersionId `LU0BsBc8Vg9fZISEhbcukx70a.qjdshz` durably cover the five expected rollout artifacts and 45 logs without delete markers. The post-container worker returned exit 3 because the generic metrics resolver searched staged artifacts for a dataset revision, while this evaluator correctly binds baked LIBERO benchmark assets through the immutable image's `libero_simulator_revision`. The narrow collector correction now requires dataset identity exactly `{name: "LIBERO fixed benchmark assets", revision: image.libero_simulator_revision}` and keeps generic training/latency resolution unchanged. Three focused regressions, the complete 60-test worker file, Ruff 0.11.12 check/format and `git diff --check` pass locally. The expensive rollout will not be rerun; its exact artifacts will be validated and accepted by a separate same-node salvage gate after publishing this corrected controller.

162. Collector correction commit `bc21ad726a70a88fa1fed1fd7d7ddf9e9f498f40` passed the complete 60-test worker file, Ruff 0.11.12 check/format and `git diff --check`. Its complete-history 1,961,175-byte bundle SHA-256 `1ea3d405bcaff50e2afdbc6879ce0ba9d7f53b70a316c7e13e89a83fac2cad63` and unchanged 8,888-byte bootstrap SHA-256 `b3a81e2163661789d7a76eba9b62ce6d10ee79eff1784e004a7cfc48f4651d94` passed strict fsck, bundle verification and a fresh empty-repository fetch. Create-once publication wrote bundle VersionId `EL3BQ2mr_rUKDVBIhzP2EA_pjKnkrAAm` and bootstrap VersionId `5_bEqiEEva_bUsHIZ5cfSRieEza3iHMB`; exact-VersionId round trips reproduced both hashes with singleton/no-delete histories. This changed no capacity, ledger, schedule or worker deadline.

163. Corrected SnapFlow run `libero-snapflow-pilot-20260805t020500z-a2-5000` completed all 5,000 steps on retained On-Demand `g7e.2xlarge` `i-05e6333955386563b`. The service exited zero at `02:33:52Z`; its 26 S3 objects are all singleton/no-delete. Exact terminal artifacts are run manifest VersionId `PBy8g3RLEPuGk5AjuWns13CUqdUk6o.0`, SHA-256 `5bd8dbeeae9d1d1f92fdada62d4c33bbf573b212a340d7c4b9367788b33c5ff4`; final sync VersionId `1DlYOjE_X3uF1o5f73FkC1XW7ECUOjjt`, SHA-256 `cc65df38836d27bea98830f75a1b1ad53c73b5e9f66d62cc5db579033a9f44ff`; and worker-input descriptor VersionId `lhv9HlHxJEU7MYSjaCZJNVFevuTWfXJ0`, SHA-256 `47720e0696e24421da0ea59abc9cf23928da39ab4a8ad0f9622160fb4c4d66a9`, binding model SHA-256 `6bb32f0e0dd92202b1a01e82a221853a571d63a614003235cd9e42f3bd6b5f99`. The first same-node gate control incorrectly summed a cancelled ledger reservation; recovery 01 checked only launched spend but mounted a numeric teacher checkpoint at a nonnumeric container path; recovery 02 mounted the parent run and resolved explicit step 5,000. That evaluation completed but correctly failed the empirical gate: one-step MSE `0.0021473438` versus naïve-one-step MSE `0.0022671017`, closing only `5.2824%` of the offline gap against the required `70%`. No rollout, export or compile was promoted. Failure-publisher SHA-256 `3b25dbebf881c4f4fe435bd677ff94dee44312a043b3ab728868b00a05283a65` preserved the report/arrays/log without rerunning evaluation. The seven evidence objects under `manual/offline-eval/libero-snapflow-5000/20260805T025000Z-i-05e6333955386563b/` are singleton/no-delete; publication VersionId `y6JXvoYgUCryOgXG63uxbi43enR2MR7.` explicitly records `execution_gate_pass=false` and `promotion_allowed=false`.

164. The completed 400-episode base evaluation was salvaged only at the metrics-collection layer on retained evaluator `i-07113ab71e463e8e5`; no simulation episode reran. Manual attempt 00 expected the failed systemd unit to be inactive rather than active-state `failed`; attempt 01 exposed the older evaluator AMI's jq without `abs/0`; attempt 02 tried `git bundle verify` outside a repository. Attempt 03 changed only those preflight mechanics, exact-fetched the bc21 bundle into an isolated checkout, revalidated every one of the 53 singleton/no-delete original run objects and six committed expected-output markers, and reran corrected `collect_run_metrics` against the unchanged files. It accepted 386/400 (`96.5%`), spatial `99/100`, object `99/100`, goal `94/100`, LIBERO-10 `94/100`, zero infrastructure errors and 67,725 steps. Final control SHA-256 `3cc84201315dcfff44256477a051bcfbbcdd8e4182efda6142ea6c365b4a673a`; accepted-metrics VersionId `d5KAt6Wq47pFsNqzF.XQr5smhGpQRJAh`, SHA-256 `6b5e110d82fa910a49f458a4222e53b68230957cb8780d2a031f7d3d6e15a366`; publication VersionId `hjfGeGg804KyL8GcbFK_X7VLEPwteaVE`, SHA-256 `b5e2a238de65ede4ace2b021a9efd7bda34c471085290472730c34e00079d202`. All five salvage objects are singleton/no-delete and bind the original run manifest, final sync and evaluation-manifest VersionIds. Termination was requested only after that audit; EC2 is `shutting-down`, and the external deadline remains until termination and root-volume deletion are confirmed.

165. A separate deterministic SnapFlow-resume renderer now enforces only the bounded `5k->10k->20k->30k` ladder, resumes the exact accepted checkpoint and optimizer state, retains only the track dataset/JAX input plus the accepted checkpoint, and emits one target checkpoint. Eleven combined fresh-SnapFlow, SnapFlow-resume and Shallow-resume tests pass, along with Ruff check/format and `git diff --check`. Its first dry-valid contract is `libero-snapflow-continuation-20260805t030000z-a3-10000`, 4,887 bytes, SHA-256 `c6ba09b2053d206b3392872a952c5820f03572eed1d9e0a3798c5796608cfdf7`; it resumes exact descriptor revision `9d1aafc9d2698daa3ec33efabe2eb268b36a39dd509a95a019b0155ae99b8ad9` under the existing fast instance/deadline. The spec has not yet been published or executed at this runbook point.

166. The entry-165 candidate was accidentally published with a guessed renderer-commit metadata value. Preserve rather than delete VersionId `pKe2xnbOJD7YRC_1Yc7GvYp5RA_XEqEf`; it is object-tagged `Status=invalid` and `Reason=renderer-commit-metadata-mismatch` and must never execute. A fresh run identity corrected the publication: `specs/libero-snapflow-continuation-20260805t030500z-a4-10000.json` is the sole 4,887-byte AES256/no-delete version of that key, VersionId `4xd5wLEVCIHw44eWvO5FGobR75f0V2ln`, SHA-256 `dd75e90706f2a3c3022b4ded95a3140a6322a6722428d74da46420f5929f1ab5`, with exact renderer commit `b314b9fba2c1385f3e95469e9e3cd187a8b9c723`. Same-node start control SHA-256 `61c1ad2c636c90bfae1c11447263db022023baf766825831d7dc62e480d60796` exact-audited the failed 5k offline promotion, accepted 5k checkpoint descriptor, clean b3 checkouts, empty 10k output namespace, unchanged On-Demand identity and retained deadline. It archived only the two fixed worker command/metadata files, dry-validated the exact full-state resume, and started `pi05-snapflow-continuation-a4.service` without new capacity or ledger reservation. Start-result VersionId `qHyZURISPHjkDPXmDOwOupEFlXWAqMkE`, SHA-256 `2b2fe2ac5caae12d62b19378d9dbf4041851ff0030caf510917a49af29d7188f`. Measured continuation throughput remains about `0.135` seconds/step on retained `i-05e6333955386563b`; at `03:18:08Z` it reached step 6,450 and projected step 10,000 near `03:26Z`, before checkpoint validation/upload.

167. Base evaluator cleanup completed only after the entry-164 exact salvage. Instance `i-07113ab71e463e8e5` reached `terminated` with user-initiated transition time `2026-08-05T02:53:19Z`; encrypted delete-on-termination root `vol-0079093db106d7353` no longer exists and is permanently unrecoverable. The accepted 386/400 rollout, original immutable run artifacts and salvage evidence remain recoverable from their exact S3 VersionIds. Obsolete external schedule `pi05-deadline-3ac2e72d-22ad-4c7c-a431-0e4d2100c17b` was then deleted and a `GetSchedule` replay returned `ResourceNotFoundException`. This released the evaluator's 16-vCPU quota allocation; no rollout rerun or replacement capacity occurred.

168. DROID pilot `droid-shallow-pilot-20260804t210300z-a2-2000` did not OOM. It reached step 1,875 at about `10.18` seconds/step, then the worker deliberately killed the container at its unchanged internal upload-buffer cutoff `2026-08-05T02:55:27.700173Z`, despite the external and guest deadline having previously moved to `04:10:27Z`. Failed run manifest VersionId `ZVjNCm2NZ.zfn31b78vqsBZjrcykTW8i`, SHA-256 `ede5a9edfb19efd22341dba10c3d3bed9d56830db40310b47f353cedb21fbb13`, records exit 137, no Python failure, no committed checkpoint marker and no published input; final-sync VersionId `6p5hQEFd8JDstDBqv5mwgKz5xr9tlWxW`, SHA-256 `0466353a00edca12d247746be7ab080dff87babe4a66180e2e5dd55e50f7ce96`, durably closes 324 receipts. Audit-first extension control SHA-256 `5d96103b5310e0515f7c531a859055c852105734b46fa90f476cec5ab7a25c33` reserved exactly seven more retained On-Demand `g7e.4xlarge` hours / `$27.98712` as `fd2b2319-d23b-4515-9457-efdaa2086854`, then synchronized the existing external and guest deadlines to `2026-08-05T11:10:27Z`. Authoritative 25-entry ledger VersionId `B.4C_IcH4SjRFDABjMjMsxE71znNA1uR`, 46,152 bytes, SHA-256 `de340270252e84d44a955f0946a947e2679336b6e04f02afef105b8724451387`, now commits `$526.21054`, leaving `$2,223.78946` after the `$250` reserve. No replacement capacity was created. Fresh deterministic four-loader recovery spec `droid-shallow-recovery-20260805t031500z-a3-2000` is singleton/no-delete VersionId `ClxY7SIxmgOhf91EJCX6kP_z5lHa_Zb_`, 5,738 bytes, SHA-256 `3298b34c8ee2c3176e676c1742c8dbc9dcdc2e76c02525a3c925cb54068dadd5`. Same-node start control SHA-256 `ecc3f7638a8b3970e58859fae92dfb88cf6f9c1325b8e46846d65dd2d6c63621` bound the exact failed terminal evidence and new ledger/deadlines, preserved the superseded fixed command metadata in its recovery archive, updated the worker's internal hard deadline, dry-validated the new prefix, and started `pi05-droid-shallow-recovery-a3.service`; start-result VersionId `j846ts8.RkYbvBVJ1fPU65uWNZAXMWy7`, SHA-256 `54cda87b1bfd2b5ab3b5f3eab4a1c85d0fe6d9e1983fcc70b5f5a1fd0ec7beae`.

Record any future console action or command here before incorporating it into CloudFormation.
